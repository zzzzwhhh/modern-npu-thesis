#import "/template.typ": algorithm, algorithm-ref, capfig, capsubfig, captab, equation-note, indent, multicite, nwpu-thesis

#show: nwpu-thesis.with(
  anonymous: false, // 是否开启盲审模式
  title: "基于 Typst 的西工大论文模板",
  author: "航小天",
  major: "计算机科学与技术",
  supervisor: ("张三", "教授"),
  submit-date: (year: 2026, month: 3),
  abstract: [
    
  ],
  keywords: ("无人机集群","路径规划；多障碍物环境；螳螂搜索算法；AL-SHADE；人工势场法"),
  abstract-en: [
    
  ],
  keywords-en: ("UAV swarm", "path planning", "multi-obstacle environment", "Mantis Search Algorithm", "AL-SHADE", "Artificial Potential Field"),
  appendix: [
    == Test
    附录内容……
    #capfig(
      image("figures/example.jpg", width: 45%),
      caption: [图片测试],
      label: <test1>,
    )
  ],
  acknowledgement: [
    致谢内容……
  ],
  design_summary: [
    小结内容……
  ],
)

= 绪论

== 研究背景

=== 研究意义

随着无人机技术的快速发展，无人机在城市低空物流配送、环境监测、灾害救援及军事侦察等领域的应用越来越广泛。相比单架无人机，无人机集群（UAV Swarm）能够通过协同控制实现更高的任务效率、更强的鲁棒性以及更复杂的作业能力。然而，在实际应用中，无人机集群在复杂环境下的飞行仍面临诸多挑战，尤其是在多障碍物环境下，集群路径规划的难度显著增加。

传统路径规划方法，如粒子群算法（PSO）、蚁群算法（ACO）和遗传算法（GA），在连续空间的全局优化中表现良好，但在二维高密度障碍物环境中容易出现路径不连续、局部收敛或早熟收敛问题。例如，PSO在靠近障碍物边缘时容易出现“贴墙飞”，ACO在密集障碍物区域可能陷入循环，GA在交叉重组过程中可能产生穿越障碍物的不可行解。同时，经典的人工势场法（APF）在复杂障碍物环境下容易导致无人机集群陷入局部极小值或死锁，尤其是在“U型”或狭窄通道障碍物处。

近年来，生物启发式算法和自适应优化算法在连续优化问题中显示出强大的全局搜索能力。螳螂搜索算法（MSA）模拟了螳螂捕食的突击与猎物追踪行为，在处理多峰值连续函数优化时具有优异的探索能力，但其原生算法在二维带障碍空间中存在随机初始化不均、早熟收敛严重的问题。AL-SHADE算法则利用成功历史自适应参数和线性种群缩减机制，实现了微分进化算法在高维连续优化问题上的高效搜索，但直接应用于二维离散路径规划会破坏路径的空间连续性，导致无人机航迹不平滑。

因此，研究适用于二维多障碍物环境的无人机集群路径规划方法，不仅需要实现全局路径的最优性和连续性，还需兼顾集群编队稳定性与局部动态避障能力。这不仅具有重要的理论价值，也对无人机群体智能协同及复杂环境下自主飞行应用具有现实意义

=== 研究现状

1. 国外研究现状

在无人机集群路径规划领域，国外研究起步较早，尤其在多无人机协同控制、智能优化算法和复杂环境避障方面积累了丰富成果。针对您的研究任务，国外研究主要集中在以下几个方面：

1.1 编队控制与协同飞行

国外学者提出了多种无人机集群编队控制方法，包括虚拟领航者（Virtual Leader）模型、一致性协议（Consensus Algorithm）、队形保持算法等。通过邻接矩阵、拉普拉斯矩阵等拓扑结构，集群能够在二维或三维空间中稳定形成“V”型、横排或人字型队形，并实现信息分布式共享和收敛。尤其在狭窄通道或动态障碍物场景下，动态队形调整和自适应收缩机制被认为是保证集群安全飞行的关键方法。

1.2 全局路径规划

国外对多障碍物环境下的全局规划研究十分活跃。传统启发式算法（PSO、ACO、GA）被广泛应用于连续空间优化，但存在早熟收敛、局部最优或路径不可行的问题。为解决这些问题，近年来引入了新型群体智能算法和混合优化算法，如螳螂搜索算法（MSA）、改进微分进化算法（AL-SHADE）和遗传算法变体。MSA通过模拟螳螂捕食行为提高探索能力，而AL-SHADE利用历史参数自适应调节搜索步长，实现复杂高维空间的全局优化。这些算法在无人机二维路径规划上的应用，主要问题是如何保证初始解可行性、路径连续性和集群协同。

1.3 局部避障与动态环境适应

国外研究普遍关注无人机在动态或未知障碍物环境下的局部避障。改进人工势场法（APF）、动态势场、潜力函数与切向旋转力等技术被提出，用于引导无人机绕开障碍物、降低局部死锁率。同时，结合编队控制，动态调整队形权重和通信拓扑，以保证在狭窄通道或紧急避障情况下集群安全通行。

综上，国外研究在无人机集群路径规划中，已形成理论成熟、算法多样、应用场景广泛的体系，但在二维高密度障碍物、路径连续性和集群协调耦合方面仍存在技术挑战。

2. 国内研究现状

国内无人机集群路径规划研究近年来发展迅速，主要聚焦于工程实现与算法改进、分层规划框架和集成仿真验证。针对您的研究任务，国内现状可概括如下：

2.1编队控制层研究

国内学者在虚拟领航者模型和一致性算法基础上，提出了横向偏置调节、队形自适应重构等方法，能够保证多无人机在二维空间中“V”型或“人”字型编队稳定收敛。尤其针对狭窄通道，国内研究强调在局部避障触发时动态修改通信权重，实现队形压缩和恢复。

2.2 全局路径规划层研究

国内研究者在PSO、ACO和GA等经典启发式算法基础上，提出了适应度函数优化、禁忌表、坐标变异算子等改进方法，以应对密集障碍物环境下的不可行路径问题。同时，有学者尝试将MSA、AL-SHADE等新型群体智能算法引入无人机路径规划，但主要挑战在于二维离散路径的连续性、算法维度过高和初始解质量低。国内研究通常通过B样条曲线等参数化方法实现路径降维和连续性维护。

2.3 局部避障与动态避碰

国内研究关注无人机在密集障碍物环境中的局部避障问题。改进人工势场法、切向旋转力引导、动态权重调整等技术被提出，以解决无人机在“U型障碍物”或动态障碍物出现局部死锁的问题。同时，通过与编队控制结合，实现狭窄通道中集群的安全通过。
3. 国内外研究比较与评价

国外研究优势：理论体系成熟，方法多样，算法创新和跨学科融合能力强，尤其在复杂环境下的集群协同、自适应优化和动态避障方面领先。

国内研究优势：注重算法工程实现和系统集成，强调三层耦合（编队控制、全局路径规划、局部避障）的实际可行性和仿真验证。

技术空白与挑战：二维高密度障碍物环境下，保持路径连续性、集群队形稳定性和多算法融合的高效性仍是国内外共同面临的研究难题。

== 研究内容

根据实际需求与面临的挑战，本文以二维多障碍物环境下的无人机集群为研究对象，围绕编队控制、全局路径规划以及局部动态避障等核心问题展开深入研究。为保证方法在复杂约束与密集障碍场景下具备可行性、稳定性与可复现性，本文的研究内容细化为以下四个方面：

1. 多无人机编队协同控制机理与算法设计： 构建基于虚拟领航者模型与多智能体一致性算法的协同控制框架，明确邻接矩阵与拉普拉斯矩阵对通信拓扑与收敛性的约束作用。针对二维平面中的编队保持与协同跟踪问题，设计位置与速度双误差耦合的控制律，并结合无人机运动学约束（速度、加速度、角速度）进行物理可行性校验。进一步引入横向偏置的自适应调节机制，使集群在开阔空间与局部受限空间内能够稳定收敛于“V”型、人字型等预定队形，并具备在通道收缩与恢复过程中的队形弹性。
2. 多障碍空间下的全局路径智能优化与轨迹平滑： 面向密集障碍物与多约束条件的二维环境，系统复现并改进粒子群（PSO）、蚁群（ACO）、遗传（GA）等经典启发式算法，通过禁忌表、路径可行性惩罚和坐标变异机制提升搜索效率与可行解比例。在此基础上，针对早熟收敛与高维路径编码问题，提出混沌初始化与非线性步长衰减的改进螳螂搜索算法（MSA）；针对差分进化在路径离散编码中的连续性缺陷，引入B样条曲线参数化的AL-SHADE算法，实现路径维度降维与连续轨迹建模，从而提升全局规划的平滑性与可飞行性。
3. 基于改进人工势场的局部动态避障策略： 面向局部动态障碍与“U”型障碍易陷入局部极小值的问题，在传统人工势场法（APF）的引力与斥力模型基础上引入切向旋转势场力，构建能够“绕障滑行”的局部引导机制。通过与全局路径引导点及编队控制律耦合，确保无人机既不偏离全局任务目标，又能在障碍边缘与狭窄通道中实现稳定的局部机动与高效逃逸。
4. 多层算法集成与仿真验证体系： 构建统一的二维多障碍物仿真环境，将编队控制层、全局规划层与局部避障层的算法进行耦合与分层验证。设置典型障碍密度与分布场景，采用路径长度、平滑度、规划成功率、收敛迭代次数与队形保持误差等指标进行横向对比，验证所提方法在安全性、稳定性与效率上的综合优势。

/* == 图表测试

引用@timing-tlt，以及@test。

#captab(
  caption: [三线表],
  label: <timing-tlt>,
)[
  | t   | 1    | 2    | 3    |
  | --- | ---- | ---- | ---- |
  | y   | 0.3s | 0.4s | 0.8s |
]

#captab(
  caption: [复杂表示例：聚合物基复合材料的性能],
  label: <composite-performance>,
  cols: (1.25fr, 1fr, 1fr, 1fr, 1fr),
  hlines: (
    (row: 2, stroke: 1pt),
  ),
)[
  | 材料           | 碳/环氧 | <    | 玻璃/环氧 | <    |
  | ^              | 纵向    | 横向 | 纵向      | 横向 |
  | 模量，GPa      | 181     | 10.3 | 38.6      | 8.3  |
  | 压缩强度，MPa  | 1500    | 246  | 610       | 118  |
  | 拉伸强度，MPa  | 1500    | 40   | 1062      | 31   |
]

#capfig(
  image("figures/example.jpg", width: 45%),
  caption: [图片测试],
  label: <test>,
)

图片之间的文字

#capsubfig(
  (
    (content: image("figures/example.jpg", width: 60%), subcaption: [第一个子图说明]),
    (content: image("figures/example.jpg", width: 60%), subcaption: [第二个子图说明]),
  ),
  columns: 2,
  caption: [总图标题],
  label: <fig-main>,
)

== 数学公式

可以像 Markdown 一样写行内公式 $x + y$，以及带编号的行间公式：

$ phi.alt := (1 + sqrt(5)) / 2 $ <ratio>

引用数学公式需要加上 `eqt:` 前缀，则由@eqt:ratio，我们有：

$ F_n = floor(1 / sqrt(5) phi.alt^n) $

我们也可以通过 `<->` 标签来标识该行间公式不需要编号

$ y = integral_1^2 x^2 dif x $ <->

而后续数学公式仍然能正常编号。

$ F_n = floor(1 / sqrt(5) phi.alt^n) $

== 算法示例

下面给出采用单独算法编号的三线表风格算法示例，见#algorithm-ref(<alg:binary-search>)。

#algorithm(
  title: [二分查找算法],
  input: [有序数组 $A$，目标值 target。],
  output: [目标值下标，不存在则返回 -1。],
  [left := 0],
  [right := len(A) - 1],
  [*while* left <= right *do*],
  indent(
    [mid := floor((left + right) / 2)],
    [*if* A.at(mid) == target *then*],
    indent([return mid]),
    [*else if* A.at(mid) < target *then*],
    indent([left := mid + 1]),
    [*else*],
    indent([right := mid - 1]),
    [*end*],
  ),
  [return -1],
) <alg:binary-search>
 */

= 相关工作
 


= 基础理论和算法实现
 == 编队控制算法设计与实现
 === 无人机编队说明
  首先，将无人机视为质点，并借助地面坐标系表示其位置与运动；在水平面上，原点O可任意选定。无人机编队的三维平面有l–ψ法和l–l法两种描述形式，本文采用l–l法。无人机间的相对位置关系由矩阵Rx、Ry给出。
 $ cases(
  R_x = mat(delim: "[",
    r^x_(1 1), r^x_(1 2), dots.h, r^x_(1 n);
    r^x_(2 1), r^x_(2 2), dots.h, r^x_(2 n);
    dots.v, dots.v, dots.down, dots.v;
    r^x_(n 1), r^x_(n 2), dots.h, r^x_(n n)
  ),
  
  R_y = mat(delim: "[",
    r^y_(1 1), r^y_(1 2), dots.h, r^y_(1 n);
    r^y_(2 1), r^y_(2 2), dots.h, r^y_(2 n);
    dots.v, dots.v, dots.down, dots.v;
    r^y_(n 1), r^y_(n 2), dots.h, r^y_(n n)
  ),

) $
其中$r^x_(i j)$,$r^y_(i j)$ 表示第$i$架无人机与第$j$架无人机之间的相对位置的差值，$r^x_(i j) = r^y_(i j) = 0$。

多无人机形成的理想稳定队形如下：
$ cases(
  |x_i - x_j| -> r^x_(i j),
  |y_i - y_j| -> r^y_(i j),
  |v_i - v_j| -> 0,
) $ 

其中 $x_i, y_i$ 是无人机的坐标，$v_i$ 是速度。

=== 无人机动力学模型

在无人机编队中，常采用含自驾仪的三自由度运动学模型。基础运动学方程在纵向上与横向上的运动是相互耦合的。文献[29]通过将横向航向自驾仪和纵向自驾仪进行解耦，建立起横纵分离的运动学模型。其中，无人机i的运动模型如公式(3)所示。
$ cases(
  dot(x)_i = v_i cos theta_i,
  dot(y)_i = v_i sin theta_i,
  dot(theta)_i = omega_i,
  dot(v)_i = frac(1, tau_v) (v_i^c - v_i),
  dot(theta)_i = frac(1, tau_theta) (theta_i^c - theta_i),
) $ <eq:kinematics>

其中 $v_i$ 是飞机在 XOY 平面上的速度； $θ_i$ 为航向； $ω_i$ 为航向角速度； $τ_v$ 为飞行状态常数对应的速度； $τ_θ$ 为航向角对应的飞行状态常数； $v_i^c$ 为无人机自动驾驶仪的速度参考输入； $θ^c_i$ 为无人机自动驾驶仪航向角参考输入

在XOY平面上的运动存在下面的耦合关系。
$ cases(
  tan theta_i = frac(v_(y i), v_(x i)),
  v_i = sqrt(v_(x i)^2 + v_(y i)^2),
) $ <eq:coupling>
$v_(y i), v_(x i)$分别为无人机在XOY平面上的横向和纵向速度。

因此自动驾驶的动力学方程可以表示为：
$ cases(
  dot(x_i) = v_(x i),
  dot(y_i) = v_(y i),
  dot(v_(x i)) = frac(1, tau_v) (v^c_(x i) - v_(x i)),
  dot(v_(y i)) = frac(1, tau_v) (v^c_(y i) - v_(y i)),
) $ <eq:autopilot>

因为无人机存在机动性的问题，需要给无人机添加动力学的约束条件，如公式(6)所示。
$ cases(
  v_i in (v_min, v_max),
  dot(v_i) in (a_min, a_max),
  dot(theta_i) in (omega_min, omega_max),
) $ <constraints>

=== 编队控制算法

通过文献中的一致性原理，本节分别构建了无人机在横向与纵向上的控制策略。在此基础上，通过将无人机的物理机动限制与控制律相融合，最终推导出了本文所提的协同控制算法。

在多无人机系统中，信息交互拓扑结构常用图论（Graph Theory）来描述。假设无人机集群由 $n$ 架无人机组成，其通信拓扑可用无向图或有向图 $G = (V, E)$ 表示。引入邻接矩阵 $A = [a_(i j)] in bb(R)^(n times n)$ 以及拉普拉斯矩阵 $L = [l_(i j)] in bb(R)^(n times n)$。邻接矩阵中的元素 $a_{i j}$ 用于表征通信链路：当无人机 $i$ 能够获取无人机 $j$ 的信息时，$a_{i j}=1$，否则 $a_{i j}=0$。

拉普拉斯矩阵定义为 $L = D - A$，其中度矩阵 $D = "diag"(d_1, d_2, dots.h, d_n)$，且节点入度 $d_i = sum_{j=1}^n a_{i j}$。由文献[]可以证明拉普拉斯矩阵不仅包含了系统的图论拓扑特征，其特征值的分布特性在控制理论上严格限制并保证了一致性算法的收敛性与集群飞行的稳定性（当且仅当通信拓扑图包含生成树时，控制系统能够实现无偏的渐近收敛）。

在 Tao 等@tao2023 的工作中，无人机水平运动被建模为双积分器系统：$dot(bold(p))_i = bold(v)_i$，$dot(bold(v))_i = bold(u)_i$，其中 $bold(u)_i = (u_(x i), u_(y i))^T$ 为待设计的虚拟加速度控制输入。基础一致性协议沿 $X_g$ 和 $Y_g$ 轴分别构造如下：

$
u_(x i)(t) = - sum_(j=1)^n a_(i j) [(x_i(t) - x_j(t) - r_(i j)^x) + gamma (v_(x i)(t) - v_(x j)(t))]
$

$
u_(y i)(t) = - sum_(j=1)^n a_(i j) [(y_i(t) - y_j(t) - r_(i j)^y) + gamma (v_(y i)(t) - v_(y j)(t))]
$

式中 $gamma > 0$ 为位置误差与速度误差的耦合增益，$r_(i j)^x = -r_(j i)^x$、$r_(i j)^y = -r_(j i)^y$ 为前文定义的期望相对位移。该协议驱动每架无人机沿两轴向邻机的期望位置靠拢，同时使其速度趋近于邻居集合的加权平均。

然而，上述基础形式隐含了两个理想假设——通信无延迟且拓扑结构固定不变。在实际飞行场景中，机间数据链存在不可忽略的传输延迟；此外受信道质量、机间距离及障碍遮挡等因素影响，通信链路可能随时中断或恢复。为弥补这些不足，Tao 等@tao2023 提出了同时处理非对称通信时延与切换拓扑的改进一致性控制律。

记 $tau_(i i)(t)$ 为无人机 $i$ 自身状态感知与解算的自延迟，$tau_(i j)(t)$ 为 $j$ 到 $i$ 的机间通信延迟，二者满足有界性条件：$0 <= tau_m(t) <= h_m$ 且 $dot(tau)_m(t) <= d_m < 1$。令 $N_i(t)$ 为 $t$ 时刻无人机 $i$ 的有效邻居集，$a_(i j)(t)$ 为对应的时变邻接权重。则 $X_g$ 与 $Y_g$ 方向上的改进一致性控制输入为：

$
u_(x i)(t) = sum_(V_j in N_i(t)) a_(i j)(t) thin [
  k_1 (x_j(t - tau_(i j)) - x_i(t - tau_(i i)) - r_(j i)^x) \
  + k_2 (v_(x j)(t - tau_(i j)) - v_(x i)(t - tau_(i i)))
] + dot(v)_x^"*" - k_3 (v_(x i)(t) - v_x^"*")
$ <improved-consensus-x>

$
u_(y i)(t) = sum_(V_j in N_i(t)) a_(i j)(t) thin [
  k_1 (y_j(t - tau_(i j)) - y_i(t - tau_(i i)) - r_(j i)^y) \
  + k_2 (v_(y j)(t - tau_(i j)) - v_(y i)(t - tau_(i i)))
] + dot(v)_y^"*" - k_3 (v_(y i)(t) - v_y^"*")
$ <improved-consensus-y>

式中各参数的物理含义：$k_1 > 0$ 为位置偏差增益，控制编队几何构型的恢复强度；$k_2 > 0$ 为速度一致性增益，调节邻机间的速度同步速率；$k_3 = k_1 k_2$ 为速度跟踪增益，使集群整体速度向期望巡航速度 $(v_x^"*", v_y^"*")$ 对齐；$dot(v)_x^"*"$、$dot(v)_y^"*"$ 为参考加速度前馈项（编队匀速巡航时取零）；$r_(j i)^x$、$r_(j i)^y$ 采用反对称形式以匹配控制量的增量方向。

与基础协议相比，改进算法在三个层面进行增强：(1) 显式引入非对称通信时延 $tau_(i j)$ 与 $tau_(i i)$，使各无人机基于实际通信时序中的延迟状态而非理想同步状态进行决策；(2) 将时不变邻接权重替换为时变形式 $a_(i j)(t)$，使控制律自适应通信链路的动态通断，只要切换序列满足联合连通条件即可保证收敛@tao2023；(3) 增设参考速度前馈通道，赋予编队整体的机动跟随能力，满足路径规划层下达的变速巡航需求。

控制输入 $bold(u)_i$ 到自驾仪速度指令的映射由一阶惯性环节的逆模型给出：

$
cases(
  v_(x i)^c = v_(x i) + tau_v u_(x i),
  v_(y i)^c = v_(y i) + tau_v u_(y i),
) $ <control-to-autopilot>

该变换补偿了自驾仪的动态响应滞后，使无人机在稳态下紧密跟踪一致性控制量的期望加速度。

*飞行约束的最小调整策略。* @eqt:control-to-autopilot 求得的指令未考虑式@eqt:constraints 所列的机动能力边界。Tao 等@tao2023 设计了分两级递进的最小调整映射，在保证指令可行性的前提下尽量保持原始控制方向。

 由 $bold(u)_i$ 计算合加速度幅值 $alpha_i = sqrt(u_(x i)^2 + u_(y i)^2)$，以采样周期 $Delta t$ 预估下一步速度 $v_i(t+Delta t) = v_i(t) + alpha_i Delta t$。若预估值越界，则收紧加速度有效区间 $[a_(min,i)^"new", a_(max,i)^"new"]$。当 $alpha_i$ 超出上界时，按比例压缩两轴分量以保持合加速度方向不变：

$
u'_(x i) = frac(a_(max,i)^"new", alpha_i) u_(x i), quad
u'_(y i) = frac(a_(max,i)^"new", alpha_i) u_(y i)
$

由缩放后的加速度预估下一时刻航向：
$
theta_i(t+Delta t) = arctan(frac(v_(y i) + u'_(y i) Delta t, v_(x i) + u'_(x i) Delta t))
$

根据最大航向角速率 $omega_max$、$omega_min$ 确定航向可达区间：

$
theta_(min,i) = theta_i(t) + omega_min Delta t, quad
theta_(max,i) = theta_i(t) + omega_max Delta t
$

若预测航向偏离该区间，则在保持合加速度幅值不变的前提下，将航向修正至最近边界值 $theta_("bound")$，联立求解加速度最终可行解 $(u''_(x i), u''_(y i))$。经两级修正后的控制量通过式@eqt:control-to-autopilot 映射为满足全部机动约束的自驾仪指令。

*离散时间实现。* 控制器以周期 $Delta t$ 迭代运行：每步采集邻机延迟状态，按式@eqt:improved-consensus-x、式@eqt:improved-consensus-y 计算虚拟加速度，经上述两级约束映射后输出自驾仪速度指令，驱动无人机状态更新。该架构在二维平面上为集群提供了兼顾拓扑鲁棒性与飞行安全性的编队控制能力，为后续全局路径规划奠定了载体平台层的协同基础@tao2023。

下面给出编队控制器的完整伪代码，主循环见#algorithm-ref(<alg:formation-control>)，约束调整子过程见#algorithm-ref(<alg:constraint-adj>)。

#pagebreak()
#algorithm(
  title: [二维编队一致性控制主循环],
  input: [
    无人机数量 $n$，仿真时长 $T$，采样周期 $Delta t$，
    期望相对位置矩阵 $bold(R)_x$, $bold(R)_y$，
    增益 $k_1, k_2, k_3 = k_1 k_2$，时间常数 $tau_v$，
    机动约束 $v_min, v_max, a_min, a_max, omega_min, omega_max$，
    时延上界 $h_m$，切换拓扑序列 $G(t)$。
  ],
  output: [所有无人机轨迹。],
  [*for* $i arrow 1$ *to* $n$ *do*],
  indent(
    [$(x_i, y_i) arrow$ 初始位置，$(v_(x i), v_(y i)) arrow$ 初始速度，$theta_i arrow$ 初始航向],
    [$v_(x i)^c arrow v_(x i)$, $v_(y i)^c arrow v_(y i)$],
  ),
  [$t arrow 0$],
  [*while* $t < T$ *do*],
  indent(
    [*for* $i arrow 1$ *to* $n$ *do*],
    indent(
      [$N_i(t) arrow$ 从 $G(t)$ 获取邻居集],
      [读取延迟状态：$x_j(t - tau_(i j))$, $y_j(t - tau_(i j))$, $v_(x j)(t - tau_(i j))$, $v_(y j)(t - tau_(i j))$, $forall V_j in N_i(t)$],
      [$u_(x i) arrow$ 按式@eqt:improved-consensus-x 计算],
      [$u_(y i) arrow$ 按式@eqt:improved-consensus-y 计算],
      [调用 ConstraintAdjust（见算法@alg:constraint-adj）修正 $(u_(x i), u_(y i))$],
      [$v_(x i)^c arrow v_(x i) + tau_v u_(x i)$, $quad v_(y i)^c arrow v_(y i) + tau_v u_(y i)$],
      [$v_(x i) arrow v_(x i) + frac(Delta t, tau_v)(v_(x i)^c - v_(x i))$, $quad v_(y i) arrow v_(y i) + frac(Delta t, tau_v)(v_(y i)^c - v_(y i))$],
      [$x_i arrow x_i + v_(x i) Delta t$, $quad y_i arrow y_i + v_(y i) Delta t$],
      [$theta_i arrow arctan(v_(y i) / v_(x i))$, $quad v_i arrow sqrt(v_(x i)^2 + v_(y i)^2)$],
    ),
    [$t arrow t + Delta t$，按序列更新 $G(t)$],
  ),
  [*return* ${(x_i(t), y_i(t)) | t in [0, T], i in 1..n}$],
) <alg:formation-control>

#pagebreak()
#algorithm(
  title: [约束最小调整子过程 ConstraintAdjust],
  input: [原始控制量 $u_(x i), u_(y i)$，当前速度 $v_i$、航向 $theta_i$，
    加速度边界 $a_max, v_max, v_min$，航向速率边界 $omega_min, omega_max$，采样周期 $Delta t$。],
  output: [约束修正后的控制量 $(u'_(x i), u'_(y i))$。],
  [$alpha_i arrow sqrt(u_(x i)^2 + u_(y i)^2)$],
  [$v_i^"pred" arrow v_i + alpha_i Delta t$],
  [*if* $v_i^"pred" > v_max$ *or* $v_i^"pred" < v_min$ *then*],
  indent(
    [$a_(max)^"new" arrow min(a_max, (v_max - v_i) / Delta t)$],
    [*if* $alpha_i > a_(max)^"new"$ *then* $(u_(x i), u_(y i)) arrow frac(a_(max)^"new", alpha_i) (u_(x i), u_(y i))$],
  ),
  [$theta_i^"pred" arrow arctan((v_(y i) + u_(y i) Delta t) / (v_(x i) + u_(x i) Delta t))$],
  [$theta_min arrow theta_i + omega_min Delta t$, $quad theta_max arrow theta_i + omega_max Delta t$],
  [*if* $theta_i^"pred" > theta_max$ *or* $theta_i^"pred" < theta_min$ *then*],
  indent(
    [将 $theta_i^"pred"$ 修正至最近边界 $theta_("bound")$],
    [联立求解：$frac(v_(y i) + u_(y i) Delta t, v_(x i) + u_(x i) Delta t) = tan theta_("bound")$ 且 $u_(x i)^2 + u_(y i)^2 = alpha_i^2$],
  ),
  [*return* $(u_(x i), u_(y i))$],
) <alg:constraint-adj>


 == 全局路径规划算法设计与实现
 === 适应度函数设计

全局路径规划阶段的优化目标并非单一的路径最短，而是需要同时满足飞行效率、安全避障、轨迹平滑、通道可通过性以及多机编队保持等多重要求。为便于不同智能优化算法在统一评价标准下进行比较，本文将路径规划问题转化为加权多目标优化问题，并构建统一的适应度函数。设某一候选解 $bold(X)$ 解码后得到的离散参考路径为

$
cal(Q) = {bold(q)_1, bold(q)_2, dots, bold(q)_M}
$ <fitness-path-points>

其中 $bold(q)_k in bb(R)^2$ 表示路径上的第 $k$ 个采样点，$M$ 为路径离散点数量。对于编队飞行场景，领航者沿 $cal(Q)$ 运动，其余无人机按照预设编队偏移或局部避障修正得到对应位置 $bold(p)_(i,k)$。因此，适应度函数不仅评价领航者参考路径本身，也评价该路径对集群队形、安全间距和通信链路的影响。

*总体评价函数。* 本文采用加权求和方式将不同优化目标融合为单一代价函数：

$
J(bold(X)) = alpha L + beta S + gamma R + mu W + lambda F
$ <fitness-total-cost>

式中，$L$ 为路径长度代价，$S$ 为路径平滑代价，$R$ 为综合风险代价，$W$ 为通道宽度约束惩罚，$F$ 为编队保持代价；$alpha$、$beta$、$gamma$、$mu$ 和 $lambda$ 分别为各分量权重。由于各分量具有不同量纲和数值范围，实际计算中可先对各项进行归一化处理，再代入加权和，即写作 $J = alpha L_"norm" + beta S_"norm" + gamma R_"norm" + mu W_"norm" + lambda F_"norm"$。当算法采用“适应度越大越优”的评价约定时，将代价函数转换为

$
"Fitness"(bold(X)) = frac(1, J(bold(X)) + epsilon)
$ <fitness-transform>

其中 $epsilon$ 为防止分母为零的极小正数。本文后续各群体智能算法在选择、排序和最优解记录时均以最小化 $J$ 为基本准则；若算法内部需要最大化适应度，则使用式@eqt:fitness-transform 进行等价转换。

*路径长度代价。* 路径长度反映无人机完成任务所需的航程与能耗，是路径规划中最基本的效率指标。对于离散路径 $cal(Q)$，长度代价定义为相邻采样点之间欧氏距离之和：

$
L = sum_(k=1)^(M - 1) norm(bold(q)_(k+1) - bold(q)_k)
$ <fitness-length>

$L$ 越小，表示路径越短、飞行时间和能耗越低。但在多障碍环境中，过度追求最短路径容易产生贴近障碍物或穿越狭窄通道的风险，因此长度项需与风险项和宽度约束项共同作用。

*路径平滑代价。* 路径平滑性用于衡量相邻航段方向变化的剧烈程度。设第 $k$ 个内部采样点处的转角为 $theta_k$，则

$
theta_k = arccos frac((bold(q)_k - bold(q)_(k-1)) dot (bold(q)_(k+1) - bold(q)_k), norm(bold(q)_k - bold(q)_(k-1)) dot norm(bold(q)_(k+1) - bold(q)_k))
$ <fitness-turn-angle>

平滑代价采用转角平方和表示：

$
S = sum_(k=2)^(M - 1) theta_k^2
$ <fitness-smoothness>

该项对大角度转弯具有更强惩罚作用，可抑制折线路径中的急转点，使生成路径更符合无人机速度、加速度和航向角速度约束。

*综合风险代价。* 无人机集群在复杂环境下的飞行风险主要来源于静态障碍物、机间碰撞和通信链路失效等因素。因此，综合风险项定义为

$
R = R_s + R_c + R_f
$ <fitness-risk-total>

其中 $R_s$ 表示静态障碍风险，$R_c$ 表示集群内部碰撞风险，$R_f$ 表示编队通信风险。

静态障碍风险由路径点到最近静态障碍物的距离决定。设 $d_s(bold(q)_k)$ 为采样点 $bold(q)_k$ 到最近静态障碍物边界的距离，$r_("eq")$ 为安全等效半径，$sigma_s$ 为风险衰减尺度，则

$
R_s = sum_(k=1)^M exp(-frac((d_s(bold(q)_k) - r_("eq"))^2, sigma_s^2))
$ <fitness-static-risk>

当路径点靠近障碍物或安全间隙不足时，该项迅速增大；当路径远离障碍物时，风险随距离指数衰减。

集群内部碰撞风险用于约束任意两架无人机之间的安全距离。设 $d_(i j)(t_k) = norm(bold(p)_(i,k) - bold(p)_(j,k))$，$d_("safe")$ 为最小安全距离，则

$
R_c = sum_(k=1)^M sum_(i < j) phi(d_(i j)(t_k))
$ <fitness-collision-risk>

其中

$
phi(d) = cases(
  (1 - d / d_("safe"))^2, d < d_("safe"),
  0, d >= d_("safe")
)
$ <fitness-collision-penalty>

当机间距离小于安全阈值时，风险随距离缩短呈平方增长；当距离满足安全要求时，该项为零。

编队通信风险用于约束通信拓扑中相邻节点的链路可靠性。设通信边集合为 $cal(E)$，通信半径为 $d_("comm")$，则

$
R_f = sum_(k=1)^M sum_((i,j) in cal(E)) max(0, d_(i j)(t_k) - d_("comm"))^2
$ <fitness-communication-risk>

当通信边上的无人机距离超过通信半径时，该项产生惩罚，从而促使路径规划结果保留必要的链路连通性。

*通道宽度约束惩罚。* 对于多机编队而言，路径点附近是否具有足够通行宽度直接影响编队能否安全通过。设 $B_k$ 为路径点 $bold(q)_k$ 处可通行通道宽度，$B_("req", k)$ 为该处最小需求宽度，则宽度约束惩罚定义为

$
W = sum_(k=1)^M max(0, B_("req", k) - B_k)^2
$ <fitness-width>

若环境允许编队在狭窄区域内进行弹性压缩，可将需求宽度写为

$
B_("req", k) = B_("form")^("min") + eta_b Delta B_k
$ <fitness-required-width>

其中 $B_("form")^("min")$ 表示编队安全通过所需的最小包络宽度，$Delta B_k$ 表示由通道收缩引起的额外编队压缩需求，$eta_b$ 为调节系数。该项能够避免全局路径选择对单机可行但对编队不可行的狭窄通道。

*编队保持代价。* 编队保持代价用于评价无人机集群在沿路径运动过程中对期望队形的维持能力。若采用理想偏移描述队形，设第 $i$ 架无人机在路径点 $k$ 处相对领航者的实际偏移为 $bold(delta)_(i,k)$，期望偏移为 $bold(delta)_i^*$，无人机数量为 $N_u$，则

$
F = sum_(k=1)^M sum_(i=1)^(N_u) norm(bold(delta)_(i,k) - bold(delta)_i^*)^2
$ <fitness-formation-offset>

对于以邻接关系描述的编队，也可采用邻近距离保持形式：

$
F = sum_(k=1)^M sum_((i,j) in cal(E)) (norm(bold(p)_(i,k) - bold(p)_(j,k)) - d_(i j)^*)^2
$ <fitness-formation-distance>

其中 $d_(i j)^*$ 为通信边或编队边对应的理想机间距离。该项越小，说明队形畸变越弱，编队稳定性越好。

综上所述，本文适应度函数通过 $L$、$S$、$R$、$W$ 和 $F$ 五类指标将路径质量、安全性和编队协同约束统一到同一优化框架中。长度项和横向宽度项约束路径的效率与可通行性，平滑项保证轨迹满足无人机机动特性，风险项保证避障与通信安全，编队保持项保证多机协同结构不被破坏。该设计为后续 PSO、ACO、GA、MSA、AHA、AL-SHADE 及 TALG 等算法提供统一的候选解评价标准。
 
 === 粒子群算法（PSO）设计与实现

粒子群优化算法（Particle Swarm Optimization, PSO）是由 Kennedy 与 Eberhart 于 1995 年提出的一种群体智能优化算法，其基本思想源于鸟群觅食行为的模拟：每个粒子代表解空间中的一个候选解，通过跟踪自身历史最优位置（个体认知）和群体历史最优位置（社会信息）来更新自身的速度与位置，从而在迭代中逐步逼近全局最优解。由于 PSO 具有参数少、收敛速度快、不依赖梯度信息等优点，在连续空间的全局路径规划问题中得到了广泛应用。

*路径编码与搜索空间构造。* 将无人机从起始点 $bold(S) = (x_S, y_S)$ 到目标点 $bold(T) = (x_T, y_T)$ 的飞行路径离散化为 $N$ 个中间航点 $bold(W) = {bold(w)_1, bold(w)_2, dots, bold(w)_N}$，其中 $bold(w)_j = (x_j, y_j) in bb(R)^2$。每个粒子的位置向量编码为 $2N$ 维实数向量：

$
bold(X) = (x_1, y_1, x_2, y_2, dots, x_N, y_N)^T in bb(R)^(2N)
$

完整的飞行路径由起点、全部航点与终点顺序连接而成：$bold(S) -> bold(w)_1 -> bold(w)_2 -> dots -> bold(w)_N -> bold(T)$。

搜索空间的初始化采用基线扰动策略。首先在起点与终点之间按等比例插入生成一组基线航点：

$
bold(w)_j^("base") = (1 - rho_j) bold(S) + rho_j bold(T), quad rho_j = j / (N + 1), quad j = 1, 2, dots, N
$

为赋予初始种群多样性，围绕基线航点在带状可行域内进行均匀随机扰动。记步长向量 $bold(Delta) = (bold(T) - bold(S)) / (N + 1)$，则航点 $j$ 的搜索边界为：

$
bold(w)_j^(min) = max(bold(w)_j^("base") - bold(b), bold(S)_("min")), quad
bold(w)_j^(max) = min(bold(w)_j^("base") + bold(b), bold(T)_("max"))
$

其中带状半宽 $bold(b) = (b_x, b_y)$ 取为 $b = max(3 sigma_0, 1.2 thin bar(delta), 80)$，$sigma_0$ 为初始散布标准差，$bar(delta)$ 为相邻基线点之间的平均间距。该策略既利用了起点-终点连线提供的先验引导，又为粒子群保留了充分的探索自由度。

*适应度评价。* PSO 的个体优劣判定沿用上一节建立的统一适应度函数。每个粒子位置 $bold(X)_i$ 解码为完整航点序列后，依次计算路径长度、平滑性、综合风险、通道宽度惩罚和编队保持代价，并以加权代价 $J(bold(X)_i)$ 作为粒子排序、个体最优和全局最优更新的依据。

*粒子更新机制。* 每个粒子 $i$ 维护当前位置 $bold(X)_i$、速度 $bold(V)_i$ 和个体历史最优 $bold(P)_i^("best")$，全局最优记为 $bold(G)^("best")$。在第 $t$ 次迭代中，速度和位置按标准 PSO 公式更新：

$
bold(V)_i^(t+1) = omega bold(V)_i^(t) + c_1 r_1 (bold(P)_i^("best") - bold(X)_i^(t)) + c_2 r_2 (bold(G)^("best") - bold(X)_i^(t))
$

$
bold(X)_i^(t+1) = bold(X)_i^(t) + bold(V)_i^(t+1)
$

其中惯性权重 $omega = 0.6$ 平衡全局探索与局部开发，个体学习因子 $c_1 = 1.6$ 和社会学习因子 $c_2 = 1.4$ 分别控制粒子向个体最优和全局最优靠拢的强度，$r_1, r_2$ 为 $[0, 1]$ 区间上独立采样的均匀随机数。

粒子更新后将其位置裁剪至航点搜索边界 $[bold(w)_j^(min), bold(w)_j^(max)]$ 内。若更新后的适应度优于个体历史最优，则更新 $bold(P)_i^("best")$；若进一步优于全局最优，则同步更新 $bold(G)^("best")$，从而逐步引导种群向全局最优路径收敛。

算法终止条件为达到预设的最大迭代次数 $T_("max")$。典型参数配置为种群规模 $P = 500$，最大迭代次数 $T_("max") = 500$，航点数量 $N = 20$，以在优化精度与计算开销之间取得平衡。通过上述设计，PSO 全局规划器能够输出一条兼顾长度、平滑性、安全性和编队兼容性的参考路径，为后续局部避障与编队控制提供空间引导。

#algorithm(
  title: [PSO 全局路径规划算法],
  input: [
    起始点 $bold(S)$，目标点 $bold(T)$，障碍物集合 $cal(O)$，
    航点数 $N$，种群规模 $P$，最大迭代次数 $T_("max")$，
    惯性权重 $omega$，学习因子 $c_1, c_2$。
  ],
  output: [全局最优航点序列 $bold(W)^"*" = {bold(w)_1^"*", dots, bold(w)_N^"*"}$。],
  [计算基线航点 $bold(W)^("base")$ 及搜索边界 $bold(W)^(min), bold(W)^(max)$],
  [*for* $i in 1..P$ *do*],
  indent(
    [$bold(X)_i arrow$ 在 $[bold(W)^(min), bold(W)^(max)]$ 内随机初始化],
    [$bold(V)_i arrow 0$, $quad bold(P)_i^("best") arrow bold(X)_i$],
  ),
  [$bold(G)^("best") arrow$ 初始种群中适应度最低的粒子],
  [*for* $t in 1..T_("max")$ *do*],
  indent(
    [*for* $i in 1..P$ *do*],
    indent(
      [$r_1, r_2 arrow$ 采样自 $cal(U)(0, 1)$],
      [$bold(V)_i arrow omega bold(V)_i + c_1 r_1 (bold(P)_i^("best") - bold(X)_i) + c_2 r_2 (bold(G)^("best") - bold(X)_i)$],
      [$bold(X)_i arrow bold(X)_i + bold(V)_i$，裁剪至 $[bold(W)^(min), bold(W)^(max)]$],
      [$J_i arrow$ 按式 $J = alpha L_"norm" + beta S_"norm" + gamma R_"norm" + mu W_"norm" + lambda_f F_"norm"$ 计算适应度],
      [*if* $J_i < J(bold(P)_i^("best"))$ *then* $bold(P)_i^("best") arrow bold(X)_i$],
      [*if* $J_i < J(bold(G)^("best"))$ *then* $bold(G)^("best") arrow bold(X)_i$],
    ),
  ),
  [*return* $bold(W)^"*" arrow bold(G)^("best")$],
) <alg:pso-global>

 === 蚁群算法（ACO）设计与实现

蚁群算法（Ant Colony Optimization, ACO）最初由 Dorigo 等受蚂蚁觅食行为启发而提出，其经典形式通过离散信息素矩阵求解组合优化问题（如旅行商问题）。然而无人机路径规划属于连续空间优化，航点坐标取值于二维实数域，无法直接套用离散网格上的信息素累积机制。为此，本节采用 Socha 与 Dorigo 提出的面向连续域的扩展蚁群算法（$"ACO"_(bb(R))$），将信息素模型由离散状态转移概率转化为基于解档案的高斯核密度估计，从而实现对连续路径空间的高效搜索。

*解档案与信息素模型。* ACOR 的核心是由 $K$ 个历史最优解构成的有序档案 $cal(A) = {bold(s)_1, bold(s)_2, dots, bold(s)_K}$，其中 $K$ 称为档案容量。档案中的解按适应度升序排列——$bold(s)_1$ 为全局最优，$bold(s)_K$ 为档案中最差。每个解 $bold(s)_l$ 编码了 $N$ 个航点的二维坐标，即 $bold(s)_l in bb(R)^(2N)$。

不同于离散 ACO 在每段路径上维护信息素浓度，ACOR 将信息素模型推广为档案加权的多维高斯核概率密度函数：

$
G(bold(X)) = sum_(l=1)^K omega_l cal(N)(bold(X) | bold(s)_l, bold(Sigma)_l)
$

其中高斯核的权重 $omega_l$ 按解的排序位置分配，排名越靠前的解被赋予越高的采样概率：

$
omega_l = frac(1, q K sqrt(2 pi)) exp(-frac((l - 1)^2, 2 (q K)^2)), quad l = 1, 2, dots, K
$

式中 $q in (0, 1)$ 为档案集中度参数，$q$ 值越小则概率分布越集中于排名靠前的解，搜索偏向精化（exploitation）；$q$ 值越大则选择压力越均匀，搜索偏向探索（exploration）。权重经归一化后构成概率向量 $bold(p) = (p_1, p_2, dots, p_K)^T$，其中 $p_l = omega_l / sum_(m=1)^K omega_m$。

*核宽度的自适应计算。* 每只蚂蚁在选择引导解 $bold(s)_("guide")$（即高斯核中心）后，需要确定各维度的采样标准差以构造新解。第 $j$ 个航点坐标分量对应的标准差 $sigma_j$ 由档案中所有解相对于引导解的离散程度自适应确定：

$
sigma_j = xi dot frac(1, K - 1) sum_(l=1)^K |bold(s)_(l, j) - bold(s)_("guide", j)| + epsilon, quad j = 1, 2, dots, 2N
$

其中 $xi in (0, 1)$ 为蒸发率（典型值 $xi = 0.85$），其作用类似于离散 ACO 中的信息素挥发系数——$xi$ 越大，核越窄，搜索越集中；$xi$ 越小，核越宽，探索越分散。$epsilon = 10^(-4)$ 为防止零方差退化的小量正数，确保搜索空间始终具有最小发散度。

该自适应核宽度机制是 ACOR 区别于 PSO 的关键特征：PSO 各维度的探索步长依赖于速度惯性记忆和随机扰动，而 ACOR 的步长由当前档案解集的分布宽度动态决定——当档案解趋于一致时，核自动收缩以精细搜索；当档案解分散时，核自动展宽以防止早熟收敛。

*蚂蚁采样与解构造。* 每只蚂蚁按以下两阶段生成新解：

（1）*引导选择*：按概率向量 $bold(p)$ 在档案中随机选取一个引导解 $bold(s)_("guide")$。

（2）*高斯采样*：以 $bold(s)_("guide")$ 为均值、$bold(sigma) = (sigma_1, dots, sigma_(2N))^T$ 为标准差，对每个坐标分量进行独立高斯采样：

$
bold(X)^("new") = bold(s)_("guide") + cal(N)(bold(0), "diag"(bold(sigma)^2))
$

*档案更新与收敛。* 所有蚂蚁生成的新解经适应度评估后并入档案，形成临时集合 $cal(A) union cal(S)^("new")$，随后按适应度排序并截断至容量 $K$——仅保留最佳的 $K$ 个解。这一"精英保留"策略保证了档案质量随迭代单调提升。算法终止条件为达到最大迭代次数 $T_("max")$，最终返回档案最优解 $bold(s)_1$ 作为全局路径航点序列。

典型参数配置为：档案容量 $K = 20$，蚂蚁数量 $M = 500$，最大迭代次数 $T_("max") = 500$，航点数量 $N = 20$，集中度参数 $q = 0.1$，蒸发率 $xi = 0.85$。初始档案通过基线插值叠加高斯噪声生成 $2K$ 个候选解后筛选最佳 $K$ 个构成。适应度函数沿用五分量加权模型 $J = alpha L_"norm" + beta S_"norm" + gamma R_"norm" + mu W_"norm" + lambda_f F_"norm"$，各项定义与 PSO 中一致，便于算法之间的公平对比。

#algorithm(
  title: [ACO 全局路径规划算法],
  input: [
    起始点 $bold(S)$，目标点 $bold(T)$，障碍物集合 $cal(O)$，
    航点数 $N$，档案容量 $K$，蚂蚁数量 $M$，最大迭代次数 $T_("max")$，
    集中度参数 $q$，蒸发率 $xi$。
  ],
  output: [全局最优航点序列 $bold(W)^"*" = {bold(w)_1^"*", dots, bold(w)_N^"*"}$。],
  [$cal(A) arrow$ 生成 $2K$ 个基线扰动解，评估后保留最佳 $K$ 个],
  [计算高斯核权重 $omega_l$ 与选择概率 $p_l$, $l = 1, dots, K$],
  [*for* $t in 1..T_("max")$ *do*],
  indent(
    [$cal(S)^("new") arrow emptyset$],
    [*for* $m in 1..M$ *do*],
    indent(
      [以概率 $p_l$ 从 $cal(A)$ 选取引导解 $bold(s)_("guide")$],
      [$sigma_j arrow xi dot "mean"_(l) |bold(s)_(l,j) - bold(s)_("guide",j)| + epsilon$, $forall j in 1..2N$],
      [$bold(X)^("new") arrow bold(s)_("guide") + cal(N)(bold(0), "diag"(bold(sigma)^2))$],
      [评估 $J(bold(X)^("new"))$，将 $(J(bold(X)^("new")), bold(X)^("new"))$ 加入 $cal(S)^("new")$],
    ),
    [$cal(A) arrow cal(A) union cal(S)^("new")$，按 $J$ 排序并截断至前 $K$ 个],
  ),
  [*return* $bold(W)^"*" arrow cal(A)$ 中适应度最优的解],
) <alg:aco-global>



 === 遗传算法（GA）设计与实现

遗传算法（Genetic Algorithm, GA）是由 Holland 于 20 世纪 70 年代提出的一种模拟自然选择与遗传进化机制的随机搜索算法。其核心思想是将候选解编码为"染色体"，通过选择、交叉和变异三个基本遗传算子，在代际迭代中实现种群适应度的持续提升。对于无人机二维路径规划这一连续优化问题，本节采用实数编码遗传算法（Real-Coded GA），直接在实向量空间上操作航点坐标，避免了二进制编码带来的精度损失与维度膨胀。

*染色体编码与种群初始化。* 每条染色体编码 $N$ 个航点的二维坐标，即 $bold(C) = (x_1, y_1, x_2, y_2, dots, x_N, y_N)^T in bb(R)^(2N)$。初始种群采用与 PSO 一致的基线扰动策略：在起点-终点连线上等比例生成基线航点，围绕基线在带状搜索域内均匀随机采样，生成规模为 $P$ 的初始种群 $cal(P)_0$。所有个体的适应度由五分量加权适应度函数 $J$ 评估。

*选择算子——锦标赛选择。* 采用 $k$-锦标赛选择策略（$k = 3$）：从当前种群中随机抽取 $k$ 个个体，选取其中适应度最优者作为父代参与繁殖。该策略具有以下优点：（1）选择压力可通过 $k$ 连续调节，避免了轮盘赌选择中适应度尺度敏感的问题；（2）仅依赖个体间的相对排序而非绝对适应度值，对适应度尺度变换具有不变性；（3）计算复杂度为 $O(P)$，不涉及全局排序。

*交叉算子——算术交叉。* 对于经锦标赛选出的两个父代染色体 $bold(C)_(p_1)$ 和 $bold(C)_(p_2)$，采用整体算术交叉生成子代：

$
bold(C)_("child") = alpha bold(C)_(p_1) + (1 - alpha) bold(C)_(p_2), quad alpha ~ cal(U)(0, 1)
$

该算子可视为在父代连线（高维空间超线段）上随机插值生成子代，其几何意义明确：子代位于两个父代所张成的凸组合空间内。对于 $n$ 维连续优化问题，算术交叉能在保持父代优良基因片段的同时产生位于中间区域的探索解，具有良好的局部搜索能力。

*变异算子——坐标高斯变异。* 以概率 $p_m = 0.1$ 对子代染色体的每个航点坐标分量独立施加高斯扰动：

$
bold(C)_("child")[w] arrow bold(C)_("child")[w] + cal(N)(0, sigma^2 bold(I)_2), quad w = 1, 2, dots, N
$

变异强度 $sigma$ 采用线性退火策略随代数递减：

$
sigma(g) = sigma_0 lr(1 - frac(g, G_"max")), quad sigma_0 = 40
$

其中 $g$ 为当前代数，$G_"max"$ 为最大代数。该退火策略在进化初期赋予较大的变异步长以维持种群多样性、促进全局探索，在进化后期逐步缩减步长以聚焦局部精化，从而在探索与开发之间实现动态平衡。

*精英保留策略。* 为避免交叉和变异操作破坏已发现的最优解，每代将种群按适应度排序后，保留前 $P_"elite" = max(5, 0.1 P)$ 个最优个体直接进入下一代，其余个体通过选择-交叉-变异生成。精英保留保证了历代最优适应度的单调非增性，显著加速收敛。

*算法终止与参数配置。* 算法达到最大进化代数 $G_"max"$ 后终止，输出种群中适应度最优的染色体作为全局路径航点序列。典型参数配置为：种群规模 $P = 500$，最大代数 $G_"max" = 500$，航点数 $N = 20$，初始变异强度 $sigma_0 = 40$，变异概率 $p_m = 0.1$，锦标赛规模 $k = 3$，精英比例 $10%$。适应度函数沿用 $J = alpha L_"norm" + beta S_"norm" + gamma R_"norm" + mu W_"norm" + lambda_f F_"norm"$，权重及分项定义与 PSO 一致，保证三种算法在统一评价框架下的可比性。

#algorithm(
  title: [GA 全局路径规划算法],
  input: [
    起始点 $bold(S)$，目标点 $bold(T)$，障碍物集合 $cal(O)$，
    航点数 $N$，种群规模 $P$，最大代数 $G_"max"$，
    变异概率 $p_m$，初始变异强度 $sigma_0$，锦标赛规模 $k$。
  ],
  output: [全局最优航点序列 $bold(W)^"*" = {bold(w)_1^"*", dots, bold(w)_N^"*"}$。],
  [$cal(P) arrow$ 基线扰动初始化 $P$ 个个体，评估适应度],
  [*for* $g in 1..G_"max"$ *do*],
  indent(
    [按 $J$ 排序 $cal(P)$，设 $cal(P)_("next") arrow$ 精英个体 $cal(P)[1 .. P_"elite"]$],
    [$sigma arrow sigma_0 (1 - g / G_"max")$],
    [*while* $|cal(P)_("next")| < P$ *do*],
    indent(
      [$bold(C)_(p_1) arrow "TournamentSelect"(cal(P), k)$, $bold(C)_(p_2) arrow "TournamentSelect"(cal(P), k)$],
      [$alpha arrow cal(U)(0, 1)$, $quad bold(C)_("child") arrow alpha bold(C)_(p_1) + (1 - alpha) bold(C)_(p_2)$],
      [*for* $w in 1..N$ *do*],
      indent(
        [*if* $cal(U)(0, 1) < p_m$ *then* $bold(C)_("child")[w] arrow bold(C)_("child")[w] + cal(N)(0, sigma^2 bold(I)_2)$],
      ),
      [评估 $J(bold(C)_("child"))$，加入 $cal(P)_("next")$],
    ),
    [$cal(P) arrow cal(P)_("next")$],
  ),
  [*return* $cal(P)[1]$ 的航点编码],
) <alg:ga-global>


  === 螳螂搜索算法（MSA）设计与实现

螳螂搜索算法（Mantis Search Algorithm, MSA）是 Abdel-Basset 等人于2023年提出的一种新型群体智能优化算法@MSA2023。该算法的核心思想来源于螳螂的捕食过程和性食同类行为：螳螂在自然界中既可以主动追踪猎物，也可以伪装成环境的一部分等待猎物进入攻击范围；当猎物距离合适时，螳螂会通过前足快速击打完成捕获；在交配过程中，雌性螳螂还可能吞食雄性个体。MSA将上述生物行为抽象为三个互相配合的优化算子，即寻找猎物阶段、攻击猎物阶段和性食同类阶段。寻找猎物阶段负责扩大搜索范围并发现潜在优良区域，攻击猎物阶段负责围绕当前最优解进行局部开发，性食同类阶段则通过个体间的信息交换和扰动维持种群多样性。

在无人机全局路径规划问题中，一个螳螂个体对应一条候选路径的编码向量。算法运行时，首先在路径搜索空间内生成若干候选路径，然后不断依据适应度函数评价路径长度、平滑性、风险代价和可行性。适应度较小的个体被视为更接近“猎物”的优良解，当前种群中的最优个体记为$bold(x)^*$。随着迭代推进，MSA通过探索、开发和性食同类三类行为的交替执行，使种群既能够在早期充分搜索复杂障碍物环境中的可行通道，又能够在后期围绕较优路径进行精细调整。

==== 算法数学模型与流程

*种群初始化。* 设种群规模为$N$，搜索空间维度为$D$，最大迭代次数为$T$。第$i$个螳螂个体在第$t$次迭代时的位置向量表示为：

$ bold(x)_i^t = [x_(i,1)^t, x_(i,2)^t, dots.h, x_(i,D)^t] $ <eq:msa-position>

在初始化阶段，算法根据变量上下界均匀随机生成初始种群：

$ bold(x)_i^0 = bold(x)_L + bold(r) dot (bold(x)_U - bold(x)_L) $ <eq:msa-init>

式中，$bold(x)_L$和$bold(x)_U$分别表示搜索空间的下界和上界，$bold(r)$为$[0, 1]$区间内的均匀随机向量，$dot$表示逐元素乘积。完成初始化后，计算所有个体的适应度并确定当前全局最优解$bold(x)^*$。同时，算法建立容量为$A$的外部存档$"Archive"$，用于保存搜索过程中产生的较优历史解。存档中的个体不仅为伏击行为提供参考位置，也为后续跳出局部最优提供扰动来源。

*寻找猎物阶段。* 寻找猎物阶段对应算法的探索过程，主要模拟两类螳螂捕猎方式：一类是主动移动寻找猎物的追捕者，另一类是保持伪装等待猎物靠近的伏击者。为控制两种行为的切换，MSA引入回收控制因子$F$：

$ F = 1 - (t "mod" (T / P)) / (T / P) $ <eq:msa-recycle>

其中$P$为回收因子，用于划分搜索周期。$F$在每个周期内由1逐渐衰减至0，使算法能够周期性地从较强的全局探索转向较集中的局部搜索，避免搜索过程过早固定在单一模式中。

对于追捕者行为，MSA将Lévy飞行、正态分布扰动和随机方向重组结合起来更新个体位置：

$ bold(x)_i^(t+1) = cases(
  bold(x)_i^t + bold(tau)_1 dot (bold(x)_i^t - bold(x)_a^t) + |tau_2| dot bold(U) dot (bold(x)_a^t - bold(x)_b^t), "if " r_1 <= r_2,
  bold(x)_i^t dot bold(U) + (bold(x)_a^t + bold(r)_3 dot (bold(x)_b^t - bold(x)_c^t)) dot (1 - bold(U)), "otherwise"
) $ <eq:msa-pursuer>

式中，$bold(x)_a^t$、$bold(x)_b^t$和$bold(x)_c^t$为从当前种群中随机选取的不同个体，$bold(tau)_1$为基于Lévy飞行生成的随机向量，$tau_2$为服从标准正态分布的随机数，$bold(U)$为二值掩码向量。$bold(U)$用于决定每个维度保留原个体信息还是采用随机重组信息，其定义为：

$ bold(U)_j = cases(
  0, "if " r_(4,j) < r_(5,j),
  1, "otherwise"
) $ <eq:msa-mask>

追捕者更新式中的第一种情况强调较大尺度的随机游走，可帮助个体跳向新的搜索区域；第二种情况通过三个随机个体的差分关系产生突发方向变化，用于模拟螳螂在追踪猎物时突然改变运动方向的行为。

伏击者行为则利用外部存档中的历史较优解进行引导。设$bold(x)_("ar")^t$为从存档中随机选取的参考解，伏击者的位置更新为：

$ bold(x)_i^(t+1) = cases(
  bold(x)_i^t + alpha dot (bold(x)_("ar")^t - bold(x)_a^t), "if " r_9 <= r_(10),
  bold(x)_("ar")^t + (2 r_7 - 1) dot mu dot (bold(x)_L + bold(r)_8 dot (bold(x)_U - bold(x)_L)), "otherwise"
) $ <eq:msa-ambuscade>

其中：

$ alpha = cos(pi r_6) dot mu, quad mu = 1 - t / T $ <eq:msa-alpha-mu>

$alpha$用于描述螳螂转动头部、扫描周围环境时的方向控制能力，$mu$为距离衰减因子。迭代初期$mu$较大，伏击行为可以在较宽范围内搜索；迭代后期$mu$逐渐减小，个体移动幅度随之降低，从而有利于在优良区域附近进行稳定开发。

*攻击猎物阶段。* 当算法由探索转入开发时，当前全局最优解$bold(x)^*$被视为猎物位置，普通个体则向该位置发起攻击。MSA首先通过Sigmoid函数计算击打速度：

$ v_s = 1 / (1 + e^(l dot rho)) $ <eq:msa-strike-velocity>

式中，$rho$为螳螂击打过程的引力加速率常数，$l$为控制击打幅度的随机参数。$v_s$越接近1，表示个体越倾向于快速靠近当前最优解；$v_s$越接近0，则说明当前攻击条件不足，个体移动更加保守。

击打距离由个体当前位置与猎物位置之间的差值给出：

$ bold(d)_(s i)^t = bold(x)^* - bold(x)_i^t $ <eq:msa-strike-distance>

当击打成功时，个体位置按照如下方式更新：

$ bold(x)_i^(t+1) = (bold(x)_i^t + bold(x)^*) / 2 + v_s dot bold(d)_(s i)^t $ <eq:strike-success>

该式由两部分组成：$(bold(x)_i^t + bold(x)^*) / 2$使个体先移动到自身与最优解之间的中间区域，$v_s dot bold(d)_(s i)^t$进一步根据击打速度向猎物方向推进，因此能够显著增强算法后期的局部收敛能力。

实际捕猎中螳螂可能因判断失误而击打失败。为了模拟这一过程，MSA设置击打失败概率：

$ P_f = a dot (1 - t / T) $ <eq:msa-failure-prob>

其中$a$为初始失败率。$P_f$随迭代次数增加而逐渐减小，表示算法早期更强调探索和方向修正，后期更强调稳定收敛。当击打失败时，个体根据两个随机螳螂之间的差异向量调整方向：

$ bold(x)_i^(t+1) = bold(x)_i^t + r_(12) dot (bold(x)_a^t - bold(x)_b^t) $ <eq:strike-failure>

若个体存在陷入局部最优的风险，则进一步引入振荡扰动机制：

$ bold(x)_i^(t+1) = bold(x)_i^t + e^(2l) dot cos(2 pi l) dot |bold(x)_i^t - bold(x)_("ar")^t| + (2 r_(13) - 1) dot (bold(x)_U - bold(x)_L) $ <eq:oscillation-perturbation>

该更新式利用指数项与余弦项构造非线性振荡扰动，同时叠加搜索空间尺度上的随机偏移，使个体能够从局部较差区域中跳出，重新寻找更具潜力的路径。

*性食同类阶段。* 性食同类阶段用于模拟雌性螳螂吸引雄性、交配并吞食雄性的过程。在算法中，该阶段以概率$P_c$触发，主要作用是促进个体间的信息重组，并为种群注入额外扰动。

首先，雌性个体吸引随机选取的雄性个体$bold(x)_a^t$，其位置更新为：

$ bold(x)_i^(t+1) = bold(x)_i^t + bold(r)_(16) dot (bold(x)_i^t - bold(x)_a^t) $ <eq:msa-attraction>

吸引概率定义为：

$ P_t = r_(17) dot mu $ <eq:msa-attraction-prob>

由于$mu$随迭代递减，性吸引行为在算法早期更容易发生，有助于扩大种群差异；在后期则逐渐减少，以免破坏已经形成的优良解结构。

随后，算法通过均匀交叉模拟交配行为，生成包含双亲信息的新个体：

$ bold(x)_i^(t+1) = bold(x)_i^t dot bold(U) + (bold(x)_a^t + bold(r)_(18) dot (bold(x)_i^t - bold(x)_a^t)) dot (1 - bold(U)) $ <eq:msa-mating>

其中$bold(U)$仍为二值掩码向量，用于决定不同维度的信息来源。该操作使新解既继承当前个体的部分结构，也吸收随机雄性个体的差异信息。

最后，雌性吞食雄性的行为被表示为：

$ bold(x)_i^(t+1) = bold(x)_a^t dot cos(2 pi l) dot mu $ <eq:msa-cannibalism>

其中$cos(2 pi l)$提供方向变化，$mu$控制雄性信息被吸收的比例。该阶段本质上是一种强扰动重组算子，能够在种群收敛过快时重新增加搜索多样性。

*边界处理、选择与终止。* 每次位置更新后，算法都需要将越界维度截断或映射回$[bold(x)_L, bold(x)_U]$范围内，以保证候选路径满足编码约束。随后重新计算个体适应度，若新位置优于旧位置，则接受更新；否则保留原位置。若新个体优于当前全局最优解，则更新$bold(x)^*$。同时，将优秀个体写入外部存档；当存档容量超过$A$时，随机替换或删除其中部分旧解。算法不断重复上述步骤，直到达到最大迭代次数$T$或满足预设精度要求，最终输出全局最优路径编码及其适应度。

综上，MSA的完整优化流程可以概括为：先通过随机初始化形成螳螂种群并建立存档；在每轮迭代中，根据概率参数$p$选择执行寻找猎物或攻击猎物；若进入探索阶段，则由回收控制因子$F$进一步决定采用追捕者行为还是伏击者行为；若进入开发阶段，则根据击打失败概率$P_f$判断执行成功击打、失败修正或局部最优逃逸；之后以概率$P_c$执行性食同类操作，完成个体间的信息交换与扰动；最后进行边界修复、适应度评价、精英保留和存档更新。该流程使MSA能够在全局搜索、局部开发和多样性维持之间形成较为平衡的协同机制。

#capfig(
  image("figures/MSA.png", width: 90%),
  caption: [螳螂搜索算法流程图],
  label: <fig:msa-flow>,
)


/* #pagebreak()
#algorithm(
  title: [螳螂搜索算法（MSA）],
  input: [
    种群规模$N$，最大迭代次数$T$，搜索空间维度$D$，
    下界$bold(x)_L$，上界$bold(x)_U$，存档容量$A$，
    回收因子$P$，引力加速率$rho$，初始失败率$a$，性食同类概率$P_c$。
  ],
  output: [全局最优解$bold(x)^*$及其适应度$f^*$。],
  [*步骤 1：种群与存档初始化*],
  [*for* $i = 1$ *to* $N$ *do*],
  indent(
    [随机初始化个体 $bold(x)_i^0$],
    [计算适应度 $f(bold(x)_i^0)$],
  ),
  [确定当前最优解 $bold(x)^*$ 及最优适应度 $f^*$],
  [初始化存档 $"Archive"$为当前种群中的较优解],
  [],
  [*步骤 2：主迭代循环*],
  [*for* $t = 0$ *to* $T - 1$ *do*],
  indent(
    [计算距离因子 $mu = 1 - t / T$],
    [计算击打失败概率 $P_f = a dot mu$],
    [计算回收控制因子 $F = 1 - (t "mod" (T / P)) / (T / P)$],
    [],
    [*步骤 3：种群更新*],
    [*for* $i = 1$ *to* $N$ *do*],
    indent(
      [*if* $cal(U)(0,1) < 0.5$ *then* — 探索阶段],
      indent(
        [*if* $cal(U)(0,1) < F$ *then*],
        indent(
          [按追踪公式执行追踪行为更新 $bold(x)_i^t$],
        ),
        [*else*],
        indent(
          [按伏击公式执行伏击行为更新 $bold(x)_i^t$],
        ),
      ),
      [*else* — 开发阶段],
      indent(
        [计算击打速度 $v_s = 1 / (1 + e^(l dot rho))$],
        [*if* $cal(U)(0,1) > P_f$ *then*],
        indent(
          [按成功击打公式执行成功击打更新],
        ),
        [*else*],
        indent(
          [*if* $cal(U)(0,1) < 0.5$ *then*],
          indent(
            [执行轨迹修正],
          ),
          [*else*],
          indent(
            [执行局部最优逃逸],
          ),
        ),
      ),
      [],
      [*步骤 4：边界处理与适应度评估*],
      [将 $bold(x)_i^t$ 各维度限制在 $bold(x)_L$ 与 $bold(x)_U$ 之间],
      [计算新适应度 $f_i^"new"$],
      [*if* $f_i^"new" < f(bold(x)_i^t)$ *then* 接受更新],
      [更新存档 $"Archive"$],
      [*if* $f_i^"new" < f^*$ *then* 更新全局最优解],
    ),
    [],
    [*步骤 5：性食同类操作*],
    [*if* $cal(U)(0,1) < P_c$ *then*],
    indent(
      [*for* $i = 1$ *to* $N$ *do*],
      indent(
        [计算吸引概率 $P_t = cal(U)(0,1) dot mu$],
        [*if* $cal(U)(0,1) < P_t$ *then*],
        indent(
          [执行吸引操作],
          [执行交配操作],
          [执行吞噬操作],
        ),
      ),
    ),
  ),
  [],
  [*步骤 6：输出结果*],
  [*return* $bold(x)^*$ 和 $f^*$],
) <alg:msa-global> */

*算法参数分析。* 种群规模$N$决定了解的多样性与搜索覆盖面，通常取50至100之间。最大迭代次数$T$根据问题规模和精度要求确定，通常设置在100至1000之间。存档容量$A$影响伏击行为和逃逸机制中参考信息的质量，较大的$A$保留了更丰富的历史较优解信息。回收因子$P$调控追踪与伏击两种行为的交替频率，标准推荐值为2。引力加速率$rho$控制击打速度Sigmoid曲线的陡峭程度，$rho = 6$意味着击打速度在中后期迅速趋近饱和。性食同类概率$P_c$决定了执行性食同类操作的预期频率，通常设为0.2。距离衰减因子$mu = 1 - t / T$贯穿全算法——从探索阶段的搜索步长缩放，到开发阶段的失败概率控制，再到性食同类阶段的吸引概率和吸收比例，确保了算法从全局探索向局部开发的平滑过渡。

*收敛性分析。* 从搜索机制层面，Lévy飞行步长的重尾分布特性保证了算法的全局可达性——由于Lévy分布的方差无限，个体能够以非零概率到达搜索空间任意位置，确保了算法不会被困于局部最优。正态分布步长提供了在均值附近的精细搜索能力。Sigmoid击打速度的单调递增特性保证了群体在迭代后期向最优解方向逐步逼近。从种群多样性维护层面，性食同类阶段通过交叉重组与吞噬替换机制持续注入新的位置扰动。MSA满足随机搜索算法全局收敛的两个必要条件：精英保留与搜索遍历性，当迭代次数趋于无穷时以概率1收敛于全局最优解。

*计算复杂度分析。* 设种群规模为$N$，问题维度为$D$，最大迭代次数为$T$。MSA各阶段的单个体更新复杂度均为$O(D)$，总体时间复杂度为$O(T N D + T A)$，其中存档维护开销$O(T A)$因$A ≪ N$可忽略不计。渐近时间复杂度$O(T N D)$与主流群体智能算法处于同一量级。空间复杂度主要来源于存储$N$个$D$维个体向量及容量为$A$的存档，为$O(N D + A D)$。



 === AHA算法设计与实现

人工蜂鸟算法（Artificial Hummingbird Algorithm, AHA）是 Zhao 等人于 2022 年提出的一类生物启发式群体智能优化算法@AHA2022。该算法的设计灵感来源于蜂鸟在自然环境中的采蜜行为：一方面，蜂鸟具有较强的空间记忆能力，能够记录不同花朵的位置、花蜜丰度以及距上一次访问的时间间隔；另一方面，蜂鸟具备悬停、侧飞、倒飞等高机动飞行能力，可在单一方向、多个方向或任意方向上快速调整飞行姿态。AHA 将上述生物行为映射为优化过程中的食物源记忆、飞行方向选择和觅食策略切换，从而形成一种兼顾全局探索与局部开发的随机优化框架。

在无人机全局路径规划问题中，蜂鸟个体对应一条候选路径，食物源的花蜜丰度对应路径适应度，采蜜过程对应候选解的迭代更新。与 PSO、ACO、GA 等算法相比，AHA 不依赖惯性权重、信息素挥发率或交叉变异概率等较多外部控制参数，而是通过访问表（Visit Table）记录种群内部的历史访问信息，并利用轴向、对角和全向三类飞行技能调节搜索方向。该机制使算法能够在障碍物约束复杂、可行域非凸且路径编码维度较高的场景下维持较好的搜索多样性。

==== 算法数学模型

*路径编码与搜索空间构造。* 为保证不同全局规划算法之间的对比公平，AHA 采用与前述 PSO、ACO、GA 及 MSA 相同的航点编码方式。设无人机起点为 $bold(S)$，终点为 $bold(T)$，规划路径由 $N$ 个中间航点构成，则第 $i$ 只蜂鸟的位置向量表示为

$
bold(x)_i = (x_1, y_1, x_2, y_2, dots, x_N, y_N)^T in bb(R)^(2N), quad i = 1, 2, dots, n
$ <eq:aha-encoding>

其中 $n$ 为蜂鸟种群规模，$D = 2N$ 为优化问题维度。算法首先沿 $bold(S)$ 与 $bold(T)$ 连线等比例生成基线航点，并在基线两侧构造带状搜索区域。各维度搜索边界记为 $bold(x)_L$ 与 $bold(x)_U$，带宽仍采用

$
b = max(3 sigma_0, 1.2 thin bar(delta), 80)
$ <eq:aha-band>

其中 $sigma_0$ 表示初始扰动尺度，$bar(delta)$ 表示障碍物或风险分布所诱导的平均安全裕度。由此可将 AHA 的搜索限制在与起终点连线相关、且具有足够绕障空间的连续可行域内，避免随机初始化产生过多明显无效路径。

*种群初始化。* 在第 0 代中，每只蜂鸟的位置在搜索边界内均匀随机生成：

$
bold(x)_i^0 = bold(x)_L + bold(r)_i dot (bold(x)_U - bold(x)_L), quad bold(r)_i in [0, 1]^D
$ <aha-init>

初始化后，将每个个体解码为完整航点序列，拼接起点与终点形成飞行路径，并计算其适应度 $J(bold(x)_i^0)$。本文将路径规划问题转化为最小化问题，适应度函数采用统一的五分量加权形式：

$
J = alpha L_"norm" + beta S_"norm" + gamma R_"norm" + mu W_"norm" + lambda_f F_"norm"
$ <eq:aha-fitness>

式中，$L_"norm"$、$S_"norm"$、$R_"norm"$、$W_"norm"$ 与 $F_"norm"$ 分别表示归一化后的路径长度、平滑性、综合风险、通信可靠性和编队可行性惩罚。适应度值越小，表示该候选路径综合质量越高。

*访问表与食物源记忆。* 访问表是 AHA 区别于一般群体智能算法的核心结构。设 $bold(V T) in bb(R)^(n times n)$ 为访问表，其中 $V T_(i,j)$ 表示蜂鸟 $i$ 自上一次访问食物源 $j$ 以来经历的迭代计数。由于蜂鸟不能访问自身所占据的食物源，访问表主对角线设为不可访问状态，其余元素初始化为 0：

$
V T_(i,j)^0 = cases(
  "null"  i = j,
  0  i != j
)
$ <aha-vt-init>

在引导觅食中，蜂鸟 $i$ 首先依据访问表选择目标食物源。具体而言，在第 $i$ 行中选取访问间隔最大的食物源作为候选目标；若存在多个 $V T_(i,j)$ 相同的目标，则选择其中适应度 $J(bold(x)_j)$ 最小者：

$
j^* = arg min_(j in cal(C)_i) J(bold(x)_j), quad
cal(C)_i = {j | V T_(i,j) = max_(k != i) V T_(i,k)}
$ <aha-target>

该选择准则同时考虑“最久未访问”和“花蜜质量更高”两个因素：前者促使个体持续探索被忽略的解区域，后者使搜索逐步向优质路径附近集中。

*飞行方向向量。* AHA 通过二值方向向量 $bold(D) in {0, 1}^D$ 表示每次位置更新所涉及的坐标维度。$D_d = 1$ 表示第 $d$ 个维度参与本次飞行，$D_d = 0$ 表示该维度保持不变。本文实现中三类飞行方式以等概率随机选取。

（1）*轴向飞行*：随机选取单一维度 $d^* in {1, 2, dots, D}$，令 $D_(d^*) = 1$，其余维度为 0。轴向飞行对应低维局部扰动，适用于对某个航点坐标进行精细修正。

（2）*对角飞行*：随机选取 $d_m$ 个维度参与更新，其中 $2 <= d_m <= D - 1$。该方式可在部分航点坐标上同时产生协同变化，兼具局部开发和中尺度探索能力。

（3）*全向飞行*：令 $bold(D) = (1, 1, dots, 1)^T$，即所有维度均参与更新。全向飞行能够在完整路径层面产生较大范围的形态调整，有利于跳出局部最优。

三种飞行方式分别对应从精细调优到广域搜索的不同尺度。由于路径规划中的每个维度均对应某一航点坐标，方向向量的稀疏程度直接决定了本次更新对路径几何形态的影响范围。

==== 觅食策略设计

*引导觅食。* 引导觅食（Guided Foraging）模拟蜂鸟根据记忆飞向目标食物源的行为。设蜂鸟 $i$ 当前位于 $bold(x)_i^t$，依据访问表选出的目标食物源为 $bold(x)_(j^*)^t$，则候选位置由下式生成：

$
bold(v)_i^(t+1) = bold(x)_(j^*)^t + a dot bold(D) dot (bold(x)_i^t - bold(x)_(j^*)^t), quad a ~ cal(N)(0, 1)
$ <aha-guided>

式中，$a$ 为标准正态随机变量，$dot$ 表示逐元素乘法。当 $a$ 的绝对值较小时，候选解靠近目标食物源，有利于围绕优质路径进行局部开发；当 $a$ 取较大正值或负值时，候选解可能跨越目标或向相反方向移动，从而增强搜索的随机跳跃能力。引导觅食主要承担利用优质历史信息、提高收敛效率的作用。

*领地觅食。* 领地觅食（Territorial Foraging）模拟蜂鸟在自身食物源附近继续搜索新花蜜的行为。其位置更新不依赖外部目标，而是在当前个体周围施加随机扰动：

$
bold(v)_i^(t+1) = bold(x)_i^t + b dot bold(D) dot bold(x)_i^t, quad b ~ cal(N)(0, 1)
$ <aha-territorial>

其中 $b$ 为标准正态随机变量。该策略在个体当前邻域内生成候选解，能够对已有路径进行局部修正。例如，当飞行方向为轴向时，算法仅改变单个航点坐标；当飞行方向为对角时，算法会同步调整若干航点位置；当飞行方向为全向时，整条路径的形态可能发生整体偏移。因此，领地觅食是 AHA 局部精炼能力的主要来源。

*迁徙觅食。* 迁徙觅食（Migration Foraging）用于模拟蜂鸟在当前区域花蜜质量不足时转移至新区域的行为。为防止种群长期集中于局部最优，本文按照原始 AHA 的设定，每隔 $2n$ 次迭代触发一次迁徙操作。此时选取当前种群中适应度最差的个体

$
i_"worst" = arg max_i J(bold(x)_i^t)
$ <aha-worst>

并将其随机重置到搜索空间中：

$
bold(x)_(i_"worst")^(t+1) = bold(x)_L + bold(r) dot (bold(x)_U - bold(x)_L), quad bold(r) in [0, 1]^D
$ <aha-migration>

迁徙操作相当于周期性地向种群注入新的候选路径，可有效缓解访问表引导下种群过度集中、搜索半径持续缩小的问题。由于迁徙周期与种群规模相关，$2n$ 的设定能够保证每只蜂鸟在平均经历足够多次访问决策后，再执行一次全局多样性补充。

==== 更新机制与实现流程

*边界处理与贪婪选择。* 由引导觅食或领地觅食生成的候选解可能超出带状搜索边界，因此需要进行逐维裁剪：

$
bold(v)_i^(t+1) arrow min(max(bold(v)_i^(t+1), bold(x)_L), bold(x)_U)
$ <aha-bound>

裁剪后计算候选解适应度，并采用贪婪准则更新个体：

$
bold(x)_i^(t+1) = cases(
  bold(v)_i^(t+1)   J(bold(v)_i^(t+1)) < J(bold(x)_i^t),
  bold(x)_i^t  "otherwise"
)
$ <aha-greedy>

该策略保证单个个体的历史最优质量不会被劣质候选解破坏，同时通过迁徙机制维持必要的全局扰动。

*访问表更新。* 每次觅食行为完成后，均需同步更新访问表。对于引导觅食，若蜂鸟 $i$ 的目标食物源为 $j^*$，则将 $V T_(i,j^*)$ 置为 0，表示该食物源刚被访问；同一行中其余可访问元素递增 1，表示未访问时间增加。若蜂鸟 $i$ 的位置发生改善，则将其他蜂鸟对新食物源 $i$ 的访问间隔设置为当前表中较大的值，以提示其他个体该新食物源值得在后续迭代中被关注。更新规则可概括为

$
V T_(i,j^*) arrow 0, quad
V T_(i,k) arrow V T_(i,k) + 1 (k != j^*), quad
V T_(m,i) arrow max_k V T_(m,k) + 1 (m != i)
$ <aha-vt-update>

对于领地觅食，由于不存在外部目标食物源，本文实现中主要递增第 $i$ 行的非对角元素；若个体位置被候选解替换，则同步刷新其他蜂鸟对该食物源的访问计数。通过上述方式，访问表在种群中形成一种分布式记忆，使算法既能避免反复访问相同解，又能对新产生的优质食物源保持较高关注度。

*算法实现步骤。* 结合无人机路径规划任务，AHA 的完整实现流程如下。

+ *步骤一：搜索空间初始化。* 根据起点、终点及障碍物分布构造基线航点和带状搜索边界 $bold(x)_L$、$bold(x)_U$，确定种群规模 $n$、航点数 $N$ 和最大迭代次数 $T$。
+ *步骤二：种群与访问表初始化。* 按式@eqt:aha-init 随机生成 $n$ 个蜂鸟个体，计算初始适应度，建立访问表 $bold(V T)$，并记录当前全局最优解 $bold(x)^*$。
+ *步骤三：飞行模式选择。* 对每只蜂鸟随机生成方向向量 $bold(D)$，在轴向、对角和全向飞行中选择一种，以确定本次更新涉及的航点坐标维度。
+ *步骤四：觅食策略选择。* 以 0.5 的概率执行引导觅食，依据访问表和适应度选取目标食物源并按式@eqt:aha-guided 生成候选解；否则执行领地觅食，按式@eqt:aha-territorial 在当前个体邻域内生成候选解。
+ *步骤五：约束修正与个体更新。* 对候选解进行边界裁剪，解码为路径并计算适应度，随后按式@eqt:aha-greedy 进行贪婪选择，同时更新访问表。
+ *步骤六：迁徙操作。* 当迭代次数满足 $t mod 2n = 0$ 时，选取当前最差个体并按式@eqt:aha-migration 随机重置，重新计算适应度并刷新访问表相关行列。
+ *步骤七：终止与输出。* 当迭代次数达到 $T$ 时终止算法，将全局最优向量 $bold(x)^*$ 解码为 $N$ 个中间航点，并与起点、终点拼接得到最终全局路径。

上述流程如 @fig:aha-flow 所示。AHA 在每轮迭代中交替执行引导觅食和领地觅食，并周期性执行迁徙觅食：引导觅食利用访问表中的历史信息实现定向搜索，领地觅食在当前个体邻域内进行细化，迁徙觅食则负责恢复种群多样性。三者共同构成了 AHA 的探索-开发平衡机制。

#capfig(
  image("figures/AHA.png", width: 90%),
  caption: [人工蜂鸟搜索算法流程图],
  label: <fig:aha-flow>,
)
/* #pagebreak()
#algorithm(
  title: [人工蜂鸟算法（AHA）全局路径规划],
  input: [
    起始点 $bold(S)$，目标点 $bold(T)$，障碍物集合 $cal(O)$，
    航点数 $N$，种群规模 $n$，最大迭代次数 $T$。
  ],
  output: [全局最优航点序列 $bold(W)^"*" = {bold(w)_1^"*", dots, bold(w)_N^"*"}$。],
  [计算基线航点及带状搜索边界 $bold(x)_L$, $bold(x)_U$],
  [*for* $i in 1..n$ *do*],
  indent(
    [$bold(x)_i arrow$ 在 $[bold(x)_L, bold(x)_U]$ 内随机初始化],
    [计算适应度 $J(bold(x)_i)$],
  ),
  [初始化访问表：$V T_(i, i) arrow "null"$，$V T_(i, j) arrow 0$ $(i != j)$],
  [确定当前最优解 $bold(x)^*$ 及其适应度 $J^*$],
  [],
  [*for* $t in 1..T$ *do*],
  indent(
    [*for* $i in 1..n$ *do*],
    indent(
      [*步骤 1：选择飞行方向向量*],
      [$r arrow cal(U)(0, 1)$],
      [*if* $r < 1 / 3$ *then* $bold(D) arrow$ 对角飞行向量],
      [*else if* $r > 2 / 3$ *then* $bold(D) arrow$ 全向飞行向量],
      [*else* $bold(D) arrow$ 轴向飞行向量],
      [],
      [*步骤 2：选择觅食策略*],
      [*if* $cal(U)(0, 1) < 0.5$ *then*],
      indent(
        [在 $\{j: V T_(i, j) = max_k V T_(i, k)\}$ 中选适应度最优者为 $bold(x)_("tar")$],
        [$bold(v)_i arrow bold(x)_("tar") + cal(N)(0, 1) dot bold(D) dot (bold(x)_i - bold(x)_("tar"))$],
      ),
      [*else*],
      indent(
        [$bold(v)_i arrow bold(x)_i + cal(N)(0, 1) dot bold(D) dot bold(x)_i$],
      ),
      [],
      [*步骤 3：边界处理与贪婪选择*],
      [将 $bold(v)_i$ 裁剪至 $[bold(x)_L, bold(x)_U]$],
      [计算 $J(bold(v)_i)$],
      [*if* $J(bold(v)_i) < J(bold(x)_i)$ *then*],
      indent(
        [$bold(x)_i arrow bold(v)_i$，$J(bold(x)_i) arrow J(bold(v)_i)$],
        [$V T_(i, j) arrow 0$，$V T_(i, k) arrow V T_(i, k) + 1$ $(k != j)$],
        [$V T_(*, i) arrow max_m V T_(*, m) + 1$],
        [*if* $J(bold(x)_i) < J^*$ *then* 更新 $bold(x)^*$, $J^*$],
      ),
      [*else*],
      indent(
        [$V T_(i, j) arrow 0$，$V T_(i, k) arrow V T_(i, k) + 1$ $(k != j)$],
      ),
    ),
    [],
    [*步骤 4：迁徙操作*],
    [*if* $t "mod" 2n = 0$ *then*],
    indent(
      [$i_("worst") arrow arg max_i J(bold(x)_i)$],
      [$bold(x)_(i_("worst")) arrow bold(x)_L + cal(U)(bold(0), bold(1)) dot (bold(x)_U - bold(x)_L)$],
      [重置 $V T_(i_("worst"), *)$ 与 $V T_(*, i_("worst"))$],
      [更新适应度],
    ),
  ),
  [*return* $bold(W)^"*" arrow$ 解码自 $bold(x)^*$],
) <alg:aha-global>
 */


*算法参数分析。* AHA 的参数结构较为简洁，除航点数 $N$ 由统一路径编码方案给定外，主要需要指定种群规模 $n$ 与最大迭代次数 $T$。与 PSO 的惯性权重和学习因子、ACO 的信息素挥发率、GA 的交叉变异参数以及 MSA 的多阶段行为参数相比，AHA 的飞行模式选择、引导/领地觅食切换和迁徙触发周期均由随机机制与访问表共同驱动，人工调参负担较小。本文实验中为兼顾访问表维护开销与种群多样性，取种群规模 $n = 50$，最大迭代次数 $T = 500$，航点数 $N = 20$。

*收敛性分析。* 从搜索遍历性角度，AHA 的收敛能力主要由访问表、正态随机扰动和迁徙机制共同保证。首先，访问表驱动的目标选择能够避免种群长期重复访问少数食物源，使被忽略区域仍具有重新被搜索的机会；其次，引导觅食和领地觅食中的正态随机因子具有非零尾部，使候选解能够以非零概率产生跨区域扰动；最后，周期性迁徙会重置最差个体，为搜索空间持续注入随机解。贪婪接受准则则保证已获得的优质路径不会被劣质候选解直接替换。因此，AHA 在有限迭代内表现为“访问表维持多样性、觅食策略提高局部质量、迁徙操作恢复全局探索”的协同收敛过程。

*计算复杂度分析。* 设种群规模为 $n$，问题维度 $D = 2N$，最大迭代次数为 $T$。每轮迭代中，各蜂鸟依次完成方向向量构造（$O(D)$）、候选解生成（$O(D)$）、边界裁剪（$O(D)$）及适应度评估（其复杂度由路径采样点数与障碍物数量决定，记为 $O(M)$）。访问表更新涉及对 $n$ 个元素的行操作，复杂度为 $O(n)$。迁徙操作涉及最差个体定位（$O(n)$）及随机重置（$O(D)$）。因此单次迭代复杂度为 $O(n(D + M + n))$，总时间复杂度为 $O(T n (D + M + n))$。空间复杂度主要来自存储 $n$ 个 $D$ 维个体及 $n times n$ 维访问表，为 $O(n D + n^2)$。与 PSO（$O(T n (D + M))$，空间 $O(n D)$）相比，AHA 因维护访问表引入了额外的 $O(T n^2)$ 时间开销与 $O(n^2)$ 空间开销。在路径规划场景中，由于航点数 $N$ 适中（$N = 15 ~ 20$，$D = 30 ~ 40$），种群规模通常取 $n = 50$，$n^2 = 2500$ 量级的额外开销在实际运行中可接受，AHA 的总运行时间与同参数配置的 MSA、GA 处于同一数量级。

  === AL-SHADE算法设计与实现

AL-SHADE（Adaptive L-SHADE）是在 L-SHADE 基础上发展而来的改进型差分进化算法@ALSHADE2022。该算法保留了 SHADE 的成功历史参数记忆和 L-SHADE 的线性种群规模缩减机制，同时引入 current-to-Amean/1 变异策略、自适应策略选择概率以及改进外部档案初始化机制。其核心思想是：在保持 L-SHADE 快速收敛能力的基础上，利用外部档案中较优历史个体的加权均值为搜索提供分布式引导，从而降低单一精英方向造成的早熟收敛风险。

对于无人机全局路径规划问题，AL-SHADE 的作用主要体现在两个方面。第一，算法通过成功历史记忆自适应生成缩放因子 $F$ 与交叉率 $C R$，能够根据不同阶段的搜索效果自动调整变异尺度和遗传信息交换强度，减少人工调参对路径质量的影响。第二，current-to-pbest/1 与 current-to-Amean/1 两种变异策略互为补充：前者强调向当前精英路径收敛，适合提高局部开发精度；后者利用外部档案的加权均值引导搜索，能够在障碍物约束复杂、可行域呈非凸分布的场景中维持较好的搜索多样性。

==== 算法数学模型

*路径编码与初始化。* 与前述全局规划算法保持一致，将无人机从起点 $bold(S)$ 至终点 $bold(T)$ 的路径表示为 $N$ 个中间航点，每个个体编码为 $D = 2N$ 维实数向量：

$
bold(x)_i = (x_1, y_1, x_2, y_2, dots, x_N, y_N)^T, quad i = 1, 2, dots, P
$ <alshade-encoding>

其中 $P$ 为当前种群规模。设第 $j$ 维搜索下界和上界分别为 $L_j$ 与 $U_j$，则初始种群按均匀分布生成：

$
x_(i,j)^0 = L_j + "rand"(0, 1) dot (U_j - L_j), quad j = 1, 2, dots, D
$ <alshade-init>

初始化后计算全部个体适应度，并按适应度升序排序。AL-SHADE 与传统 L-SHADE 的一个重要差异在于：外部档案 $cal(A)$ 不再从空集开始，而是将初始化阶段的最优个体存入档案，为后续 current-to-Amean/1 变异提供初始引导信息。同时，历史记忆数组 $bold(M)_F$ 和 $bold(M)_("CR")$ 初始化为 0.5，最后一个记忆槽保持为 0.9，以增强算法在早期阶段的参数采样范围。

*适应度函数。* 本文仍采用统一的五分量加权适应度函数评价候选路径：

$
J = alpha L_"norm" + beta S_"norm" + gamma R_"norm" + mu W_"norm" + lambda_f F_"norm"
$ <alshade-fitness>

其中 $L_"norm"$、$S_"norm"$、$R_"norm"$、$W_"norm"$ 和 $F_"norm"$ 分别表示路径长度、平滑性、综合风险、通信可靠性和编队可行性惩罚的归一化值。AL-SHADE 的优化目标为最小化 $J$。

==== 变异、交叉与选择

*current-to-pbest/1 变异。* 该策略继承自 L-SHADE，用于将个体引导至当前种群中较优的搜索区域。第 $g$ 代中，目标向量 $bold(x)_i^g$ 的供体向量计算为

$
bold(v)_i^g = bold(x)_i^g + F_i dot (bold(x)_("pbest")^g - bold(x)_i^g) + F_i dot (bold(x)_(r_1)^g - bold(x)_(r_2)^g)
$ <mutation-pbest>

式中，$bold(x)_("pbest")^g$ 从当前种群前 $p$ 比例的优质个体中随机选取；$bold(x)_(r_1)^g$ 从当前种群中随机选取；$bold(x)_(r_2)^g$ 从当前种群与外部档案 $cal(P) union cal(A)$ 的并集中随机选取，且索引互不相同。该策略利用精英个体提供收敛方向，并通过差分项引入群体多样性。

*current-to-Amean/1 变异。* 为降低单一精英个体对搜索方向的过强牵引，AL-SHADE 引入基于外部档案加权均值的 current-to-Amean/1 策略：

$
bold(v)_i^g = bold(x)_i^g + F_i dot (bold(x)_("Amean")^g - bold(x)_i^g) + F_i dot (bold(x)_(r_1)^g - bold(x)_(r_2)^g)
$ <mutation-amean>

其中 $bold(x)_("Amean")^g$ 由外部档案中排名靠前的个体加权计算得到。设档案按适应度升序排列，选取前

$
m = max(1, "round"(epsilon |cal(A)|))
$ <amean-count>

个个体参与计算，其中 $epsilon in (0, 1]$ 为档案利用比例。第 $k$ 个档案个体的权重定义为

$
w_k = frac(ln(m + 0.5) - ln(k), sum_(q=1)^m (ln(m + 0.5) - ln(q))), quad k = 1, 2, dots, m
$ <amean-weight>

则加权档案均值为

$
bold(x)_("Amean")^g = sum_(k=1)^m w_k bold(x)_k^("A")
$ <archive-weighted-mean>

该权重形式使排名靠前的档案个体具有更高贡献，同时保留多个历史较优解的分布信息。与 current-to-pbest/1 相比，current-to-Amean/1 的引导方向更加平滑，不易将种群过早压缩到单一局部区域。

*边界修正。* 供体向量生成后，若某一维度越过搜索边界，则采用父代值与边界值的中点进行修正：

$
v_(i,j)^g = cases(
  (L_j + x_(i,j)^g) / 2, v_(i,j)^g < L_j,
  (U_j + x_(i,j)^g) / 2, v_(i,j)^g > U_j,
  v_(i,j)^g, "otherwise"
)
$ <alshade-boundary>

该方法避免了硬截断造成的大量个体贴附边界现象，同时保留了变异方向中的有效信息。

*二项交叉。* 交叉操作采用差分进化中常用的二项交叉，将供体向量 $bold(v)_i^g$ 与目标向量 $bold(x)_i^g$ 组合为试验向量 $bold(u)_i^g$：

$
u_(i,j)^g = cases(
  v_(i,j)^g, "if " "rand"(0, 1) < C R_i space "or" space j = j_("rand"),
  x_(i,j)^g, "otherwise"
)
$ <binomial-crossover>

其中 $j_("rand")$ 为随机选定的维度，用于保证试验向量至少有一个分量来自供体向量。$C R_i$ 越大，试验向量继承变异信息的比例越高。

*选择与外部档案更新。* 试验向量生成后，计算其适应度并与父代进行贪婪选择：

$
bold(x)_i^(g+1) = cases(
  bold(u)_i^g, J(bold(u)_i^g) <= J(bold(x)_i^g),
  bold(x)_i^g, "otherwise"
)
$ <de-selection>

若 $bold(u)_i^g$ 优于父代，则将被替换的父代 $bold(x)_i^g$ 存入外部档案，并记录本次成功使用的 $F_i$、$C R_i$ 以及适应度改进量 $Delta J_i = |J(bold(u)_i^g) - J(bold(x)_i^g)|$。当档案容量超过上限时，采用随机删除或截断方式维持固定规模。

==== 参数与策略自适应

*缩放因子与交叉率生成。* AL-SHADE 通过历史记忆数组 $bold(M)_F$ 和 $bold(M)_("CR")$ 生成每个个体的控制参数。对个体 $i$，首先随机选择记忆槽 $h_i in {1, 2, dots, H}$；随后从以 $M_(F,h_i)$ 为位置参数的柯西分布中采样 $F_i$，并将其限制在 $(0, 1]$ 内；从以 $M_(C R,h_i)$ 为均值、0.1 为标准差的正态分布中采样 $C R_i$，并截断至 $[0, 1]$。这种采样方式既能围绕历史成功参数进行稳定搜索，又能通过柯西分布的长尾特性保留较大步长扰动的可能性。

*历史记忆更新。* 每代结束后，将成功产生更优试验向量的参数分别保存到集合 $cal(S)_F$ 与 $cal(S)_("CR")$ 中，并以适应度改进量作为权重更新记忆槽。设归一化权重为

$
tilde(w)_i = frac(Delta J_i, sum_(k in cal(S)) Delta J_k)
$ <alshade-success-weight>

则加权 Lehmer 均值为

$
"mean"_"WL"(S) = frac(sum_(i in cal(S)) tilde(w)_i s_i^2, sum_(i in cal(S)) tilde(w)_i s_i)
$ <lehmer-update>

由此更新 $M_F$ 和 $M_("CR")$ 的当前记忆槽。最后一个记忆槽保持为 0.9，用于保留较大参数取值的采样通道，避免参数记忆在多代迭代后过度收缩。

*自适应策略选择。* AL-SHADE 对两种变异策略采用概率调度。对每个个体生成随机数 $r in [0, 1]$，若 $r < P_s$，则执行 current-to-pbest/1；否则执行 current-to-Amean/1。第 $g$ 代结束后，根据两种策略产生改进解的成功概率更新 $P_s$：

$
P_s^(g+1) = P_s^g + 0.05 dot (1 - P_s^g) dot (P_1 - P_2) dot frac(F_("Es"), F_("max"))
$ <strategy-prob-update>

其中 $P_1$ 和 $P_2$ 分别表示 current-to-pbest/1 与 current-to-Amean/1 在当前代中的成功比例，$F_("Es")$ 为已消耗的函数评价次数，$F_("max")$ 为最大函数评价次数。若 $P_1 > P_2$，说明精英引导策略在当前阶段更有效，$P_s$ 相应增大；反之，则增加 current-to-Amean/1 的执行机会。该机制使算法能够随搜索进程动态调节开发与探索强度。

*线性种群规模缩减。* AL-SHADE 继承 L-SHADE 的线性种群缩减策略。设初始种群规模为 $P_("init")$，最小种群规模为 $P_("min")$，则当前目标种群规模为

$
P_(g+1) = "round"lr(frac(F_("Es"), F_("max")) dot (P_("min") - P_("init")) + P_("init")) 
$ <lpsr>

当实际种群规模大于 $P_(g+1)$ 时，按适应度排序删除最差个体。该机制使算法在前期保留较大种群以增强全局搜索能力，在后期逐步减少个体数量以集中计算资源进行局部精化。

==== 实现流程

结合本文无人机路径规划任务，AL-SHADE 的实现流程可概括如下。

+ *步骤一：初始化搜索空间。* 根据起点、终点及障碍物分布确定航点编码维度和搜索边界，设置 $P_("init")$、$P_("min")$、$F_("max")$、$H$、$p$ 和初始策略概率 $P_s$。
+ *步骤二：初始化种群与档案。* 按式@eqt:alshade-init 生成初始种群，计算适应度并排序，将初始最优个体存入外部档案，同时初始化 $bold(M)_F$ 和 $bold(M)_("CR")$。
+ *步骤三：参数采样。* 对每个个体从历史记忆中采样 $F_i$ 与 $C R_i$，清空本代成功参数集合 $cal(S)_F$、$cal(S)_("CR")$ 以及策略成功统计量。
+ *步骤四：策略选择与变异。* 按概率 $P_s$ 选择 current-to-pbest/1 或 current-to-Amean/1，分别按式@eqt:mutation-pbest 和式@eqt:mutation-amean 生成供体向量，并进行边界修正。
+ *步骤五：交叉与选择。* 按式@eqt:binomial-crossover 生成试验向量，计算适应度后按式@eqt:de-selection 执行贪婪选择，并将被替换父代写入外部档案。
+ *步骤六：自适应更新。* 根据成功集合更新 $bold(M)_F$ 与 $bold(M)_("CR")$，根据两种变异策略的成功比例更新 $P_s$，并依据式@eqt:lpsr 执行线性种群规模缩减。
+ *步骤七：终止输出。* 当函数评价次数达到 $F_("max")$ 或迭代次数达到上限时，输出当前最优个体，并解码为全局航点序列。

上述流程如 @fig:alshade-flow 所示。总体而言，AL-SHADE 通过“历史参数记忆 + 双变异策略 + 外部档案均值 + 线性种群缩减”构成完整的自适应优化框架，使算法在路径规划中既能快速逼近优质路径区域，又能在复杂障碍物环境中保持必要的搜索多样性。

#capfig(
  image("figures/ALSHADE.png", width: 85%),
  caption: [AL-SHADE算法流程图],
  label: <fig:alshade-flow>,
)

/* #pagebreak()
#algorithm(
  title: [AL-SHADE 全局路径规划算法],
  input: [
    起始点 $bold(S)$，目标点 $bold(T)$，障碍物集合 $cal(O)$，
    航点数 $N$，初始种群规模 $P_("init")$，最小种群规模 $P_("min")$，
    最大迭代次数 $T$，历史记忆容量 $H$，精英比例 $p_("best")$，
    档案比率 $r_("arc")$，初始策略概率 $S_p^0$。
  ],
  output: [全局最优航点序列 $bold(W)^"*" = {bold(w)_1^"*", dots, bold(w)_N^"*"}$。],
  [计算基线航点及带状搜索边界 $bold(x)_L$, $bold(x)_U$],
  [$P arrow P_("init")$],
  [*for* $i in 1..P$ *do*],
  indent(
    [$bold(x)_i arrow$ 在 $[bold(x)_L, bold(x)_U]$ 内随机初始化],
    [计算适应度 $J(bold(x)_i)$],
  ),
  [按适应度升序排列种群],
  [$cal(A) arrow \{bold(x)^*\}$，$N_A arrow r_("arc") dot P$],
  [$bold(M)_F arrow (0.5, dots, 0.5, 0.9)$，$bold(M)_("CR") arrow (0.5, dots, 0.5, 0.9)$],
  [$S_p arrow 0.5$, $quad k arrow 0$],
  [],
  [*for* $t in 1..T$ *do*],
  indent(
    [计算加权档案均值 $bold(x)_("mean")^t$],
    [$cal(S)_F arrow emptyset$, $cal(S)_("CR") arrow emptyset$, $cal(S)_(Delta J) arrow emptyset$],
    [$n_1^("all") arrow 0$, $n_2^("all") arrow 0$, $n_1^("suc") arrow 0$, $n_2^("suc") arrow 0$],
    [],
    [*for* $i in 1..P$ *do*],
    indent(
      [$h_i arrow$ 从 $0..H-1$ 中随机选取],
      [$C R_i arrow$ 截断至 $[0, 1]$ 的正态采样 $cal(N)(M_(C R, h_i), 0.1)$],
      [*repeat* $F_i arrow "Cauchy"(M_(F, h_i), 0.1)$ *until* $F_i > 0$; $F_i arrow min(F_i, 1)$],
      [],
      [*if* $cal(U)(0, 1) < S_p$ *then*],
      indent(
        [按式@eqt:mutation-pbest 执行 current-to-pbest/1 变异],
        [$n_1^("all") arrow n_1^("all") + 1$, $quad "tag" arrow 1$],
      ),
      [*else*],
      indent(
        [按式@eqt:mutation-amean 执行 current-to-Amean/1 变异],
        [$n_2^("all") arrow n_2^("all") + 1$, $quad "tag" arrow 2$],
      ),
      [越界分量中点反射修正],
      [按式@eqt:binomial-crossover 生成试验向量 $bold(u)_i^t$],
      [将 $bold(u)_i^t$ 各维度限制在 $[bold(x)_L, bold(x)_U]$ 内],
      [计算 $J(bold(u)_i^t)$],
      [],
      [*if* $J(bold(u)_i^t) < J(bold(x)_i^t)$ *then*],
      indent(
        [将 $F_i$, $C R_i$, $Delta J_i = |J(bold(u)_i^t) - J(bold(x)_i^t)|$ 加入各成功集合],
        [若 $"tag" = 1$ 则 $n_1^("suc") arrow n_1^("suc") + 1$，否则 $n_2^("suc") arrow n_2^("suc") + 1$],
        [将被淘汰的 $bold(x)_i^t$ 存入 $cal(A)$（若 $|cal(A)| >= N_A$ 则随机替换）],
        [$bold(x)_i^t arrow bold(u)_i^t$, $quad J(bold(x)_i^t) arrow J(bold(u)_i^t)$],
      ),
      [*else if* $J(bold(u)_i^t) == J(bold(x)_i^t)$ *then*],
      indent(
        [$bold(x)_i^t arrow bold(u)_i^t$],
      ),
    ),
    [],
    [*步骤：参数记忆更新*],
    [*if* $cal(S)_F != emptyset$ *then*],
    indent(
      [以 $Delta J_i$ 为权重，按式@eqt:lehmer-update 更新 $M_(F, k)$ 和 $M_(C R, k)$],
      [$k arrow (k + 1) mod (H - 1)$],
    ),
    [*else if* 所有成功 $C R$ 为 0 *then* $M_(C R, k) arrow -1$],
    [],
    [*步骤：自适应策略概率更新*],
    [*if* $n_1^("all") > 0$ *and* $n_2^("all") > 0$ *then*],
    indent(
      [$r_1 arrow n_1^("suc") / n_1^("all")$, $quad r_2 arrow n_2^("suc") / n_2^("all")$],
      [$S_p arrow S_p + 0.05 dot (1 - S_p) dot (r_1 - r_2) dot (t / T)$],
      [$S_p arrow "clip"(S_p, 0.1, 0.9)$],
    ),
    [],
    [*步骤：线性种群缩减*],
    [$P_("target") arrow "round"(P_("init") - (P_("init") - P_("min")) dot t / T)$],
    [*if* $P_("target") < P$ *then*],
    indent(
      [按适应度升序保留前 $P_("target")$ 个个体],
      [$P arrow P_("target")$, $quad N_A arrow r_("arc") dot P$],
      [$cal(A) arrow$ 若 $|cal(A)| > N_A$ 则按适应度升序截断至 $N_A$],
    ),
  ),
  [*return* $bold(W)^"*" arrow$ 解码自种群最优个体],
) <alg:alshade-global> */

*算法参数分析。* AL-SHADE 的主要外部参数包括初始种群规模 $P_("init")$、最小种群规模 $P_("min")$、最大函数评价次数 $F_("max")$、历史记忆容量 $H$、精英比例 $p$、档案利用比例 $epsilon$ 以及初始策略概率 $P_s$。其中 $P_("min")$ 通常取 4，以满足差分变异对互异个体数量的基本要求；$H$ 控制历史成功参数的记忆长度，过小会导致参数波动过强，过大则会降低对当前搜索阶段的响应速度；$p$ 决定 current-to-pbest/1 中精英候选集合的大小；$epsilon$ 决定参与 $bold(x)_("Amean")$ 计算的档案个体比例。本文实验中采用 $P_("init") = 500$、$P_("min") = 4$、$H = 6$、$p = 0.1$、航点数 $N = 20$，并以统一迭代次数约束函数评价规模，从而保证其与其他全局规划算法具有可比性。

*收敛性分析。* AL-SHADE 的收敛过程由贪婪选择、参数自适应和种群规模缩减共同驱动。首先，式@eqt:de-selection 所示的选择机制保证试验向量只有在不劣于父代时才进入下一代，从而实现精英保留。其次，$F$ 和 $C R$ 的历史记忆更新使成功参数在后续迭代中被更高概率采样，推动算法逐步形成适合当前适应度地形的搜索尺度。再次，current-to-Amean/1 通过外部档案均值引入历史较优解的分布信息，可缓解 current-to-pbest/1 向单一精英区域过快收缩的问题。最后，LPSR 在早期保留较大种群以增强覆盖范围，在后期减少个体数量以提高局部开发强度。因此，AL-SHADE 在有限迭代内表现为先广域探索、后集中精化的动态收敛特征。

*计算复杂度分析。* 设当前种群规模为 $P$，问题维度为 $D = 2N$，外部档案容量为 $N_A$，单次适应度评估复杂度为 $O(M)$。每一代中，参数采样、变异、边界修正和二项交叉的复杂度均为 $O(P dot D)$；适应度评估复杂度为 $O(P dot M)$；外部档案排序及 $bold(x)_("Amean")$ 计算复杂度约为 $O(N_A log N_A + m dot D)$；历史记忆和策略概率更新复杂度不超过 $O(P)$；LPSR 所需的种群排序复杂度为 $O(P log P)$。因此，单代时间复杂度可表示为 $O(P dot (D + M) + N_A log N_A + P log P)$。空间复杂度主要来自当前种群、外部档案和历史记忆数组，为 $O((P + N_A) dot D + H)$。由于 $P$ 随函数评价次数线性减小，AL-SHADE 后期计算负担会逐步下降；其额外开销主要来源于外部档案维护和档案均值计算。

 == 局部避障算法设计与实现

前文所述的全局路径规划器（PSO、ACO、GA、MSA、AHA、AL-SHADE）为无人机集群提供了宏观的参考航点序列，然而在实际飞行中，集群需面对两类全局规划难以预先穷举的局部风险：一是障碍物分布信息的有限精度（传感器探测范围受限或环境动态变化），二是多机编队保持与单机避障之间的实时耦合冲突。为此，在全局规划层之下构建局部避障层，以人工势场法（Artificial Potential Field, APF）为核心，通过改进的斥力场设计与前视预测机制，为每架无人机实时生成局部修正加速度，弥补全局路径在动态环境下的安全裕度不足。

==== 改进人工势场法

经典人工势场法由 Khatib 于 1986 年提出，其基本原理是在工作空间中构造虚拟势场：目标点产生引力场，障碍物产生斥力场，无人机在合力驱动下向目标运动的同时绕开障碍物。该方法结构简洁、实时性好，但在复杂障碍物分布下存在两个固有缺陷：一是目标不可达问题（Goal Non-Reachable with Obstacles Nearby, GNRON），即当目标位于障碍物附近时，斥力可能压倒引力导致无人机无法逼近目标；二是局部极小值（Local Minima）问题，即在"U 型"障碍物或狭窄通道中合势场为零，无人机陷入来回振荡或静止不前的死锁状态。本节针对上述两个问题，分别从斥力场建模与前视自适应减速两个维度对经典 APF 进行改进。

*障碍物斥力场的切向旋转增强。* 经典 APF 的斥力方向严格沿障碍物中心到无人机的径向线，在无人机与障碍物、目标三者共线时，合力方向仅沿该直线，无法产生绕行运动。本节在径向斥力分量之外引入切向旋转分量，构成矢量复合斥力场。

设无人机 $i$ 的当前位置为 $bold(p)_i = (x_i, y_i)^T$，速度矢量为 $bold(v)_i = (v_(x i), v_(y i))^T$。对于当前探测范围内的全部障碍物，首先筛选出距无人机最近的障碍物 $O^"*"$（以间隙距离 $d = norm(bold(p)_i - bold(c)_("obs")) - R_("obs")$ 为度量，其中 $bold(c)_("obs")$ 和 $R_("obs")$ 分别为障碍物中心与半径）。斥力的作用范围定义为 $d_("inf") = R_("obs") + d_("safe") + 50$，其中 $d_("safe")$ 为预设安全距离（取 20 m）。当 $d >= d_("inf")$ 时斥力为零；当 $d < d_("inf")$ 时，斥力由径向分量 $bold(f)_("rad")$ 和切向分量 $bold(f)_("tan")$ 合成。

径向斥力沿无人机指向障碍物中心的单位向量 $bold(e)_r$ 方向，其幅值由归一化距离强度 $sigma = max(0, (d_("inf") - d) / d_("inf"))$ 调控：

$
bold(f)_("rad") = k_r sigma bold(e)_r, quad bold(e)_r = frac(bold(p)_i - bold(c)_("obs"), norm(bold(p)_i - bold(c)_("obs"))) $ <radial-repulsion>

式中 $k_r = 25$ 为径向斥力增益。$sigma in [0, 1)$ 随无人机接近障碍物而单调增大，使斥力在障碍物边缘达到最大。

切向斥力的设计是本改进的核心。取 $bold(e)_r$ 的逆时针正交单位向量 $bold(e)_t = (-e_(r, y), e_(r, x))^T$，但为保证切向力方向与当前飞行方向一致（引导无人机顺势绕过而非逆势折返），将 $bold(e)_t$ 与速度矢量进行点积校核：若 $bold(v)_i dot bold(e)_t < 0$，则将 $bold(e)_t$ 取反。切向斥力为：

$
bold(f)_("tan") = k_t sigma bold(e)_t $ <tangential-repulsion>

其中 $k_t = 15$ 为切向力增益。切向分量的引入使得合力方向偏离径向线，形成沿障碍物边缘的旋转趋势，从而在"U 型"障碍物或狭窄通道中赋予无人机主动绕行的运动分量，有效降低局部死锁概率。

复合斥力为两分量之和 $bold(f)_("rep") = bold(f)_("rad") + bold(f)_("tan")$。当无人机在开阔区域远离障碍物时，$sigma ≈ 0$ 导致斥力自动退化为零，避免对集群编队产生不必要的扰动；在紧邻障碍物时，径向分量主导安全避障，切向分量提供偏向绕行引导，二者协同实现平滑规避。

*前视预测减速机制。* GNRON 问题的根源在于目标附近障碍物的斥力抵消了引力，导致无人机在终点前停滞。本节引入基于运动预测的前视安全评估——沿无人机当前速度方向，以固定时间步长向前模拟一段预测时域内的轨迹，计算该预测轨迹上距各障碍物的最小间隙。若预测间隙小于减速阈值，则按比例降低引力参考速度的幅值，保证无人机以安全速度靠近目标区域。

具体地，取预测视距 $T_h = 5.0$ s，将其等分为 $N_s = 25$ 个微段，微段时间步长为 $Delta t_s = T_h / N_s$。以当前位置为起点，假设速度恒定，逐段向前递推得到 $N_s$ 个预测位置点。对每个预测点计算其距各障碍物的间隙，取全预测时域的最小值记为 $d_("pred")$：

$
d_("pred") = min_(k=1)^(N_s) min_(O in cal(O)) lr(norm(bold(p)_i + k Delta t_s bold(v)_i - bold(c)_O) - R_O) $ <predicted-clearance>

设减速阈值为 $d_("slow") = 2.5 d_("safe") = 50$ m，前视减速因子 $lambda$ 定义为：

$
lambda = "clip"lr(frac(d_("pred"), d_("slow")), space 0.2, space 1.0) $ <speed-reduction>

当 $d_("pred") >= d_("slow")$（前方安全）时 $lambda = 1$，引力速度全额保留；当 $d_("pred") -> 0$ 时 $lambda arrow 0.2$，引力速度降至巡航值的 20%，确保无人机在密集障碍区以低速谨慎穿行而非强行高速逼近。将 $lambda$ 作用于导航参考速度 $bold(v)_("ref")$，得到经安全缩放后的有效参考速度 $bold(v)_("ref")^("eff") = lambda bold(v)_("ref")$。

*目标引力场。* 引力场驱动无人机沿全局路径航点序列飞行。设当前目标航点为 $bold(p)_("tgt")$（来自全局规划器输出经 APF 局部修正后的下一航点），无人机 $i$ 的位置为 $bold(p)_i$，则目标方向、期望巡航速度及引力加速度指令分别为：

$
"direction" arrow bold(p)_("tgt") - bold(p)_i, quad "distance" arrow norm("direction") $ <target-direction>

$
bold(v)_("ref") = v_("cruise") dot frac("direction", "distance"), quad v_("cruise") = 25 space "m/s" $ <ref-velocity>

$
bold(u)_("nav") = dot(bold(v))_("ref") - k_3 thin (bold(v)_i - bold(v)_("ref")^("eff")) $ <nav-guidance>

当无人机距目标航点小于 50 m 时，巡航速度按比例线性缩减（$v = max(5, 25 dot d / 50)$），实现平滑的航点切换减速；当距最终目标小于 1 m 时，引力置零，无人机由编队控制接管完成最终的精确悬停。

==== 综合控制律与动力学约束

每架无人机 $i$ 在每一控制周期 $Delta t = 0.1$ s 内，接收来自三个功能通道的加速度指令：

$
bold(u)_i^("cmd") = bold(u)_i^("cons") + bold(u)_("nav") + bold(f)_("rep") $ <integrated-control>

其中 $bold(u)_i^("cons")$ 为第二章所述基于改进一致性协议（时延-切换拓扑）的编队控制加速度，$bold(u)_("nav")$ 为式@eqt:nav-guidance 给出的目标引力与速度跟踪加速度，$bold(f)_("rep")$ 为式@eqt:radial-repulsion 和式@eqt:tangential-repulsion 合成的障碍物复合斥力加速度（单位质量归一化）。三者以加性方式并行叠加，实现"编队保持—目标引导—局部避障"的实时耦合。

在输出至自动驾驶仪之前，加速度指令需通过两级物理约束校验。第一级为速度约束：预估下一时刻的速度 $bold(v)_i(t + Delta t) = bold(v)_i(t) + bold(u)_i^("cmd") Delta t$，若其范数超过最大允许速度 $v_("max") = 80$ m/s，则按比例收缩各分量：$bold(u)_i^("cmd") arrow (v_("max") / norm(bold(v)_i(t + Delta t))) bold(u)_i^("cmd")$。第二级为加速度约束：若 $norm(bold(u)_i^("cmd")) > a_("max") = 4g$，则等比例缩放：$bold(u)_i^("cmd") arrow (a_("max") / norm(bold(u)_i^("cmd"))) bold(u)_i^("cmd")$。经约束映射后的可行加速度指令通过式@eqt:control-to-autopilot 转换为一阶惯性自动驾驶仪的速度指令，完成从控制计算到物理执行的闭环。

*控制优先级分析。* 在实际执行中，三个加速度通道之间的优先级通过各分量的增益幅值间接体现。障碍物斥力增益（$k_r = 25$、$k_t = 15$）显著高于编队控制增益（$k_1 = 0.8$、$k_2 = 1.2$），使得在近距离遭遇障碍物时（$sigma -> 1$），斥力 $bold(f)_("rep")$ 的幅值（约 40 N/kg 级）远大于编队控制力（约 1–5 N/kg 级），自然形成"安全优先"的优先级次序：避障 > 目标引导 > 编队保持。当 $sigma -> 0$（远离障碍物）时，斥力自动归零，编队控制与目标引导恢复主导，集群回归稳定编队巡航。

*分层耦合架构总结。* 局部避障层与全局规划层、编队控制层之间形成"上-中-下"三层耦合架构。全局规划层以数秒至数十秒的周期运行，输出覆盖全程的参考航点序列；编队控制层以 0.1 s 周期运行，维持集群几何构型的稳定；局部避障层嵌入同一 0.1 s 控制周期内，以加性加速度修正的方式对前两层指令进行在线增补。上层输出的全局航点作为本层的目标引力源，本层输出的安全修正加速度与编队加速度矢量叠加后共同驱动无人机运动。该架构保证了路径规划的全局最优性与避障反应的高频实时性之间的解耦与协同——全局层"看得远但不快"，局部层"看得不远但足够快"。

#pagebreak()
#algorithm(
  title: [APF 局部避障控制（单步执行周期）],
  input: [
    无人机状态 $(bold(p)_i, bold(v)_i)$，当前目标航点 $bold(p)_("tgt")$，
    障碍物集合 $cal(O)$，编队一致加速度 $bold(u)_i^("cons")$，
    参数集 $Theta = \{k_r, k_t, k_3, v_("max"), a_("max"), d_("safe"), T_h, N_s, Delta t\}$。
  ],
  output: [满足动力学约束的自动驾驶仪速度指令 $(v_(x i)^c, v_(y i)^c)$。],
  [*步骤 1：目标引力计算*],
  indent(
    [计算目标方向 $bold(d) arrow bold(p)_("tgt") - bold(p)_i$，距离 $l arrow norm(bold(d))$],
    [$v arrow$ 若 $l < 50$ 则 $max(5, 25 dot l / 50)$ 否则 25],
    [$bold(v)_("ref") arrow v dot bold(d) / l$，$dot(bold(v))_("ref") arrow bold(0)$（匀速巡航假设）],
  ),
  [*步骤 2：前视预测与减速因子*],
  indent(
    [$d_("pred") arrow$ 按式@eqt:predicted-clearance 沿 $bold(v)_i$ 方向预测未来 $T_h$ 内的最小障碍物间隙],
    [$lambda arrow "clip"(d_("pred") / (2.5 d_("safe")), 0.2, 1.0)$],
    [$bold(v)_("ref")^("eff") arrow lambda bold(v)_("ref")$],
  ),
  [*步骤 3：障碍物斥力计算*],
  indent(
    [筛选距 $bold(p)_i$ 最近的障碍物 $O^"*"$，间隙 $d arrow norm(bold(p)_i - bold(c)_O) - R_O$],
    [$d_("inf") arrow R_O + d_("safe") + 50$],
    [*if* $d < d_("inf")$ *then*],
    indent(
      [$sigma arrow max(0, (d_("inf") - d) / d_("inf"))$],
      [$bold(e)_r arrow (bold(p)_i - bold(c)_O) / norm(bold(p)_i - bold(c)_O)$],
      [$bold(f)_("rad") arrow k_r sigma bold(e)_r$],
      [$bold(e)_t arrow (-e_(r,y), e_(r,x))^T$; 若 $bold(v)_i dot bold(e)_t < 0$ 则 $bold(e)_t arrow -bold(e)_t$],
      [$bold(f)_("tan") arrow k_t sigma bold(e)_t$],
      [$bold(f)_("rep") arrow bold(f)_("rad") + bold(f)_("tan")$],
    ),
    [*else* $bold(f)_("rep") arrow bold(0)$],
  ),
  [*步骤 4：导航引力加速度*],
  indent(
    [$bold(u)_("nav") arrow dot(bold(v))_("ref") - k_3 (bold(v)_i - bold(v)_("ref")^("eff"))$],
  ),
  [*步骤 5：三通道加速度合成*],
  indent(
    [$bold(u)_i^("cmd") arrow bold(u)_i^("cons") + bold(u)_("nav") + bold(f)_("rep")$],
  ),
  [*步骤 6：动力学约束投影*],
  indent(
    [$bold(v)_("next") arrow bold(v)_i + bold(u)_i^("cmd") Delta t$],
    [*if* $norm(bold(v)_("next")) > v_("max")$ *then* $bold(u)_i^("cmd") arrow (v_("max") / norm(bold(v)_("next"))) bold(u)_i^("cmd")$],
    [*if* $norm(bold(u)_i^("cmd")) > a_("max")$ *then* $bold(u)_i^("cmd") arrow (a_("max") / norm(bold(u)_i^("cmd"))) bold(u)_i^("cmd")$],
  ),
  [*步骤 7：自驾仪指令映射*],
  indent(
    [$v_(x i)^c arrow v_(x i) + tau_v u_(x i)^("cmd")$, $quad v_(y i)^c arrow v_(y i) + tau_v u_(y i)^("cmd")$],
  ),
  [*return* $(v_(x i)^c, v_(y i)^c)$],
) <alg:apf-local-avoidance>

*控制参数分析与标定。* APF 局部避障器涉及三个主参数：径向斥力增益 $k_r$、切向力增益 $k_t$ 和安全距离 $d_("safe")$。$k_r = 25$ 的取值原则为——在 $d_("safe") = 20$ m 的缓冲区边缘（$sigma ≈ 0.5$），径向斥力约 12.5 N/kg，足以在两到三个控制周期内显著改变 80 m/s 巡航无人机的航向。$k_t = 15$ 的取值使切向分量约为径向分量的 60%，该比例经过仿真调试确定：过小（< 30%）则绕行效果不足，过大（> 90%）则可能引导无人机过度偏离目标方向。$d_("safe") = 20$ m 综合考虑了编队包络半径（约 60 m）和 GPS/惯导组合定位误差（约 5–10 m），为障碍物周围保留两级缓冲区——$d_("safe")$ 内为强斥力急避区，$d_("safe")$ 至 $d_("inf")$ 之间为线性衰减预警区。

前视预测参数 $T_h = 5.0$ s 和 $N_s = 25$ 的配置使预测精度约为 0.2 s/段，对应 80 m/s 速度下约 16 m/段的预测空间分辨率。该分辨率足以提前一到两个预测段发现前方障碍物。减速阈值 $d_("slow") = 2.5 d_("safe") = 50$ m 与下限 $lambda_("min") = 0.2$ 的组合确保无人机在进入安全缓冲区之前已开始降速，避免高速冲入急避区后因机动能力饱和而无法及时转向。

= 算法改进

== AL-SHADE 算法的三项改进：TALG 框架

原生 AL-SHADE 在路径规划中的核心困境可归结为三点。第一，航点序列的离散编码方式天然不具备路径连续性约束——各航点坐标独立演化，即使相邻两代在搜索空间中接近，其解码后的路径仍可能出现折线式的剧烈拐弯，导致路径平滑性项 $S$ 的优化与长度项 $L$ 的优化相互冲突。第二，变异因子 $F$ 的柯西采样对全体个体一视同仁，无法感知障碍物的空间分布——在开阔区域需要的小步长精细搜索与在障碍物密集区域需要的大步长跳跃逃离之间缺少自适应切换机制。第三，外部档案 $cal(A)$ 的随机替换策略可能存入大量重复或高度相似的解，造成档案多样性的隐性退化，削弱 current-to-Amean/1 变异策略所依赖的种群分布广度。针对上述问题，TALG（Threat-Adaptive Lévy-Gaussian）框架依次引入基于欧氏距离的外部档案相似度去重、种群威胁度驱动的 Lévy-Gaussian 双态 $F$ 生成以及 B 样条曲线参数化路径建模三项改进，以分别提升档案多样性、环境感知搜索能力和路径几何连续性。

=== 外部档案相似度去重

原生 AL-SHADE 的外部档案 $cal(A)$ 在存入被淘汰父代向量时采用的策略是"若档案满则随机替换一项"。该方法实现简单，但隐含一个严重问题：当种群在迭代后期收敛至某个较优解附近时，大量被淘汰的父代向量与该较优解高度相似（在 $bb(R)^(2N)$ 空间中相互距离极近），随机替换策略无法识别并抑制这种冗余——档案中可能累积大量近乎相同的解，导致加权档案均值 $bold(x)_("mean")^t$ 退化为单一聚类中心的噪声估计，丧失其融合多个独立优质解分布信息的设计初衷。

*基于欧氏距离的相似度判别。* TALG 在每次向外部档案 $cal(A)$ 写入被淘汰父代向量 $bold(x)_("parent")$ 之前，计算其与当前档案所有成员的欧氏距离：

$
d_"min"^("archive") = min_(j=1)^(|cal(A)|) norm(bold(x)_("parent") - bold(a)_j)_2 $ <archive-min-dist>

设定相似度阈值 $tau_("sim") = 25.0$。此阈值的物理含义为：在 $2N$ 维航点坐标空间（$N = 20$，$D = 40$）中，两个个体若在欧氏距离意义上相距不足 25 个单位（平均每维度差异约 $25 / sqrt(40) ≈ 4.0$），则其解码后的候选路径在几何形态上已高度重合，对加权均值计算的贡献几乎等效。

*三支去重决策逻辑。* 比较 $d_"min"^("archive")$ 与阈值 $tau_("sim")$，分三种情况处理：

$
"ArchiveUpdate"(bold(x)_("parent")) = cases(
  "替换最近邻" bold(a)_("nearest"), "if " d_"min"^("archive") < tau_("sim") space "and" space J_"parent" < J_"nearest",
  "丢弃父代", "if " d_"min"^("archive") < tau_("sim") space "and" space J_"parent" >= J_"nearest",
  "存入档案（追加或随机替换）", "if " d_"min"^("archive") >= tau_("sim")
) $ <archive-dedup-logic>

情况一（空间相似 + 父代更优）：虽然两个解在空间上高度相似，但父代代表了向更优方向的微小改进——用父代替换档案中的相似成员，既消除了冗余、又提升了档案的整体质量。情况二（空间相似 + 父代不优）：两个高度相似的解中档案成员更优，说明该空间区域已被充分探索，父代不再存入档案，从源头杜绝了冗余累积。情况三（空间不相似）：父代与所有档案成员均保持足够差异，代表了档案中尚未覆盖的新解区域——若档案未满则直接追加，若已满则随机替换一项（保有原生 L-SHADE 的随机性以维持档案更新的无偏性）。

*去重效益分析。* 相似度去重使外部档案的有效信息熵显著提升。在障碍物密集的路径规划场景中，种群容易在少数几条可行的狭窄通道周围形成高密度聚类——若无去重机制，容量为 $N_A ≈ 1300$（$P = 500$，$r_("arc") = 2.6$）的档案可能在一半以上的槽位中存储相互距离不足 25 的近重复解。去重后，档案向"低冗余、高覆盖"方向演化，$bold(x)_("mean")^t$ 的计算综合了分布更广的优质解区域信息，current-to-Amean/1 变异策略的差分方向 $(bold(x)_("mean")^t - bold(x)_i^t)$ 由此获得更丰富的搜索引导信号，而非指向单一聚类的退化均值。

档案相似度去重的完整决策流程见#algorithm-ref(<alg:archive-dedup>)。

#algorithm(
  title: [外部档案相似度去重更新（ArchiveDedupUpdate）],
  input: [
    外部档案矩阵 $cal(A) in bb(R)^(N_A times D)$，档案适应度向量 $bold(J)_A$，
    待存入父代 $bold(x)_("parent") in bb(R)^D$，父代适应度 $J_("parent")$，
    档案容量 $N_A$，相似度阈值 $tau_("sim") = 25.0$。
  ],
  output: [更新后的档案 $cal(A)$ 与适应度 $bold(J)_A$。],
  [*步骤 1：空档案初始化*],
  indent(
    [*if* $|cal(A)| = 0$ *then*],
    indent(
      [$cal(A) arrow [bold(x)_("parent")]$, $quad bold(J)_A arrow [J_("parent")]$],
      [*return* $cal(A)$, $bold(J)_A$],
    ),
  ),
  [*步骤 2：计算父代与档案所有成员的欧氏距离*],
  indent(
    [*for* $j in 1..|cal(A)|$ *do* $d_j arrow norm(bold(x)_("parent") - cal(A)_j)_2$],
    [$j^"*" arrow arg min_(j) d_j$, $quad d_("min") arrow d_(j^"*")$],
  ),
  [*步骤 3：三支去重决策*],
  indent(
    [*if* $d_("min") < tau_("sim")$ *then* — 空间高度相似],
    indent(
      [*if* $J_("parent") < bold(J)_A[j^"*"]$ *then* — 父代更优，替换相似成员],
      indent(
        [$cal(A)_(j^"*") arrow bold(x)_("parent")$, $quad bold(J)_A[j^"*"] arrow J_("parent")$],
      ),
      [*else* — 父代不优于相似成员，直接丢弃],
      indent(
        [不存入档案],
      ),
    ),
    [*else* — 空间足够不相似，父代代表新区域],
    indent(
      [*if* $|cal(A)| < N_A$ *then* — 追加],
      indent(
        [$cal(A) arrow cal(A) union {bold(x)_("parent")}$, $quad bold(J)_A arrow bold(J)_A union {J_("parent")}$],
      ),
      [*else* — 随机替换一项（保留无偏性）],
      indent(
        [$k arrow$ 从 ${1, dots, N_A}$ 中均匀随机采样],
        [$cal(A)_k arrow bold(x)_("parent")$, $quad bold(J)_A[k] arrow J_("parent")$],
      ),
    ),
  ),
  [*return* $cal(A)$, $bold(J)_A$],
) <alg:archive-dedup>

=== 种群威胁度量化模型

TALG 的第二项改进是引入种群威胁度（Population Threat）$T_i in [0, 1]$ 作为个体级障碍物风险的量化指标，替代原生 AL-SHADE 对所有个体无差别对待的变异策略。威胁度 $T_i$ 的核心思想是：将每个候选路径与障碍物集合的空间关系压缩为一个标量——$T_i$ 越大表示该路径越贴近障碍物或已发生碰撞，越需要大尺度变异以跳出危险区域。

*三区威胁映射。* 对于候选路径 $bold(x)_i$，计算每个采样点到全体障碍物表面的最小净空距离。取全路径最小净空 $d_("min", i) = min_(j=1)^100 min_(O in cal(O)) (norm(bold(p)_j - bold(c)_O) - R_O)$，按如下分段函数映射为威胁度：

$
T_i = cases(
  1.0  d_("min", i) <= 0 space "(碰撞区)",
  exp(lr(-frac(2 d_("min", i), d_("safe") - d_("min", i) + epsilon)))0 < d_("min", i) < d_("safe") space "(过渡区)",
  0.0d_("min", i) >= d_("safe") space "(安全区)"
) $ <threat-mapping>

碰撞区（$d_("min") <= 0$）表示路径至少有一处穿透障碍物，威胁度饱和为 1。安全区（$d_("min") >= d_("safe") = 20$ m）表示整条路径距所有障碍物均保持安全距离以上，威胁度归零，算法以纯高斯态进行精细开发。过渡区（$0 < d_("min") < d_("safe")$）是映射设计的关键——采用指数函数 $exp(-2 d_("min") / (d_("safe") - d_("min")))$ 而非简单的线性缩放，原因在于：当 $d_("min")$ 从 $d_("safe")$ 向 0 递减时，分母 $d_("safe") - d_("min")$ 同步缩小，使得指数项加速趋近零，$T_i$ 加速逼近 1。这一非线性特性贴合物理直觉——从距障碍物 20 m 到 10 m 的风险增幅应远小于从 5 m 到 1 m 的风险增幅。

/* *B 样条感知对齐。* 威胁度的计算基于 B 样条曲线采样点而非控制点，这一点至关重要。若直接对控制点计算净空距离，可能出现"控制点均安全、但控制点之间的 B 样条曲线段紧贴障碍物"的漏判情况——这正是原生方法在评估函数与优化变量之间存在表征鸿沟的体现。将 100 个 B 样条采样点投入广播距离计算虽然在单次威胁评估中引入了 $O(100 dot |cal(O)|)$ 的额外开销，但其带来的安全收益——避免优化器产出外观安全实则贴障的虚假可行路径——在障碍物密集场景中远超计算代价。

种群威胁度 $T_i$ 的计算与 B 样条曲线生成紧密耦合——威胁评估的输入并非原始控制点而是光滑采样点，保证了风险评估与路径实际几何的一致性。完整威胁度量化流程见 */#algorithm-ref(<alg:threat-quant>)。

#algorithm(
  title: [种群威胁度量化（ComputePopulationThreat）],
  input: [
    种群矩阵 $bold(X) in bb(R)^(P times 2N)$（$P$ 个个体，每行编码 $N$ 个航点），
    障碍物集合 $cal(O)$，航点数 $N$，起始点 $bold(S)$，目标点 $bold(T)$，
    安全距离 $d_("safe") = 20$，B 样条采样数 $N_s = 100$。
  ],
  output: [威胁度向量 $bold(T) = (T_1, dots, T_P)^T$, $T_i in [0, 1]$。],
  /* [*步骤 1：个体解码与 B 样条路径生成*],
  indent(
    [*for* $i in 1..P$ *do*],
    indent(
      [航点 $bold(W)_i arrow$ 从 $bold(x)_i$ 解码为 $N times 2$ 矩阵],
      [控制点 $bold(C)_i arrow$ 将 $bold(S)$、$bold(W)_i$、$bold(T)$ 纵向拼接为 $(N+2) times 2$ 矩阵],
      [B 样条采样点 $bold(P)_i arrow$ GenerateCubicBSpline$(bold(C)_i, N_s)$],
    ),
  ), */
  [*步骤 1：广播净空距离计算*],
  indent(
    [提取障碍物中心矩阵 $bold(C)_"obs"$ 及半径向量 $bold(R)_"obs"$],
    [*forall* $i$ 同步广播计算：$bold(D)_("surf", i) = norm(bold(P)_i["," * "," "new"] - bold(C)_"obs") - bold(R)_"obs"$],
    [$d_(i)^"min" arrow min bold(D)_("surf", i)$],
  ),
  [*步骤 2：三区威胁度映射*],
  indent(
    [*for* $i in 1..P$ *do*],
    indent(
      [*if* $d_(i)^"min" <= 0$ *then* $T_i arrow 1.0$],
      [*else if* $d_(i)^"min" < d_("safe")$ *then* $T_i arrow exp(lr(-frac(2 d_(i)^"min", d_("safe") - d_(i)^"min" + epsilon)))$],
      [*else* $T_i arrow 0.0$],
    ),
  ),
  [*return* $bold(T)$],
) <alg:threat-quant>

=== 威胁驱动的 Lévy-高斯双态变异缩放因子

在获得个体威胁度 $T_i$ 后，TALG 进一步将其反馈至 DE 变异操作的核心——缩放因子 $F$ 的生成机制。

*原生方法的局限。* 原生 AL-SHADE 对所有个体统一从柯西分布 $F_i ~ "Cauchy"(M_(F, h_i), 0.1)$ 采样 $F$。柯西分布的重尾特性赋予了 $F$ 以低概率取大值（$F ≫ 1$）的可能，理论上可为受困个体提供逃离局部最优的跳跃能力。但其缺陷在于——$F$ 的生成与个体当前所处的环境风险完全解耦。一个已经处于开阔安全区域的个体和一个正紧贴障碍物、即将碰撞的个体，在原生框架中获得大 $F$ 值的概率完全相同。这导致两个低效后果：安全个体偶发的大步长扰动破坏了已有的精细收敛，而危险个体仅凭均匀的低概率重尾难以获得足够的逃逸强度。

*双态采样引擎。* TALG 以个体威胁度 $T_i$ 为切换概率，建立 Lévy-Gaussian 双态 $F$ 生成器。对于个体 $i$，从历史记忆槽 $h_i$ 读取 $F$ 的基准位置参数 $mu_F = M_(F, h_i)$，以尺度参数 $sigma_F = 0.1$ 生成缩放因子：

$
F_i = cases(
  mu_F + sigma_F dot "Lévy"(beta), "if " "rand"(0, 1) <= T_i space "(Lévy 态)",
  mu_F + sigma_F dot cal(N)(0, 1), "otherwise" space "(高斯态)"
) $ <talg-dual-mode>

生成后经边界处理：若 $F_i <= 0$ 则重新采样（最多重试 100 次，极端情况下赋机器精度 $epsilon$ 避免搜索停滞），若 $F_i > 1$ 则截断为 1（$F = 1$ 对应全幅差分跳跃，已为搜索步长上限）。

高斯态（$"rand" > T_i$）遵循经典 SHADE 的 $F$ 采样范式——以正态扰动在均值附近精细调节搜索步长，适用于安全区域（$T_i ≈ 0$，几乎总是进入高斯态）的平滑收敛。Lévy 态（$"rand" <= T_i$）是 TALG 的核心创新——当个体受胁于障碍物时，以概率 $T_i$ 将正态噪声替换为标准 Mantegna Lévy 分布（$beta = 1.5$）的随机步长。Lévy 分布的方差无限且尾部呈幂律衰减（$P(|x| > t) ∝ t^(-beta)$），其产生大幅值步长的概率远高于正态分布和柯西分布——在 $T_i ≈ 1$ 的高威胁场景下，个体约有 30%～40% 的概率获得 $F > 0.8$ 的大幅变异，相比纯柯西采样的约 15% 概率提升了一倍以上。

*Mantegna Lévy 步长生成。* Lévy 分布的随机数通过 Mantegna 算法高效生成。对于稳定指数 $beta in (0, 2)$，构造两个独立正态变量 $u ~ cal(N)(0, sigma_u^2)$ 和 $v ~ cal(N)(0, 1)$，其中：

$
sigma_u = lr(frac(Gamma(1 + beta) sin(pi beta / 2), Gamma((1 + beta) / 2) beta 2^((beta-1)/2)))^(1 / beta) $ <mantegna-sigma>

则 Lévy 步长为 $s = u / |v|^(1/beta)$。$beta = 1.5$ 是群智能优化中的经验折中值——$beta$ 越小则尾部越重、跳跃越激进；$beta$ 越接近 2 则趋近正态分布。为保障数值稳定，分母引入小量 $epsilon = 10^(-12)$ 防止除以零。

*威胁自适应的整体效果。* 双态 $F$ 生成器将环境感知嵌入参数层面，实现了搜索行为的空间自适应调节。在开阔空域，$T_i ≈ 0$，几乎所有个体以高斯态运行，$F$ 紧密围绕历史成功均值 $mu_F$ 取值，变异步长小且稳定，算法进行精细的路径长度和平滑性优化。在障碍物密集走廊，$T_i -> 1$，大量个体进入 Lévy 态，产生大幅度差分跳跃——这种跳跃不仅可能将个体直接弹射至障碍物另一侧的安全区域，还能通过外部档案 $cal(A)$ 的更新将大尺度探索的遗传信息保存下来，间接影响后续代的加权档案均值 $bold(x)_("mean")^t$ 和 current-to-Amean/1 变异策略的搜索方向。高斯态与 Lévy 态的比例由 $T_i$ 连续调节，不存在硬切换边界，保证了搜索行为在"安全-过渡-受胁"全谱系上的平滑过渡。

缩放因子 $F$ 的双态生成与 Mantegna Lévy 采样器的完整流程见#algorithm-ref(<alg:talg-f-gen>) 和 #algorithm-ref(<alg:mantegna-levy>)。

#algorithm(
  title: [TALG 双态 $F$ 生成器（GenerateTALGScalingFactors）],
  input: [
    历史记忆 $bold(M)_F in bb(R)^H$，各体记忆槽索引 $"slots" in {0, dots, H-1}^P$，
    威胁度向量 $bold(T) in [0, 1]^P$，Lévy 稳定指数 $beta = 1.5$，尺度 $sigma = 0.1$，最大重试次数 $R_("max") = 100$。
  ],
  output: [缩放因子向量 $bold(F) = (F_1, dots, F_P)^T$，$F_i in (0, 1]$。],
  [*for* $i in 1..P$ *do*],
  indent(
    [$mu_F arrow bold(M)_F["slots"[i]]$],
    [*for* $"attempt" in 1..R_("max")$ *do*],
    indent(
      [*if* $"rand"(0, 1) <= T_i$ *then* — Lévy 态],
      indent(
        [$s arrow$ MantegnaLevy$(beta)$],
        [$F_i arrow mu_F + sigma dot s$],
      ),
      [*else* — 高斯态],
      indent(
        [$F_i arrow mu_F + sigma dot cal(N)(0, 1)$],
      ),
      [*if* $F_i > 0$ *then* *break*],
    ),
    [*if* $"attempt" = R_("max") + 1$ *then* $F_i arrow epsilon_("machine")$],
    [$F_i arrow min(F_i, 1.0)$],
  ),
  [*return* $bold(F)$],
) <alg:talg-f-gen>

#algorithm(
  title: [Mantegna Lévy 步长采样器（MantegnaLevy）],
  input: [Lévy 稳定指数 $beta in (0, 2)$，标准值 $1.5$。],
  output: [Lévy 分布随机步长 $s$。],
  [计算 $sigma_u arrow lr(frac(Gamma(1 + beta) dot sin(pi beta / 2), Gamma((1 + beta) / 2) dot beta dot 2^((beta - 1) / 2)))^(1 / beta)$],
  [$u arrow cal(N)(0, sigma_u^2)$, $quad v arrow cal(N)(0, 1)$],
  [$s arrow u / (|v|^(1 / beta) + 10^(-12))$],
  [*return* $s$],
) <alg:mantegna-levy>

=== B 样条曲线参数化路径建模

B 样条曲线（B-spline）是一类由控制点和节点向量共同确定的分段多项式参数曲线。与直接连接离散航点形成折线路径不同，B 样条通过控制点间接塑造曲线形态，具有局部支撑、连续性高和端点可约束等优点。对于无人机路径规划而言，该方法能够在优化阶段仍使用有限个控制点作为决策变量，而在适应度评估与轨迹执行阶段生成连续光滑的飞行路径，从而缓解离散航点编码导致的急转、折线和曲率突变问题。

*曲线定义。* 设控制点序列为 $bold(c)_0, bold(c)_1, dots, bold(c)_m in bb(R)^2$，其中 $m+1$ 为控制点数量；设 B 样条次数为 $p$，节点向量为

$
bold(U) = (u_0, u_1, dots, u_(m+p+1))
$ <bspline-knot-vector>

则 $p$ 次 B 样条曲线可表示为

$
bold(C)(u) =
sum_(i=0)^m N_(i,p)(u) bold(c)_i,
quad u in [u_p, u_(m+1)]
$ <bspline-def>

其中 $N_(i,p)(u)$ 为第 $i$ 个 $p$ 次 B 样条基函数。基函数由 Cox-de Boor 递推公式给出。零次基函数定义为

$
N_(i, 0)(u) = cases(
  1, "if " u_i <= u < u_(i+1),
  0, "otherwise"
) $ <bspline-basis0>

当 $r = 1, 2, dots, p$ 时，$r$ 次基函数递推为

$
N_(i,r)(u) =
frac(u - u_i, u_(i+r) - u_i) N_(i,r-1)(u)
+ frac(u_(i+r+1) - u, u_(i+r+1) - u_(i+1)) N_(i+1,r-1)(u)
$ <bspline-basisr>

若递推式中的分母为 0，则对应分式项按 0 处理。该递推关系保证了任一参数 $u$ 处仅有有限个相邻基函数非零，因此单个控制点只影响曲线的一段局部形状。这一局部支撑性使优化器对某个控制点的调整不会破坏整条路径结构，有利于提高变异操作和交叉操作的有效性。

*节点向量构造。* 为使曲线起点和终点分别通过首尾控制点，即满足 $bold(C)(u_p) = bold(c)_0$ 和 $bold(C)(u_(m+1)) = bold(c)_m$，本文采用 clamped 均匀节点向量。其基本思想是在节点向量两端分别设置 $p+1$ 个重复节点，使曲线端点被控制点“夹紧”；中间节点则均匀分布，以保证参数区间划分均衡。节点向量可写为

$
u_i = cases(
  0, 0 <= i <= p,
  frac(i - p, m - p + 1), p < i <= m,
  1, m < i <= m + p + 1
) $ <clamped-knot>

其中 $i = 0, 1, dots, m+p+1$。本文采用三次 B 样条，即 $p = 3$。三次 B 样条在普通节点处具有 $C^2$ 连续性，可以保证路径位置、切向方向和曲率变化较为平滑，更符合无人机受速度、加速度和航向角速度约束的实际飞行特性。

*de Boor 递推求值。* 在实际计算中，若直接根据基函数定义计算 $bold(C)(u)$，需要显式求取多个基函数，计算过程较为繁琐。本文采用 de Boor 算法对 B 样条曲线进行稳定求值。给定参数 $u$，首先确定其所在节点区间 $[u_s, u_(s+1))$，即

$
u_s <= u < u_(s+1), quad s in {p, p+1, dots, m}
$ <deboor-span>

随后取受该参数影响的 $p+1$ 个控制点，并初始化

$
bold(d)_j^((0)) = bold(c)_(s - p + j),
quad j = 0, 1, dots, p
$ <deboor-init>

第 $r$ 轮递推（$r = 1, 2, dots, p$）按如下线性插值进行：

$
bold(d)_j^((r)) =
(1 - alpha_(j,r)) bold(d)_(j-1)^((r-1))
+ alpha_(j,r) bold(d)_j^((r-1))
$ <deboor-recur>

其中

$
alpha_(j,r) =
frac(u - u_(s - p + j), u_(s + 1 + j - r) - u_(s - p + j)),
quad j = r, r+1, dots, p
$ <deboor-alpha>

完成 $p$ 轮递推后，曲线点由

$
bold(C)(u) = bold(d)_p^((p))
$ <deboor-result>

得到。de Boor 算法只对与当前参数相关的局部控制点进行递推，单点求值复杂度为 $O(p^2)$。由于本文固定采用三次 B 样条，$p$ 为常数，因此该求值过程在路径采样中具有较好的计算效率和数值稳定性。

*路径重构与适应度评估。* 在 TALG 框架中，优化器仍以实数向量形式维护候选解。设个体 $bold(x)_i in bb(R)^(2N)$ 解码后得到 $N$ 个中间控制点，将其与起点 $bold(S)$ 和终点 $bold(T)$ 拼接，可得到完整控制点集合

$
cal(C)_i = {bold(S), bold(c)_1, bold(c)_2, dots, bold(c)_N, bold(T)}
$ <bspline-control-set>

随后在参数区间 $[u_p, u_(m+1)]$ 上均匀选取 $N_s$ 个参数值，并利用 de Boor 算法生成采样路径

$
cal(P)_i = {bold(C)(u_1), bold(C)(u_2), dots, bold(C)(u_(N_s))}
$ <bspline-sampled-path>

本文取 $N_s = 100$。适应度函数中的路径长度 $L$、平滑性 $S$、综合风险 $R$、通道宽度惩罚 $W$ 和编队保持代价 $F$ 均基于采样点集合 $cal(P)_i$ 计算，而不再直接基于折线控制点计算。这样可以保证算法评价的是实际将要执行的光滑轨迹，而不是控制点折线的近似形态。

该参数化方式具有两方面优势。首先，B 样条曲线天然具备连续性，能够显著降低折线路径中的急转和曲率突变，使路径更符合无人机飞行约束。其次，由于每个控制点只影响有限的局部曲线段，优化器对单一控制点的调整不会造成全局路径剧烈震荡，有助于提升差分变异和交叉操作生成可接受试验向量的概率。

下面给出 B 样条路径参数化的完整流程，见#algorithm-ref(<alg:bspline-gen>)。

#pagebreak()
#algorithm(
  title: [三次 B 样条曲线生成与路径参数化],
  input: [
    控制点集 $cal(C) = {bold(c)_0, bold(c)_1, dots, bold(c)_m}$（含起点 $bold(S)$ 与终点 $bold(T)$），
    采样点数 $N_s = 100$，曲线次数 $p = 3$。
  ],
  output: [平滑路径采样点 $cal(P) = {bold(p)_1, dots, bold(p)_(N_s)}$。],
  [*步骤 1：控制点预处理*],
  indent(
    [去除 $cal(C)$ 中连续重复点（容差 $10^(-6)$）],
    [*if* $|cal(C)| <= 1$ *then* *return* $cal(C)$],
    [$n_"ctrl" arrow |cal(C)|$, $quad "degree" arrow min(3, n_"ctrl" - 1)$],
  ),
  [*步骤 2：构造 Clamped 均匀节点向量*],
  indent(
    [$n arrow n_"ctrl" - 1$],
    [$"interior" arrow$ 在 $(0, 1)$ 内均匀插入 $n - "degree"$ 个内部节点],
    [$bold(U) arrow underbrace({0, dots, 0}, "degree"+1) dot "interior" dot underbrace({1, dots, 1}, "degree"+1)$],
  ),
  [*步骤 3：de Boor 递推采样*],
  indent(
    [$bold(P) arrow$ 空数组],
    [*for* $u$ *in* $"linspace"(U_("degree"), U_(n_"ctrl"), N_s)$ *do*],
    indent(
      [*步骤 3a：定位节点区间*],
      [*if* $u >= U_(n+1)$ *then* $k_"span" arrow n$],
      [*else* $k_"span" arrow$ 满足 $U_(k_"span") <= u < U_(k_"span"+1)$ 的最大下标（限定在 $["degree", n]$ 内）],
      [*步骤 3b：初始化受影响控制点*],
      [$bold(d)_j arrow bold(c)_(k_"span" - "degree" + j)$, $quad j = 0, 1, dots, "degree"$],
      [*步骤 3c：递推插值（r = 1→degree）*],
      [*for* $r in 1.."degree"$ *do*],
      indent(
        [*for* $j in "degree" .. r$ (降序) *do*],
        indent(
          [$"left" arrow U_(j + k_"span" - "degree")$, $quad "right" arrow U_(j + 1 + k_"span" - r)$],
          [$alpha arrow 0$ *if* $"right" - "left" < 10^(-12)$ *else* $(u - "left") / ("right" - "left")$],
          [$bold(d)_j arrow (1 - alpha) bold(d)_(j-1) + alpha bold(d)_j$],
        ),
      ),
      [将 $bold(d)_("degree")$ 追加至 $bold(P)$],
    ),
  ),
  [*步骤 4：锚定首尾端点*],
  indent(
    [$bold(P)[0] arrow bold(c)_0$, $quad bold(P)[-1] arrow bold(c)_m$],
  ),
  [*return* $bold(P)$],
) <alg:bspline-gen>

*降维效应。* B 样条参数化还可以在保持路径表达能力的同时降低搜索难度。经典航点编码中，若设置 $N = 20$ 个中间航点，则优化维度为 $D = 40$。若采用少量控制点描述曲线主体形态，则可将需要优化的自由控制点减少至约 $5 ~ 7$ 个，对应搜索维度降至 $10 ~ 14$。较低维度下，差分向量 $(bold(x)_(r_1) - bold(x)_(r_2))$ 的有效信息更加集中，交叉操作也更容易保留有意义的路径结构。因此，B 样条不仅改善了路径平滑性，也降低了 AL-SHADE 在高维航点空间中搜索的困难程度。

=== TALG 框架参数配置总览

综合上述三项改进，TALG 框架在原生 AL-SHADE 基础上引入的参数变化可归纳如下。历史记忆容量由 $H = 6$ 增至 $H = 15$——更大的记忆体为双态 $F$ 生成器中的高斯态与 Lévy 态分别保留统计空间，避免两种分布的历史信息在有限槽位中相互干扰。档案比率由 $r_("arc") = 2.5$ 微增至 2.6，以补偿去重机制剔除的冗余解导致的档案规模下降。精英比例由 $p_("best") = 0.10$ 提升至 0.15，增强精英引导在 B 样条平滑路径搜索中的骨干作用。三项新增参数——相似度阈值 $tau_("sim") = 25.0$、Lévy 稳定指数 $beta = 1.5$、B 样条次数 $p = 3$ 与采样密度 100——均具备明确的物理或数学含义，无需针对不同障碍物布局进行二次标定。



= 实验结果分析

== 实验设置与环境配置

=== 仿真环境与障碍物布局

实验构建了 $1000 times 1000$ m 的二维仿真空域，无人机集群的起始点 $bold(S) = (0, 0)$，目标点 $bold(T) = (1000, 1000)$。障碍物集合包含 21 个圆形障碍物，分为两类：（1）13 个沿主对角线 $y = x$ 两侧以 120 m 偏距交替排布的结构化障碍物（半径 $R = 35$ m），模拟城市低空走廊的规整建筑群；（2）8 个在 $(100, 900) times (100, 900)$ 区域内均匀随机生成的散落障碍物（半径 $R = 35$ m），模拟非结构化动态环境中的未知小尺度障碍。此外在 $(500, 500)$ 处设置一个中心障碍物，构成全局最难穿越的"瓶颈"区域——在此处，起点-终点直连线两侧各只有约 60 m 的狭窄安全通道，对路径规划算法的探索-开发平衡能力构成核心考验。

所有障碍物为静态（$bold(v)_("obs") = bold(0)$），编队由 5 架无人机构成（1 架虚拟领航者 + 4 架僚机），基础队形为 V 型（偏移量：$(-20, 20)$、$(-20, -20)$、$(-40, 40)$、$(-40, -40)$ m）。仿真步长固定为 $Delta t = 0.1$ s，单次实验最长仿真时间 $T_("max") = 100$ s。

=== 对比算法与参数配置

实验涉及七类全局路径规划算法，各算法统一采用 $N = 20$ 航点编码和五分量加权适应度函数 $J = 0.24 L_"norm" + 0.18 S_"norm" + 0.32 R_"norm" + 0.14 W_"norm" + 0.12 F_"norm"$，局部避障层统一采用改进 APF（参数 $k_r = 25$、$k_t = 15$、$d_("safe") = 20$ m），编队控制层统一采用第 2 章所述改进一致性协议（$k_1 = 0.8$、$k_2 = 1.2$、$k_3 = 0.8$）。各算法的种群规模与迭代次数对齐为 $P_"init" = 500$、$T = 500$（AHA 因访问表开销取 $n = 50$），其余参数沿用各算法原始文献的标准推荐值。涉及的算法配置如下：

#captab(
  caption: [对比算法参数配置一览],
  label: <exp-algo-config>,
)[
  | 算法            | 种群/迭代  | 类别             | 关键参数                                        |
  | PSO             | 500 / 500  | 经典群体智能     | $omega = 0.6$, $c_1 = 1.6$, $c_2 = 1.4$          |
  | ACOR            | 500 / 500  | 经典群体智能     | $K = 20$, $q = 0.1$, $xi = 0.85$                 |
  | GA              | 500 / 500  | 经典进化算法     | $p_m = 0.1$, $sigma_0 = 40$, $k = 3$             |
  | MSA             | 500 / 500  | 新型群体智能     | $P = 2$, $rho = 6$, $a = 0.5$, $P_c = 0.2$       |
  | AHA             | 50 / 500   | 新型群体智能     | 无外部控制参数                                  |
  | AL-SHADE (原生) | 500 / 500  | 自适应 DE        | $H = 6$, $p_("best") = 0.10$, $r_("arc") = 2.5$  |
  | AL-SHADE-TALG-QU | 500 / 500 | 改进自适应 DE    | $H = 15$, $p_("best") = 0.15$, $r_("arc") = 2.6$, $tau_("sim") = 25$, $beta = 1.5$ |
]

=== 评价指标体系

实验从五个维度对算法性能进行量化评价：

（1）*收敛性能指标*：最终适应度 $J^"*"$ 与首次达到最终改进幅度 95% 所需的迭代次数 $T_("95%")$，用于刻画算法的寻优速度和收敛水平。

（2）*任务效率指标*：平均路径长度 $L$（m）与领航者到达时间 $t_("arr")$（s），用于衡量算法生成路径的飞行经济性和任务完成效率。

（3）*轨迹平滑性指标*：平均航向偏转角（°），用于衡量路径转向是否平缓。该指标越小，说明路径几何连续性越好，越有利于无人机稳定跟踪。

（4）*编队保持指标*：相对队形误差 RMS（m），用于衡量僚机相对期望队形的偏离程度。该指标越小，说明编队稳定性越好。

（5）*集群安全指标*：最小无人机间距（m），用于衡量多机飞行过程中的内部安全裕度。该指标越大，说明无人机之间发生近距离冲突的风险越低。

所有算法在相同障碍物布局、相同编队控制器和相同局部避障参数下运行，并对上述指标进行统一统计和对比。

== 路径规划可视化对比

=== 全局路径形态对比

下图展示了七种全局规划算法与改进 APF 局部避障器联合运行后的终态轨迹。图中黑色虚线为全局规划路径，红色曲线为领航者实际轨迹，其余彩色曲线为四架僚机轨迹，蓝色圆形区域表示障碍物及其安全缓冲区。左上角标注给出了各算法的最终到达时间、最小无人机间距和碰撞状态。

#capsubfig(
  (
    (content: image("figures/lujing/final_snapshot_pso_apf.png", height: 5.2cm, fit: "contain"), subcaption: [PSO + APF]),
    (content: image("figures/lujing/final_snapshot_aco_apf.png", height: 5.2cm, fit: "contain"), subcaption: [ACOR + APF]),
    (content: image("figures/lujing/final_snapshot_ga_apf.png", height: 5.2cm, fit: "contain"), subcaption: [GA + APF]),
    (content: image("figures/lujing/final_snapshot_msa_apf.png", height: 5.2cm, fit: "contain"), subcaption: [MSA + APF]),
    (content: image("figures/lujing/final_snapshot_aha_apf.png", height: 5.2cm, fit: "contain"), subcaption: [AHA + APF]),
    (content: image("figures/lujing/final_snapshot_alshade_origin_apf.png", height: 5.2cm, fit: "contain"), subcaption: [AL-SHADE + APF]),
    (content: image("figures/lujing/final_snapshot_alshade_talg2_apf.png", height: 5.2cm, fit: "contain"), subcaption: [AL-SHADE-TALG-QU + APF]),
  ),
  columns: 2,
  caption: [七种算法联合 APF 局部避障后的终态轨迹对比],
  label: <exp-path-compare>,
)

从整体轨迹看，七种算法均能在 APF 局部避障器辅助下到达目标点，且图中碰撞状态均为 No，说明局部避障层能够对全局规划路径进行有效安全修正。但是，不同全局路径的质量直接影响 APF 的介入程度和编队轨迹形态。若全局路径在障碍物密集区过于贴近障碍物，僚机轨迹会出现更明显的横向偏移和队形拉伸；若全局路径本身较平滑且保留足够通行空间，则领航者与僚机轨迹更容易保持平行一致。

PSO、GA、MSA 和 AHA 的终态轨迹整体较为接近，均沿主对角线方向穿越障碍物分布区，并在中段绕开多个圆形障碍物。其中 PSO 的最终时间为 77.70 s，最小无人机间距为 19.11 m；GA 的最终时间为 81.00 s，最小无人机间距为 19.77 m；MSA 和 AHA 的最终时间均为 77.60 s，最小无人机间距分别为 18.79 m 和 19.83 m。可以看出，这几类算法均能够完成任务，但中段和终点附近的僚机轨迹存在一定程度的队形展开，说明局部避障修正对编队结构仍产生了影响。

ACOR 的轨迹最为曲折，最终到达时间为 86.50 s，是七种算法中最长的一类。其全局路径在中部区域出现明显弯折，导致领航者和僚机在多个障碍物之间产生额外绕行，虽然最终未发生碰撞，但任务效率相对较低，且最小无人机间距仅为 18.12 m。该现象与前文五指标中 ACOR 路径长度最长、到达时间最长的结果一致。

原生 AL-SHADE 的终态轨迹最短、到达最快，最终时间为 70.60 s。但从图中可以看出，其全局路径在若干障碍物之间选择了较直接的穿越方式，僚机轨迹在局部区域需要依靠 APF 进行一定横向修正，最终最小无人机间距为 18.88 m。AL-SHADE-TALG-QU 的最终时间为 73.00 s，略慢于原生 AL-SHADE，但其最小无人机间距提升至 20.38 m，是图示结果中最高的安全裕度。与此同时，AL-SHADE-TALG-QU 的全局路径和领航者轨迹更加平滑，僚机轨迹之间保持较稳定的相对间隔，体现出 B 样条参数化和威胁感知机制对轨迹连续性与编队安全性的改善作用。

综合而言，路径可视化结果与五项指标对比相互印证：原生 AL-SHADE 在任务效率上具有优势，AL-SHADE-TALG-QU 则在保持较短路径和较快到达的同时，进一步提升了最小机间距和编队轨迹一致性，更适合安全裕度要求较高的无人机集群任务。


== 收敛性分析

=== 迭代收敛曲线

下图展示了七种全局路径规划算法在 500 次迭代过程中的最优适应度收敛曲线。纵轴为当前最优适应度值 $J$，数值越小表示候选路径的综合代价越低；横轴为迭代次数。由图可见，各算法在迭代初期均表现出明显的快速下降趋势，说明初始随机种群能够在较短时间内排除大量高代价路径；随着迭代推进，曲线逐渐趋于平缓，算法进入局部精细搜索或停滞阶段。

#capsubfig(
  (
    (content: image("figures/pso_fitness_convergence.png", width: 92%), subcaption: [PSO]),
    (content: image("figures/aco_fitness_convergence.png", width: 92%), subcaption: [ACOR]),
    (content: image("figures/ga_fitness_convergence.png", width: 92%), subcaption: [GA]),
    (content: image("figures/msa_fitness_convergence.png", width: 92%), subcaption: [MSA]),
    (content: image("figures/aha_fitness_convergence.png", width: 92%), subcaption: [AHA]),
    (content: image("figures/alshade_fitness_convergence.png", width: 92%), subcaption: [AL-SHADE]),
    (content: image("figures/alshade_talg2_fitness_convergence.png", width: 60%), subcaption: [AL-SHADE-TALG-QU]),
  ),
  columns: 2,
  caption: [七种算法的适应度收敛曲线],
  label: <exp-convergence>,
)

从收敛行为中可归纳出三种典型模式。

*快速收敛型（PSO、ACOR、GA）。* PSO、ACOR 和 GA 在前 30 代内均完成了主要的适应度下降。其中，PSO 下降最为迅速，约在 20 代左右即接近最终稳定值，说明全局最优粒子的社会学习项对种群具有较强牵引作用；ACOR 同样在初期快速下降，但约 30 代后基本停滞，最终收敛值高于 PSO，表明其连续高斯核采样在后期精细开发能力不足；GA 前期依靠选择压力和高斯变异迅速降低代价，但后期曲线趋于平直，说明种群逐渐集中于有限的可行区域，进一步改进空间较小。

*稳步改进型（MSA、AHA、AL-SHADE）。* MSA、AHA 与原生 AL-SHADE 的收敛过程相对更平滑，前期下降后仍能在较长迭代区间内保持小幅改进。MSA 在约 50 代后逐渐稳定，最终适应度约为 6.31；AHA 曲线呈阶梯式下降，说明访问表机制和迁徙觅食策略能够周期性地引入新搜索方向，使算法在中后期仍保留一定探索能力；AL-SHADE 的下降过程较为连续，约在 60 代后接近稳定，外部档案与自适应参数记忆机制使其后期仍具有微弱改进能力。

*慢收敛或高平台型（AL-SHADE-TALG-QU）。* AL-SHADE-TALG-QU 在前期同样能够快速降低适应度，但其后续下降速度明显慢于 PSO、GA 和 MSA，约在 200 代后进入平台期，最终适应度约为 8.85。该结果表明，在当前参数配置和适应度权重下，改进算子并未直接带来更低的适应度值。可能原因是 B 样条平滑、威胁驱动变异和档案去重机制更倾向于提高路径安全裕度与几何平滑性，而非单纯压低加权适应度；若权重设置中过度强调路径长度或短期代价，则该类保守搜索策略可能表现为收敛值偏高。因此，对 AL-SHADE-TALG-QU 的评价不应仅依赖适应度曲线，还应结合安全间隙、路径平滑性和编队可行性等指标综合判断。

=== 收敛速度定量比较

下表汇总了七种算法的关键收敛指标。由于本节曲线为图像结果而非原始数据文件，表中数值根据坐标轴读取得到，主要用于比较不同算法的收敛趋势和相对水平。

#captab(
  caption: [算法收敛性能定量对比（由适应度曲线近似读取）],
  label: <exp-convergence-table>,
)[
  | 算法                 | 初始 $J$（约） | 最终 $J^"*"$（约） | $T_("95%")$（约/代） | 收敛特征         |
  | PSO                  | 28             | 6.05               | 20                   | 极快下降-早停滞  |
  | ACOR                 | 33             | 6.92               | 20                   | 快速下降-平台较高 |
  | GA                   | 31             | 6.52               | 30                   | 快速下降-缓慢修正 |
  | MSA                  | 31             | 6.31               | 50                   | 稳步下降-平台稳定 |
  | AHA                  | 31             | 6.35               | 90                   | 阶梯下降-持续探索 |
  | AL-SHADE (原生)      | 31             | 6.37               | 65                   | 连续下降-后期微调 |
  | AL-SHADE-TALG-QU     | 31             | 8.85               | 100                  | 下降较慢-平台偏高 |
]

从最终适应度看，PSO 的收敛值最低，约为 6.05；MSA、AHA、AL-SHADE 和 GA 的最终结果集中在 6.31 至 6.52 之间，差异相对较小；ACOR 的最终值约为 6.92，略高于上述算法；AL-SHADE-TALG-QU 的最终值约为 8.85，在单纯适应度指标上表现不占优。由此可见，若仅以加权适应度 $J$ 作为评价标准，PSO 在本组实验中具有最快收敛速度和最低最终代价。然而，PSO 的快速收敛也意味着其后期搜索多样性较弱，可能更容易受到局部区域和路径安全裕度的限制；AHA、MSA 和 AL-SHADE 虽然下降速度略慢，但中后期仍具备一定持续改进能力。对于 AL-SHADE-TALG-QU，需要进一步结合路径平滑性、安全裕度和编队保持指标进行综合评价，以判断其改进机制是否在非适应度维度上产生收益。

== 算法性能定量对比

为避免仅依据适应度曲线评价算法优劣，本文进一步从飞行任务执行效果出发，选取五个具有直接物理意义的评价指标进行横向比较：平均路径长度、领航者到达时间、路径平滑性、相对队形误差以及最小无人机间距。其中，平均路径长度和到达时间反映任务效率，路径平滑性反映轨迹可执行性，相对队形误差反映编队保持能力，最小无人机间距反映集群内部安全裕度。除最小无人机间距外，其余四项指标均为数值越小越优；最小无人机间距越大，说明机间安全裕度越充分。

#capsubfig(
  (
    (content: image("figures/zhibiao/path_length1.png", width: 92%), subcaption: [平均路径长度]),
    (content: image("figures/zhibiao/arrival_time1.png", width: 92%), subcaption: [领航者到达时间]),
    (content: image("figures/zhibiao/smoothness1.png", width: 92%), subcaption: [路径平滑性]),
    (content: image("figures/zhibiao/formation1_error.png", width: 92%), subcaption: [相对队形误差]),
    (content: image("figures/zhibiao/min_distance1.png", width: 92%), subcaption: [最小无人机间距]),
  ),
  columns: 2,
  caption: [七种算法在五个飞行性能指标上的对比],
  label: <exp-five-metrics>,
)

五项指标的具体数值汇总如表@exp-five-metrics-table 所示。为便于阅读，表中用向下箭头表示数值越小越优，用向上箭头表示数值越大越优。

#captab(
  caption: [七种算法五项性能指标对比],
  label: <exp-five-metrics-table>,
)[
  | 算法 | 路径 (m) ↓ | 时间 (s) ↓ | 偏转 (°) ↓ | 队形 RMS (m) ↓ | 机间距 (m) ↑ |
  | PSO（粒子群） | 1646.43 | 77.70 | 0.937 | 9.977 | 19.48 |
  | ACOR（蚁群） | 1805.31 | 86.00 | 1.062 | 9.271 | 18.06 |
  | GA（遗传） | 1684.37 | 80.90 | 0.930 | 9.274 | 19.59 |
  | MSA | 1640.24 | 77.00 | 0.908 | 9.154 | 18.59 |
  | AHA | 1659.34 | 77.50 | 0.952 | 9.395 | 18.74 |
  | AL-SHADE | 1540.46 | 71.30 | 0.835 | 9.214 | 19.12 |
  | AL-SHADE-TALG-QU | 1543.71 | 72.60 | 0.796 | 7.989 | 20.47 |
]

从任务效率看，AL-SHADE 与 AL-SHADE-TALG-QU 的平均路径长度明显短于其他算法，分别为 1540.46 m 和 1543.71 m，二者差异仅为 3.25 m，说明改进算法在引入平滑和安全约束后并未显著牺牲路径长度。领航者到达时间方面，AL-SHADE 最短，为 71.30 s；AL-SHADE-TALG-QU 为 72.60 s，仅比 AL-SHADE 增加 1.30 s，仍明显优于 PSO、ACOR、GA、MSA 和 AHA。由此可见，改进算法在保持较高飞行效率的同时，为路径平滑性和编队安全性预留了更充分的优化空间。

从轨迹平滑性看，AL-SHADE-TALG-QU 的平均航向偏转最低，仅为 0.796°，优于 AL-SHADE 的 0.835°，也显著低于 ACOR 的 1.062° 和 AHA 的 0.952°。这说明 B 样条曲线参数化路径建模能够有效削弱离散航点连接造成的局部折角，使领航者轨迹更加连续。对于无人机集群而言，更小的航向偏转不仅能够降低飞行控制过程中的加速度突变，也有利于僚机在跟随过程中维持稳定队形。

从编队保持效果看，AL-SHADE-TALG-QU 的相对队形误差 RMS 为 7.989 m，是七种算法中最低的结果。相比原生 AL-SHADE 的 9.214 m，误差降低约 13.30%；相比 PSO 的 9.977 m，误差降低约 19.93%。该结果表明，改进算法生成的路径对编队控制层更加友好，能够减少局部急转和通道压缩对队形结构的扰动，使僚机在跟随领航者时保持更稳定的相对位置。

从安全性看，AL-SHADE-TALG-QU 的最小无人机间距达到 20.47 m，为所有算法中最大，说明其在整个飞行过程中保留了最充分的机间安全裕度。相比之下，ACOR 和 MSA 的最小机间距分别为 18.06 m 和 18.59 m，安全裕度相对较弱；AL-SHADE 虽然路径较短、到达时间最优，但最小机间距为 19.12 m，低于改进算法。综合来看，AL-SHADE-TALG-QU 在路径长度与到达时间上接近原生 AL-SHADE，同时在平滑性、队形误差和机间安全距离三个关键指标上取得最优结果，体现出更好的综合飞行性能。

== 五指标综合评价

为进一步比较各算法的整体表现，本文对五项指标进行名次统计。路径长度、到达时间、平均航向偏转和队形误差按数值从小到大排序，最小无人机间距按数值从大到小排序。单项排名越靠前，说明该算法在对应指标上表现越优；平均排名越小，说明算法综合性能越好。

#captab(
  caption: [七种算法五项指标排名统计],
  label: <exp-rank-table>,
)[
  | 算法 | 路径长度排名 | 到达时间排名 | 平滑性排名 | 队形误差排名 | 机间距排名 | 平均排名 |
  | PSO | 4 | 5 | 5 | 7 | 3 | 4.8 |
  | ACOR | 7 | 7 | 7 | 4 | 7 | 6.4 |
  | GA | 6 | 6 | 4 | 5 | 2 | 4.6 |
  | MSA | 3 | 3 | 3 | 2 | 6 | 3.4 |
  | AHA | 5 | 4 | 6 | 6 | 5 | 5.2 |
  | AL-SHADE | 1 | 1 | 2 | 3 | 4 | 2.2 |
  | AL-SHADE-TALG-QU | 2 | 2 | 1 | 1 | 1 | 1.4 |
]

由表@exp-rank-table 可知，AL-SHADE-TALG-QU 的平均排名为 1.4，在七种算法中综合最优。其路径长度和到达时间均排名第二，与原生 AL-SHADE 的差距很小；同时在路径平滑性、队形误差和最小机间距三项指标上均排名第一，说明改进算法的优势主要体现在轨迹质量、编队稳定性和安全裕度方面。原生 AL-SHADE 的平均排名为 2.2，综合表现次优，其突出特点是任务效率最高，但在队形保持和机间安全方面不及 AL-SHADE-TALG-QU。MSA 的平均排名为 3.4，整体表现较均衡；GA 和 PSO 分别在最小机间距或收敛速度方面具有一定优势，但综合排名落后于 MSA、AL-SHADE 和改进算法。ACOR 在本组指标中的平均排名最高，说明其在当前场景下不适合作为复杂障碍物环境中的首选规划算法。

== 综合分析与讨论

=== 算法选型建议

基于以上实验结果，针对不同应用场景的算法选型建议如下：

（1）*效率优先场景*：若任务目标主要关注路径长度和到达时间，可优先考虑原生 AL-SHADE。其平均路径长度为 1540.46 m，到达时间为 71.30 s，均为七种算法中的最优结果，适合对飞行时间敏感且环境安全裕度要求相对可控的任务。

（2）*安全与编队稳定优先场景*：推荐 AL-SHADE-TALG-QU。该算法的最小无人机间距达到 20.47 m，队形误差 RMS 降至 7.989 m，均为本组实验最优；同时其平均航向偏转仅为 0.796°，说明生成路径更加平滑，能够有效降低局部避障和编队控制层的调整压力。

（3）*综合均衡场景*：推荐 AL-SHADE-TALG-QU 或 MSA。AL-SHADE-TALG-QU 在五项指标的平均排名最低，适合作为复杂环境下的综合优选方案；MSA 在路径长度、到达时间、平滑性和队形误差上均处于中上水平，可作为结构相对简单、实现复杂度适中的备选算法。

（4）*快速收敛或基准对比场景*：PSO 可作为基础对比算法。其适应度曲线收敛速度最快，且最小机间距排名第三，但在队形误差和路径平滑性方面不占优势。因此，PSO 更适合用于算法基准对照或对实时性要求高、对轨迹品质要求相对较低的场景。

=== 局限性与未来方向

尽管实验结果整体验证了 AL-SHADE-TALG-QU 在多指标综合性能上的优势，但仍存在以下局限：（1）本文主要基于单组典型障碍物环境进行对比，尚未在不同障碍物密度、不同通道宽度和不同集群规模下系统验证算法排名的稳定性；（2）B 样条控制点数、采样密度和曲线次数对路径质量的影响仍需进一步分析；（3）威胁度阈值 $d_("safe")$、相似度阈值 $tau_("sim")$ 以及 Lévy 稳定指数 $beta$ 主要依据经验设定，参数自适应能力仍有提升空间；（4）当前实验环境为静态二维障碍物，面向三维动态障碍物和真实低空空域的扩展仍需进一步研究。

= 总结与展望

== 工作总结

本文围绕复杂多障碍环境下无人机集群路径规划与协同飞行问题，构建了由全局路径规划、编队控制和局部避障共同组成的分层耦合框架。该框架以全局规划算法生成任务级参考路径，以改进一致性控制方法维持多机编队结构，并在执行阶段引入改进人工势场法进行在线安全修正，从而兼顾路径全局性、飞行安全性、队形稳定性和局部避障实时性。

在编队控制方面，本文以虚拟领航者为核心建立无人机集群协同模型，通过邻接矩阵与拉普拉斯矩阵描述通信拓扑关系，并在一致性控制律中引入通信延迟、速度跟踪和横向偏置调节机制。该方法能够使僚机在跟随领航者运动的同时保持预设相对位置，并在通道收缩或障碍物绕行过程中实现队形的适度弹性调整，为后续全局路径规划与局部避障提供了稳定的集群运动基础。

在全局路径规划方面，本文首先构建了统一的适应度函数，将路径长度、路径平滑性、静态障碍风险、集群内部碰撞风险、通信风险、通道宽度约束和编队保持代价纳入同一评价框架。随后对 PSO、ACOR、GA、MSA、AHA 和 AL-SHADE 等算法进行了系统建模与对比分析。针对原生 AL-SHADE 在路径连续性、环境风险感知和外部档案多样性方面的不足，本文进一步提出 AL-SHADE-TALG-QU 改进框架：通过外部档案相似度去重减少冗余历史解，通过种群威胁度驱动的 Lévy-Gaussian 双态缩放因子增强障碍物密集区域的跳出能力，并通过 B 样条曲线参数化路径建模提升轨迹连续性和平滑性。

在局部避障方面，本文在经典人工势场法基础上引入切向旋转势场力，使无人机在接近障碍物时不仅受到径向排斥作用，还能够获得沿障碍物边缘绕行的切向引导。该改进缓解了传统人工势场法在障碍物前方易出现局部极小值和震荡的问题，并与全局规划层输出的参考路径形成互补：全局路径提供远距离引导，局部避障则负责短时域内的安全修正。

== 主要结论

通过二维多障碍环境下的收敛曲线对比和五项飞行性能指标分析，本文得到以下结论。

第一，从适应度收敛曲线看，PSO 的收敛速度最快，并取得最低的最终适应度值，说明其全局最优引导机制在当前加权适应度函数下具有较强的快速寻优能力。然而，PSO 在路径平滑性和队形误差指标上并不占优，表明单纯适应度值较低并不必然意味着集群飞行性能最优。

第二，从任务效率看，原生 AL-SHADE 在平均路径长度和领航者到达时间两项指标上表现最佳，路径长度为 1540.46 m，到达时间为 71.30 s。AL-SHADE-TALG-QU 的路径长度为 1543.71 m，到达时间为 72.60 s，与原生 AL-SHADE 接近，说明改进算法在提升轨迹质量和安全裕度的同时，基本保持了较高的飞行效率。

第三，从轨迹平滑性和编队保持效果看，AL-SHADE-TALG-QU 取得最优结果。其平均航向偏转为 0.796°，低于原生 AL-SHADE 的 0.835°；相对队形误差 RMS 为 7.989 m，相比原生 AL-SHADE 的 9.214 m 降低约 13.30%。这说明 B 样条参数化和威胁感知机制能够有效改善路径几何连续性，并降低僚机跟随过程中的队形扰动。

第四，从集群安全性看，AL-SHADE-TALG-QU 的最小无人机间距为 20.47 m，是七种算法中的最大值，说明其能够为多机飞行提供更充分的内部安全裕度。相比之下，ACOR 和 MSA 的最小机间距分别为 18.06 m 和 18.59 m，安全裕度相对较弱。

第五，综合五项指标排名，AL-SHADE-TALG-QU 的平均排名为 1.4，优于原生 AL-SHADE 的 2.2 和 MSA 的 3.4，表现出最佳的综合飞行性能。该结果表明，改进算法虽然在单纯适应度收敛曲线上并非最优，但在路径平滑性、队形保持和安全裕度等实际飞行指标上具有明显优势，更适合复杂障碍物环境下的无人机集群协同路径规划任务。


== 研究展望

尽管本文方法在二维静态障碍物环境下取得了较好的规划效果，但仍存在进一步完善的空间。后续研究可从以下几个方面展开。

第一，扩展至三维复杂空域与动态障碍物场景。本文主要针对二维静态障碍物环境展开研究，而实际低空空域中常存在高度约束、运动障碍物、临时禁飞区以及风场扰动等因素。未来可将 B 样条路径建模扩展至 $bb(R)^3$ 空间，并结合动态障碍物预测模型构建时空风险场，使规划算法能够同时处理空间避障和时间避碰问题。

第二，进一步研究 B 样条控制点数量与优化维度之间的关系。本文采用 B 样条参数化提升路径连续性，但控制点数量、曲线次数和采样密度对搜索效率与路径质量的影响仍需系统分析。后续可通过消融实验确定不同障碍物密度下的最优控制点规模，并研究自适应增删控制点机制，使曲线表达能力能够随环境复杂度动态调整。

第三，提升威胁度模型与参数选择的自适应能力。当前威胁度阈值 $d_("safe")$、相似度阈值 $tau_("sim")$ 以及 Lévy 稳定指数 $beta$ 主要依据经验设定。未来可引入贝叶斯优化、强化学习或在线参数估计方法，使算法根据障碍物密度、通道宽度和种群分布状态自适应调整关键参数，降低人工调参依赖。

第四，面向实时重规划与机载部署开展轻量化研究。AL-SHADE-TALG-QU 在路径平滑性和安全裕度方面具有优势，但 B 样条采样、威胁度计算和档案去重仍会增加一定计算负担。后续可从并行距离计算、增量式 B 样条更新、档案压缩存储和 GPU 加速等方面降低计算开销，并研究全局规划与局部避障之间的事件触发式重规划机制。

第五，结合真实无人机平台进行半实物或实飞验证。本文实验主要基于仿真环境，尚未充分考虑传感器噪声、定位误差、通信丢包、执行器延迟和飞控约束等实际因素。未来可在 ROS/Gazebo、AirSim 或真实多无人机平台上开展进一步验证，将算法输出与低层飞控接口相结合，检验其在真实系统中的稳定性、鲁棒性和工程可用性。

综上所述，本文提出的分层耦合路径规划框架和 AL-SHADE-TALG-QU 改进算法在复杂障碍物环境下表现出较好的路径质量、安全性和编队协同性。随着三维动态建模、在线自适应优化和真实平台验证的深入，该方法有望进一步应用于城市低空巡检、物流配送、灾害搜索和多无人机协同侦察等实际任务中。














