#import "/template.typ": algorithm, algorithm-ref, capfig, capsubfig, captab, equation-note, indent, multicite, nwpu-thesis

#show: nwpu-thesis.with(
  anonymous: false, // 是否开启盲审模式
  title: "多障碍物环境中无人机集群路径规划方法研究",
  author: "吴昭桦",
  major: "计算机科学与技术",
  supervisor: ("陈进朝", "教授"),
  submit-date: (year: 2026, month: 6),
  abstract: [
    面向城市低空物流、环境监测、灾害救援还有协同侦察这些应用场景，无人机集群需要在多障碍物的环境里面同时去完成路径搜索、队形保持以及在线避障的任务，单机的路径可行并不一定就意味着集群的飞行是安全的：全局路径要是过于曲折就会把僚机的跟随误差放大，而局部避障与编队控制之间的耦合不够充分，又可能会造成队形拉伸、机间距离太小或者出现局部振荡，因此，在复杂障碍物环境下对无人机集群做路径规划，就需要在任务效率、轨迹平滑性、编队稳定性和安全裕度这几者之间去做综合的协调。

针对上面这些问题，本文建立了一个由全局路径规划层、编队控制层和局部避障层所组成的分层耦合框架：全局路径规划层负责去生成从起点到目标点的任务级参考航路，并且把路径长度、平滑性、障碍物风险、通道宽度还有编队保持代价这些因素统一地考虑进去；编队控制层则基于改进之后的一致性协议来描述多无人机之间的协同关系，在存在通信时延和拓扑变化的条件下，仍然维持住虚拟领航者-僚机这种队形；局部避障层被嵌入到飞行执行的过程当中，对全局路径和编队控制指令进行实时的安全修正，这三个层次之间通过参考航点、速度跟踪以及加速度修正相互耦合在一起，使得系统既保留下了全局规划的远距离引导能力，同时又具备了近场障碍物的响应能力，在全局规划层里面，本文复现并且比较了 PSO、ACO、GA、MSA、AHA 还有AL-SHADE 等算法，并且针对 AL-SHADE 存在的外部档案冗余、对环境风险感知不足以及离散航点连续性比较弱这些问题，提出了 AL-SHADE-TALG-QU 这种改进方法，这种方法通过外部档案的相似度去重来提升历史解的多样性，通过由威胁所驱动的 Lévy-Gaussian 双态缩放因子来加强在障碍物密集区域的跳出能力，再通过 B 样条曲线参数化路径建模来改善轨迹的连续性和可执行性，在局部避障层里面，本文以人工势场法作为基础，加入了切向旋转斥力机制，让无人机在靠近障碍物的时候不单单能够做径向的避让，还能够顺着障碍物的边缘形成一种绕行的趋势，这样一来就降低了局部极小值和目标不可达问题所带来的影响。

仿真实验是在二维多障碍物环境当中进行的，本文把不同方法从路径长度、平滑度、规划成功率、收敛迭代次数还有队形保持误差这些指标上做了比较，实验的结果表明，经过改进之后的 AL-SHADE 在路径平滑性、编队稳定性还有无人机之间的安全裕度这些方面是具有优势。
  ],
  keywords: ("无人机集群","路径规划；多障碍物环境；螳螂搜索算法；AL-SHADE；人工势场法"),
  abstract-en: [
    For applications such as urban low-altitude logistics, environmental monitoring, disaster relief, and collaborative reconnaissance, drone swarms must simultaneously perform path searching, formation maintenance, and online obstacle avoidance in environments with multiple obstacles. A feasible path for a single drone does not necessarily guarantee safe flight for the entire swarm: overly convoluted global paths can amplify following errors among follower drones, while insufficient coupling between local obstacle avoidance and formation control may lead to formation stretching, excessive inter-drone distances, or local oscillations. Therefore, path planning for drone swarms in complex obstacle environments requires a comprehensive coordination among mission efficiency, trajectory smoothness, formation stability, and safety margins. 

To address the aforementioned issues, this paper proposes a hierarchical coupled framework consisting of a global path planning layer, a formation control layer, and a local obstacle avoidance layer. The global path planning layer generates task-level reference trajectories from start to goal, integrating considerations such as path length, smoothness, obstacle risk, channel width, and formation maintenance cost. The formation control layer employs an improved consensus protocol to model cooperative relationships among multiple drones, maintaining a virtual leader-follower formation even under communication delays and dynamic topology changes. The local obstacle avoidance layer is embedded within the flight execution process, providing real-time safety corrections to both the global path and formation control commands. These three layers are mutually coupled through reference waypoints, velocity tracking, and acceleration adjustments, enabling the system to retain long-range guidance capabilities from global planning while also responding effectively to nearby obstacles. Within the global planning layer, this study reproduces and compares several algorithms including PSO, ACO, GA, MSA, AHA, and AL-SHADE. To address AL-SHADE's shortcomings—such as redundant external archives, insufficient environmental risk perception, and weak continuity between discrete waypoints—a novel improvement named AL-SHADE-TALG-QU is proposed. This method enhances solution diversity by deduplicating similar entries in the external archive, strengthens exploration capability in obstacle-dense regions using a threat-driven Lévy-Gaussian dual-state scaling factor, and improves trajectory continuity and feasibility via B-spline curve-based path parameterization. In the local obstacle avoidance layer, an artificial potential field approach is adopted, augmented with a tangential rotational repulsion mechanism that enables drones not only to perform radial avoidance when approaching obstacles but also to follow the obstacle edges and navigate around them, thereby mitigating the impact of local minima and unreachable goals. 

The simulation experiments were conducted in a two-dimensional environment with multiple obstacles. This paper compares different methods based on metrics such as path length, smoothness, planning success rate, number of convergence iterations, and formation maintenance error. The experimental results show that the improved AL-SHADE outperforms others in terms of path smoothness, formation stability, and safety margin among drones.
  ],
  keywords-en: ("UAV swarm", "path planning", "multi-obstacle environment", "Mantis Search Algorithm", "AL-SHADE", "Artificial Potential Field"),
  appendix: [
     
  ],
  acknowledgement: [
    在最后的最后我要感谢xxx,感谢xxx在我的学业
  ],
  design_summary: [
    小结内容……
  ],
)

= 绪论

== 研究背景

近些年无人机技术不断成熟，它的应用范围也从早期的航拍和军事侦察，逐渐拓展到城市低空物流配送、环境监测、灾害救援以及精准农业等更复杂的场景中，这些任务对飞行平台在效率、自主性和环境适应能力方面都提出了更高的要求。与单架无人机相比，无人机集群（UAV Swarm）在很多方面都更有优势，通过协同控制技术，集群中的个体可以在分配任务与执行任务的过程中共享信息、协调行为，从而在整体上提高任务完成的效率和系统运行的稳定性。这种多机一起协作的方式，不仅可以在部分节点出现故障时让系统依然保持运行，也让集群更加适合去执行大范围的环境感知、多目标的并行搜索以及高动态的协同运输等比较复杂的工作。但在实际飞行中，环境带来的限制会使集群控制的难度明显增加，尤其是在作业场景里分布了大量密集的建筑物、树木或者其他临时障碍物时，路径规划就不再只是给每架无人机找到一条从起点到终点的连线，而是要同时处理多个运动体在障碍物空间中的时空协调、机间防撞以及与外部障碍物的规避，这使得问题需要求解的维数明显升高，对规划方法的全局能力和实时性也提出了更高的要求。

在已有的路径规划研究当中，像$A^*$、Dijkstra 和 RRT 这一类传统全局规划算法，被使用的时间比较早，也比较具有代表性，Dijkstra 算法能够在图结构里求出比较明确的最短路径，但它的搜索范围比较大，计算量也会跟着节点数量的增加而明显上升；$A^*$ 在 Dijkstra 的基础上加入了启发函数，搜索效率相应地变得更高一些，可路径质量会比较依赖栅格分辨率和启发函数的设计；RRT 很适合在连续空间里快速扩展随机树，对于高维空间使用起来也比较方便，不过它生成的路径往往不够平滑，到了后期还需要额外去做修剪和优化，对于二维而且障碍物密度比较高的无人机集群任务来说，这类方法的思路虽然很清楚，但是在路径平滑性、编队限制还有多目标综合评价这些方面，还是显得不够灵活。

在此基础之上，粒子群算法（PSO）、蚁群算法（ACO）和遗传算法（GA）这一类传统方法，由于原理比较直观、实现起来也较方便，在连续空间的全局优化问题上已经得到了相当广泛的应用。但是，当把它们直接移植到二维高密度障碍物环境下的集群路径规划时，各方法自身的局限之处就开始集中显露出来，往往容易表现出路径不连续、搜索限于局部区域或过早收敛等问题。分开来看，PSO 在靠近障碍物边缘的时候，因为粒子的速度更新方式没有充分利用障碍物的几何信息，容易产生”贴墙飞行”的情况，使规划出的路径紧贴障碍物表面弯曲前进，既不安全，也不利于实际飞行中的跟踪控制；ACO 在障碍物分布较密的区域里，由于信息素的积累和挥发之间的反馈关系失去了平衡，单个蚂蚁容易在局部区域内反复兜圈，陷进循环搜索而迟迟收不到可行的路径；GA 在交叉重组的环节中，因为染色体的离散编码形式与连续的路径空间之间不够对应，可能会频繁产生直接穿过障碍物的不可行解，这就加大了后续修补和筛选环节的计算开销。此外，经典人工势场法（APF）虽然在实时性方面表现还可以，但当环境中出现”U 型”障碍物或者狭窄通道之类特殊结构的时候，合力场的内在特性比较容易将无人机集群拖入局部极小值中，或者造成死锁状态，使算法不能继续向前推进，难以满足复杂环境里对规划可靠性的要求。

近几年，生物启发式算法和自适应优化算法在连续优化问题里表现出了较好的全局搜索能力，因此也逐渐被引入到复杂环境下的路径规划研究中。这类算法一般是通过模拟自然界中的生物行为，或者借助参数自适应机制来调节搜索过程，让种群在迭代过程中具备一定的跳出局部最优的能力，为多障碍环境中的路径规划提供了新的思路。

螳螂搜索算法（MSA）模拟了螳螂捕食过程中的追踪、伏击和攻击行为，将全局探索与局部的精细开发结合起来，在一些多峰连续函数的优化问题中取得了比较好的结果。但把 MSA 直接用在二维障碍物环境下的路径规划中时，还是会遇到一些问题。一方面，随机初始化不容易保证初始的解在复杂的障碍物空间中均匀地分布，种群可能从一开始就集中在了少数几个局部区域，这会影响后续搜索的多样性；另一方面，算法运行到后期，对搜索步长和方向的调节能力比较有限，容易过早地收敛到仅在局部较优的路径上，从而影响在复杂环境下持续寻找更优解的稳定性。

人工蜂鸟算法（AHA）通过模拟蜂鸟的觅食行为和灵活的飞行方式，利用访问表把不同食源的访问情况记录下来，再结合轴向飞行、对角飞行和全向飞行这三种方式，在引导觅食、领地觅食和迁徙觅食之间进行切换，这样的机制让 AHA 在连续优化问题里面可以兼顾搜索范围和收敛速度，但是在二维离散路径规划任务里，AHA 本身缺少针对航点连续性的专门限制，也就是说，算法可以去更新航点的坐标，却不能自然地保证相邻航点之间的路径足够平滑，因此，所生成的无人机航迹往往会出现比较明显的折线和急转弯，这就很难满足实际飞行对路径可行性和飞行稳定性的要求。

基于成功历史参数自适应学习的差分进化算法（AL-SHADE）利用成功历史参数自适应机制，根据搜索过程里成功个体的信息去调整变异因子和交叉概率，再结合线性种群规模缩减策略，把计算资源逐步集中到较优个体的附近，这样的设计让差分进化算法处理高维连续优化问题的能力得到了提高，但是当 AL-SHADE 被直接用到二维离散航点规划的时候，算法虽然可以找到把起点和终点连接起来的低代价路径，却没有明确地限制相邻航点之间的几何连续性和转弯的平滑程度，所以，规划出来的结果里仍然可能出现急转、锯齿状波动这些现象，这就会影响到路径的实际执行效果。

由此可见，在二维多障碍物环境下对无人机集群进行路径规划，不仅需要考虑全局路径最优与航迹连贯性之间的矛盾，还要兼顾集群编队保持稳定以及局部动态避碰等实时性需求。这些问题相互耦合，使得单纯依靠某一类传统或新兴算法都难以同时获得满意的效果。所以，开展这方面的研究，既有助于从理论上丰富和完善无人机集群协同规划的方法体系，也可以为城市低空物流、灾后快速搜救等实际应用场景中的高效安全运行提供直接的技术支撑和借鉴意义。

== 研究现状

在无人机集群路径规划的研究里面，编队控制属于比较早被重视起来的基础问题之一，国外的研究更加偏向理论模型的搭建，把多智能体一致性的思想比较早地运用到了多个飞行器之间的协同控制上，Ren 和 Beard 对分布式一致性控制以及它在多飞行器系统中的应用做了较为详细的说明，这给后面编队控制方向的研究提供了重要的参考@RenBeard2008，而国内的研究更多是从实际任务落地的角度出发，在虚拟领航者模型和一致性控制的框架下面，结合无人机在动力学上的限制、通信拓扑的变化还有任务对队形的需要，来讨论像人字形或者横队这一类典型编队要怎么去维持@ZhangW2020UAVFormation，从这些研究当中可以看出来，国外主要把注意力放在了通信拓扑和收敛性分析上，国内则更加在意队形能不能适应具体的飞行航线和受限空间，随着任务开展的环境从开阔空域逐步转向城市低空、比较窄的通道以及地形比较复杂的地带，固定队形不够用的情况就开始显露出来了，为了让集群穿过受限环境的能力得到加强，对队形做约束调整、让队形自适应地改变、把队形收拢以及横向错开等方法，也陆续被用到了相关的研究当中@Zhang2019Formation。

在全局路径规划方面，国内外的研究都比较多的采用了 PSO、ACO、GA 等启发式算法，国外的研究从比较早的时候就从算法本身入手，把粒子群优化、差分进化还有新近出现的生物启发式算法用在连续优化和路径搜索当中，Kennedy 和 Eberhart 所提出的粒子群优化算法在结构上比较简单、收敛也快，所以比较早的时候就被应用到了无人机的路径搜索里@Kennedy1995PSO；后续的研究又陆续把螳螂搜索算法@MSA2023、L-SHADE@Tanabe2014LSHADE 以及基于成功历史参数自适应学习的差分进化算法@ALSHADE2022 等方法引了进来，用来加强在复杂空间当中的搜索效果，国内的研究则往往是从无人机路径规划所要面对的工程约束入手，对适应度函数进行改造，在评价的过程里把路径长度、障碍物风险、平滑程度还有约束惩罚这些因素一起纳入计算，再配合禁忌表、惩罚函数或者精英保留等措施来提高可行解所占的比例@Wang2021APF，两者的侧重点并不完全一样：国外更加看重算法本身的搜索能力与参数自适应机制，国内则更关注路径是不是可行、是不是安全，以及在仿真当中验证起来是不是方便。

局部避障的功能，主要是去处理全局路径在实际飞行执行过程当中所碰到的临时风险，国外在这个方向上的一个经典研究是 Khatib 提出的人工势场法，这种方法借助目标点给出的吸引力以及障碍物产生的排斥力来实现实时避障@Khatib1986APF，但是，当环境里面存在凹陷区域或者狭窄缝隙这一类结构的时候，经典人工势场法就比较容易陷入局部极小值，轨迹也可能出现抖动的现象，国内的相关研究大多是在工程仿真的环境下面去对人工势场法做改进的，所做的工作包括重新设定斥力函数的表达形式、加入沿切线方向的旋转力、调整各个部分的权重比例，还有把局部避障的目标和全局引导点关联起来等等，这些做法的目的都是要让无人机在复杂障碍物附近绕行的能力变得更强@Wang2021APF。

从总体上来看，国外的研究在局部避障模型和控制理论方面的积累要更深一些，国内的研究则更加重视把它跟全局路径以及编队控制耦合在一起来实现，不过，现有的各种方法还是经常把全局路径搜索、局部避障还有编队控制这三块分开来设计，这就让路径长度、轨迹的平滑程度、编队能不能保持得住还有机间的安全距离这些指标，很难被放在同一个优化的过程里面一起协调好，从这样一个现状出发，本文选用基于成功历史参数自适应学习的差分进化算法来作为全局路径规划的主要框架，并且打算通过外部档案相似度去重、由障碍物净空距离来驱动的 Lévy-Gaussian 双态变异缩放因子还有 B 样条曲线参数化这三项办法来做改进。

== 研究内容

结合实际任务需要和前面分析的问题，本文把多障碍环境中的无人机集群作为研究对象，主要从编队控制、全局路径规划和局部动态避障这几个方面展开工作。为了让提出的方法在复杂限制和密集障碍场景下能够可行、稳定并且可以被重复验证，研究内容被划分成以下四个部分：

1. 多无人机编队协同控制模型：在多智能体一致性算法和虚拟领航者模型的基础上，搭建了多无人机协同控制的框架，同时说明了邻接矩阵和拉普拉斯矩阵对通信拓扑以及收敛过程的约束关系。对于二维平面里的编队保持与协同跟踪问题，设计了一种同时把位置误差和速度误差考虑进去的控制律，随后结合速度、加速度和角速度等运动学限制，对物理上的可行性做了检验。在这个基础上，又引入了一种横向偏置的自适应调节方式，使无人机集群无论是在开阔区域还是在局部受限区域，都可以收敛到人字型这一类预定队形，并且在通道收缩和恢复的过程中还能保留一定的队形弹性。

2. 多障碍空间中的全局路径智能优化与轨迹平滑：针对密集障碍物和多种限制的二维环境，复现并改进了粒子群算法、蚁群算法和遗传算法等常见的启发式方法。为了进一步对比不同群体智能算法在复杂连续空间中的搜索表现，本文还引入了螳螂搜索算法、人工蜂鸟算法，以及一种依靠历史成功参数进行自适应学习的差分进化算法；螳螂搜索算法和人工蜂鸟算法分别从不同的生物行为中得到搜索方式，全局探索的能力比较强，而基于成功历史参数自适应学习的那一种差分进化算法，则是通过历史成功参数记忆和自适应调整策略，改进了搜索的效率。通过对以上这些算法进行统一的建模和实验比较，可以更全面地去分析它们在路径长度、平滑程度、避障能力和收敛效果上的区别。针对差分进化算法在离散航点编码时轨迹连续性不太够的问题，又进一步使用了用B样条曲线来做参数化的自适应差分进化算法，对路径维度进行降维并建立连续轨迹模型，借此改善全局规划结果的光滑程度和可飞行性。

3. 基于改进人工势场的局部动态避障策略：局部避障这部分主要处理的是由动态障碍和“U”型障碍可能引起的局部极小值情况。本文在传统人工势场法的引力模型和斥力模型之上，加进了一种切向旋转势场力，使得无人机在靠近障碍物的时候可以顺着障碍边缘绕行，而不是只能依靠径向的斥力后退或者停在原地。这种避障策略会与全局路径引导点以及编队控制律一起发挥作用，让无人机在不明显偏离全局任务方向的条件下，完成比较稳定的局部机动和逃逸。

4. 多层算法集成与仿真检验：为了检验前面这些方法的整体表现，本文搭建了一个统一的二维多障碍物仿真环境，将编队控制层、全局规划层和局部避障层放在同一个场景当中，进行分层耦合的测试。实验里设置了不同的障碍密度和典型的障碍分布，从路径长度、平滑度、规划成功率、收敛迭代次数和队形保持误差等指标来做横向比较，分析所提出的方法在安全性、稳定性以及效率方面的实际效果。
#capfig(
  image("figures/yanjiuneirong.png", width: 90%),
  caption: [研究内容示意图],
  label: <test>,
)



= 相关工作

无人机集群的路径规划会涉及到编队控制、全局路径搜索和局部避障等多个环节，在这些环节当中，参考航迹的整体质量由全局规划算法来决定，并且它也会直接影响到后续编队跟踪和局部修正的稳定性，所以本章把重点放在全局路径规划算法上面，先给出统一的路径评价函数，再对 PSO、ACO、GA、MSA、AHA 和 AL-SHADE 等算法在本文任务里的建模方式与实现逻辑进行说明。

== 全局路径规划算法设计与实现

在全局路径规划层中，它的任务是为领航者生成一条从起点到目标点的参考路径，同时这条路径需要能够被编队控制和局部避障模块稳定地使用，因为不同优化算法所得到的候选路径在长度、平滑性、安全裕度还有编队可行性这些方面会有比较大的差异，所以本文采用了一个统一的适应度函数来作为评价的标准，各个算法都在相同的路径编码、搜索边界以及限制条件下进行优化，这样就能保证后续的实验比较具有一致性。

=== 适应度函数设计

假设候选解经过解码之后，得到的二维参考路径为

$
cal(Q) = {bold(q)_1, bold(q)_2, dots, bold(q)_M}, quad bold(q)_k in bb(R)^2
$

这里 $M$ 为路径采样点数量。领航者沿 $cal(Q)$ 飞行，而其余无人机会按照预设的队形偏移和局部避障修正来确定自己的位置 $bold(p)_(i,k)$。所以，这样一来，适应度函数就不仅仅是在评价领航者本身的路径，同时也在评价这条路径对集群安全和队形保持所带来的影响。

本文将路径规划写成加权多目标优化问题：

$
J(bold(X)) = alpha L + beta S + gamma R + mu W + lambda F
$

在上面的式子里，$L$ 为路径长度代价，$S$ 为路径平滑代价，$R$ 为综合风险代价，$W$ 为通道宽度限制惩罚，$F$ 为编队保持代价；$alpha$、$beta$、$gamma$、$mu$ 和 $lambda$ 分别是它们所对应的权重。在实际计算的时候，每一个分量都会被先做归一化处理，然后再代入到加权和当中，算法采用的是“适应度越大越优”这种形式，把它转换成

$
"Fitness"(bold(X)) = frac(1, J(bold(X)) + epsilon)
$

进行等价转换，其中 $epsilon$ 为极小正数。本文主要以最小化 $J$ 为优化目标。

路径长度代价用于限制飞行距离：

$
L = sum_(k=1)^(M-1) norm(bold(q)_(k+1) - bold(q)_k)
$

路径平滑代价由相邻路径段夹角决定：

$
theta_k = arccos frac((bold(q)_k - bold(q)_(k-1)) dot (bold(q)_(k+1) - bold(q)_k), norm(bold(q)_k - bold(q)_(k-1)) norm(bold(q)_(k+1) - bold(q)_k))
$

$
S = sum_(k=2)^(M-1) theta_k^2
$

当路径频繁急转时，$theta_k$ 增大，平滑代价随之上升。该项可以减少折线航迹，使规划结果更适合无人机连续飞行。

综合风险代价由静态障碍风险、集群内部碰撞风险和通信风险组成：

$
R = R_s + R_c + R_f
$

静态障碍风险用于描述路径点与障碍物之间的距离关系：

$
R_s = sum_(k=1)^M exp lr(- frac((d_s(bold(q)_k) - r_("eq"))^2, sigma_s^2))
$

其中 $d_s(bold(q)_k)$ 表示路径点到最近静态障碍物表面的距离，$r_("eq")$ 为安全半径，$sigma_s$ 控制风险衰减速度。无人机越靠近障碍物，风险项越大。

集群内部碰撞风险用于限制无人机之间的最小距离：

$
R_c = sum_(k=1)^M sum_(i<j) phi(d_(i j)(t_k)), quad
phi(d) = cases((1 - d / d_("safe"))^2 quad d < d_("safe"), 0quad d >= d_("safe"))
$

其中 $d_("safe")$ 为最小安全距离。通信风险用于避免编队链路过长：

$
R_f = sum_(k=1)^M sum_((i,j) in cal(E)) max(0, d_(i j)(t_k) - d_("comm"))^2
$

$cal(E)$ 表示通信链路集合，$d_("comm")$ 为通信半径。

通道宽度惩罚用于判断当前路径是否满足编队通行需求：

$
W = sum_(k=1)^M max(0, B_("req", k) - B_k)^2
$

其中 $B_k$ 为路径点附近的可通行宽度，$B_("req", k)$ 为编队通过该位置所需的最小宽度。当允许队形适度压缩时，可令

$
B_("req", k) = B_("form")^"min" + eta_b Delta B_k
$

编队保持代价用于衡量实际队形与期望队形之间的偏差。若采用领航者-僚机偏移模型，可写为

$
F = sum_(k=1)^M sum_(i=1)^n norm(delta_(i,k) - delta_i^*)^2
$

其中 $delta_i^*$ 为第 $i$ 架无人机的理想相对偏移，$delta_(i,k)$ 为实际偏移。也可以用邻接边距离误差表示：

$
F = sum_(k=1)^M sum_((i,j) in cal(E)) (norm(bold(p)_(i,k) - bold(p)_(j,k)) - d_(i j)^*)^2
$

适应度函数把路径效率、安全避障、轨迹平滑和编队保持统一到同一评价框架中，为后续各类优化算法提供共同目标。

=== 粒子群算法（PSO）设计与实现

粒子群算法通过模拟群体协同行为完成搜索。每个粒子表示一条候选路径，其位置向量由 $N$ 个中间航点组成：

$
bold(X)_i = [x_(i,1), y_(i,1), dots, x_(i,N), y_(i,N)] in bb(R)^(2N)
$

种群初始化时，先根据起点和终点生成基线航点，再在基线两侧设置带状搜索边界。粒子位置在边界内随机生成，速度向量可初始化为零或小扰动。第 $i$ 个粒子在第 $t$ 代的速度和位置更新为

$
bold(V)_i^(t+1) = omega bold(V)_i^t + c_1 r_1 (bold(P)_i^"best" - bold(X)_i^t) + c_2 r_2 (bold(G)^"best" - bold(X)_i^t)
$

$
bold(X)_i^(t+1) = bold(X)_i^t + bold(V)_i^(t+1)
$

其中 $omega$ 为惯性权重，$c_1$ 和 $c_2$ 分别为个体学习因子和群体学习因子，$r_1$、$r_2$ 为 $[0,1]$ 内随机数，$bold(P)_i^"best"$ 为个体历史最优位置，$bold(G)^"best"$ 为全局最优位置。更新完成之后如果粒子超出了边界，就把它裁剪到允许的范围里面。

PSO 的执行过程可以被描述为下面这样：首先，依据起点、终点还有带状搜索边界把粒子的位置初始化好，同时给每一个粒子都设置好初始的速度；接下来，把粒子的位置解码成航点序列，算出对应路径的适应度，并且把个体最优位置和全局最优位置记录下来；然后，在每一轮迭代当中，按照惯性项、个体学习项和群体学习项去更新粒子的速度，再通过速度去更新粒子的位置；在这之后，对越界的粒子进行边界修正，重新计算适应度，如果新位置比粒子自身的历史最优还要好，就对个体最优进行更新，要是它还优于当前的全局最优，就同时把全局最优也一起更新；等到迭代全部结束以后，算法会把全局最优粒子所对应的航点序列当作规划出来的路径进行输出。

=== 蚁群算法（ACO）设计与实现

基本蚁群算法通常被用在离散图的搜索里面，它的核心是依靠信息素的强度还有启发式因子，来决定一只蚂蚁从当前的节点转移到下一个节点的概率，对于蚂蚁 $k$ 来说，它从节点 $i$ 转移到被允许访问的节点 $j$ 的概率，一般会被写成下面这个样子：

$
p_(i j)^k = frac(tau_(i j)^alpha eta_(i j)^beta, sum_(l in "allowed"_k) tau_(i l)^alpha eta_(i l)^beta)
$

在这个式子里，$tau_(i j)$ 是边 $(i,j)$ 上面的信息素强度，$eta_(i j)$ 是启发式因子，$alpha$ 和 $beta$ 分别用来表示信息素和启发式因子各自的重要程度，在一次迭代结束以后，信息素通常会按照挥发和增量这两种方式来进行更新：

$
tau_(i j)(t+1) = (1-rho) tau_(i j)(t) + Delta tau_(i j)(t), quad
Delta tau_(i j)(t) = sum_(k=1)^m Delta tau_(i j)^k
$

这里面，$rho$ 是信息素的挥发率，$Delta tau_(i j)^k$ 所表示的是第 $k$ 只蚂蚁在边 $(i,j)$ 上所释放出来的信息素量，它常见的一种定义是

$
Delta tau_(i j)^k = cases(
  Q / L_k quad "if ant " k " used edge " (i,j),
  0 quad "otherwise"
)
$

/* 本文在无人机路径规划里采用的是连续航点编码，候选解是直接处在实数的坐标空间当中的，并不存在固定的离散边 $(i,j)$，所以这里并不直接去使用基本 ACO 的状态转移概率，而是采用了连续域的蚁群优化算法 ACO，ACO 把信息素模型转化成了建立在解档案上面的高斯核采样，通过档案里优质解的排序权重来引导新解的产生，设档案的容量为 $K$，档案当中第 $l$ 个解是 $bold(s)_l$，它的适应度为 $J_l$，档案按照适应度从小到大排好序以后，较优的解就能获得更高的采样概率：

$
omega_l = frac(1, q K sqrt(2 pi)) exp(lr(- frac((l - 1)^2, 2 q^2 K^2))), quad
p_l = frac(omega_l, sum_(h=1)^K omega_h)
$

在这里 $q$ 是用来控制选择压力的，每一只蚂蚁先按照概率 $p_l$ 去选择一个引导解，然后再围绕这个解去做高斯采样：

$
X_j^"new" = s_(l,j) + cal(N)(0, sigma_j^2)
$

标准差 $sigma_j$ 是根据档案里面同一个维度上的离散程度来确定的，它通常被写成

$
sigma_j = xi frac(1, K-1) sum_(h=1, h != l)^K |s_(h,j) - s_(l,j)|
$

式子里面的 $xi$ 是搜索尺度的参数，新的解生成之后还需要去裁剪边界、把它解码成路径并且计算出适应度，然后再和原来的档案合并在一起重新排序，只把排在前面的 $K$ 个解保留下来。 */

ACO 的执行过程可以被概括为下面这样：首先，在带状的搜索空间里面去生成一批初始的候选路径，并且按照适应度把这些路径排好顺序，由此建立起一个解档案；随后，依据档案里面各个解的排序位置，去算出高斯核权重和选择概率，这样一来质量比较好的路径就能够获得更高的采样概率；然后，每一只蚂蚁都会按照概率从档案里面去挑选出一个引导解，再根据档案的分布情况去计算出各个维度的采样标准差；在这之后，把引导解当作中心来进行高斯采样，由此生成新的航点编码，同时也对那些越界的部分做出修正处理；再然后，计算出新路径的适应度，把它和原来的档案合并到一起重新进行排序，只把排在前面的 $K$ 个较优解保留下来；像这样的过程会被反复地执行，一直等到迭代次数达到了最大值才停下来，最后把档案里面适应度最小的那一条路径输出，作为全局规划的结果。

=== 遗传算法（GA）设计与实现

遗传算法（Genetic Algorithm, GA）是一种模拟自然选择和遗传进化过程的随机搜索算法，主要通过选择、交叉还有变异这些操作来持续地更新种群，在本文中，针对无人机二维路径规划里的连续航点优化问题，采用的是实数编码的遗传算法，直接在航点的坐标空间当中进行搜索，这样就可以避免因二进制编码而带来的精度损失和维度膨胀问题。

*染色体编码与初始化。* 每条染色体对应 $N$ 个中间航点的二维坐标，可表示为

$
bold(C) = [x_1, y_1, x_2, y_2, dots, x_N, y_N] in bb(R)^(2N)
$

初始种群的生成方式和 PSO 里所用的基线扰动方式是一样的，形成规模为 $P$ 的初始种群 $cal(P)_0$。所有个体均通过统一适应度函数 $J$ 进行评价，适应度较小的染色体表示路径质量较优。

*选择算子。* 本文采用 $k$-锦标赛选择，在每一次进行选择的时候，会从当前的种群里面随机抽出 $k$ 个体，然后从里面挑出适应度最小的那一个来作为父代，和轮盘赌选择相比较，锦标赛选择并不依赖于适应度的绝对数值，只是按照相对优劣来确定父代，因此在路径代价差异比较大的时候会表现得更加稳定。本文取 $k = 3$。

*交叉算子。* 对两个父代染色体 $bold(C)_(p_1)$ 和 $bold(C)_(p_2)$，采用整体算术交叉生成子代：

$
bold(C)_("child") = alpha bold(C)_(p_1) + (1 - alpha) bold(C)_(p_2), quad alpha tilde.op cal(U)(0, 1)
$

该算子相当于在两个父代张成的高维线段上进行插值，可以继承父代中的有效航点组合，并在二者之间生成新的候选路径。

*变异算子。* 为防止种群过早收敛，对子代中的航点坐标施加高斯变异。对第 $w$ 个航点，以概率 $p_m$ 执行：

$
bold(C)_("child")[w] arrow bold(C)_("child")[w] + cal(N)(0, sigma(g)^2 bold(I)_2), quad w = 1, 2, dots, N
$

其中 $bold(I)_2$ 为二维单位矩阵。变异强度随代数线性衰减：

$
sigma(g) = sigma_0 * (1 - frac(g, G_("max")))
$

$g$ 为当前代数，$G_("max")$ 为最大代数，$sigma_0$ 为初始变异强度。进化前期使用较大的变异步长，有利于扩大搜索范围；后期逐渐减小步长，使种群更多围绕较优路径进行局部修正。

*精英保留与终止。* 为避免交叉和变异破坏已获得的优质路径，每代先按适应度对种群排序，并将前 $P_("elite") = max(5, round(0.1 P))$ 个最优个体直接保留到下一代。其余个体通过锦标赛选择、算术交叉和高斯变异生成，直到新种群规模恢复为 $P$。当算法达到最大代数 $G_("max")$ 后，输出当前种群中适应度最小的染色体作为全局路径航点序列。

GA 的执行过程如下，首先根据起点、终点和带状搜索边界生成初始染色体种群，并计算每条候选路径的适应度；随后，按适应度排序并保留一部分精英个体，避免当前较优路径在后续操作中被破坏；然后，通过锦标赛选择从种群中选出父代；接着，采用算术交叉生成子代，并按给定概率对航点坐标施加高斯变异；之后，对越界航点进行修正，并重新计算子代适应度。新一代种群由精英个体和新生成子代共同组成，循环执行上述过程，直到达到最大代数，最后输出适应度最小的染色体作为全局规划路径。


=== 螳螂搜索算法（MSA）设计与实现

螳螂搜索算法（Mantis Search Algorithm, MSA）由 Abdel-Basset 等提出，其基本思想来源于螳螂寻找猎物、攻击猎物以及性食同类等行为。与一般只依靠单一扰动方式的群体智能算法不同，MSA 将搜索过程划分为寻找猎物、攻击猎物和性食同类三个阶段：寻找猎物阶段负责扩大搜索范围，攻击猎物阶段围绕当前最优解进行开发，性食同类阶段则通过个体重组和强扰动维持种群多样性。

在无人机路径规划中，每只螳螂对应一条候选航点序列。第 $i$ 个个体在第 $t$ 次迭代时的位置向量为

$
bold(x)_i^t = [x_(i,1)^t, y_(i,1)^t, dots, x_(i,N)^t, y_(i,N)^t]
$

初始化阶段根据搜索空间上下界生成初始种群：

$
bold(x)_i^0 = bold(x)_L + bold(r) dot (bold(x)_U - bold(x)_L)
$

其中 $bold(x)_L$ 和 $bold(x)_U$ 分别为下界和上界，$bold(r)$ 为 $[0,1]$ 内的随机向量。初始化后计算所有个体的适应度，并记录当前全局最优解 $bold(x)^*$。同时建立外部存档，用于保存迭代过程中出现的优质历史解，为后续伏击行为和跳出局部区域提供参考。

*寻找猎物阶段。* 该阶段主要承担全局探索任务。MSA 通过回收控制因子 $F$ 调节追捕者行为和伏击者行为的切换：

$
F = 1 - (t "mod" (T / P)) / (T / P)
$

式中，$P$ 为回收因子。$F$ 在每个周期内由 1 逐渐衰减到 0，使算法可以周期性地从大范围搜索过渡到较集中的局部搜索，避免搜索模式过早固定。

追捕者行为用于模拟螳螂主动移动寻找猎物。其位置更新可写为：

$
bold(x)_i^(t+1) = cases(
  bold(x)_i^t + bold(tau)_1 dot (bold(x)_i^t - bold(x)_a^t) + |tau_2| dot bold(U) dot (bold(x)_a^t - bold(x)_b^t) quad r_1 <= r_2,
  bold(x)_i^t dot bold(U) + (bold(x)_a^t + bold(r)_3 dot (bold(x)_b^t - bold(x)_c^t)) dot (1 - bold(U)) quad "otherwise"
)
$

其中 $bold(x)_a^t$、$bold(x)_b^t$ 和 $bold(x)_c^t$ 为随机个体，$bold(tau)_1$ 为 Lévy 飞行扰动向量，$tau_2$ 为正态随机数，$bold(U)$ 为二值掩码向量。该式既包含大尺度随机游走，也包含由随机个体差分关系产生的方向重组，有利于在复杂障碍环境中寻找新的可行通道。

伏击者行为则利用外部存档中的历史优质解作为参考。设 $bold(x)_("ar")^t$ 为存档中随机选取的个体，则有：

$
bold(x)_i^(t+1) = cases(
  bold(x)_i^t + alpha dot (bold(x)_("ar")^t - bold(x)_a^t) quad r_9 <= r_(10),
  bold(x)_("ar")^t + (2 r_7 - 1) dot mu dot (bold(x)_L + bold(r)_8 dot (bold(x)_U - bold(x)_L)) quad "otherwise"
)
$

其中

$
alpha = cos(pi r_6) dot mu, quad mu = 1 - t / T
$

$mu$ 随迭代逐渐减小，使伏击者在前期具有较大的搜索半径，在后期更多围绕历史优质区域精细调整。

*攻击猎物阶段。* 当算法进入开发阶段时，当前全局最优解 $bold(x)^*$ 被视为猎物位置。MSA 先通过 Sigmoid 函数计算击打速度：

$
v_s = 1 / (1 + e^(l dot rho))
$

再根据个体与猎物之间的距离确定攻击方向：

$
bold(d)_(s i)^t = bold(x)^* - bold(x)_i^t
$

若击打成功，个体向当前最优解靠近：

$
bold(x)_i^(t+1) = (bold(x)_i^t + bold(x)^*) / 2 + v_s dot bold(d)_(s i)^t
$

若击打失败，则根据随机个体之间的差分关系修正方向：

$
bold(x)_i^(t+1) = bold(x)_i^t + r_(12) dot (bold(x)_a^t - bold(x)_b^t)
$

击打失败概率定义为

$
P_f = a dot (1 - t / T)
$

该概率随迭代增加而降低，表示算法前期允许更多方向试探，后期逐渐转向稳定收敛。若个体出现陷入局部区域的趋势，还可引入振荡扰动：

$
bold(x)_i^(t+1) = bold(x)_i^t + e^(2l) dot cos(2 pi l) dot |bold(x)_i^t - bold(x)_("ar")^t| + (2 r_(13) - 1) dot (bold(x)_U - bold(x)_L)
$

这一项通过非线性振荡和搜索空间尺度扰动增强跳出局部最优的能力。

*性食同类阶段。* 该阶段用于模拟雌性螳螂吸引、交配并吞食雄性的过程，本质上是一组维持多样性的重组算子。首先，个体根据随机同伴进行吸引更新：

$
bold(x)_i^(t+1) = bold(x)_i^t + bold(r)_(16) dot (bold(x)_i^t - bold(x)_a^t)
$

随后通过二值掩码执行交配重组：

$
bold(x)_i^(t+1) = bold(x)_i^t dot bold(U) + (bold(x)_a^t + bold(r)_(18) dot (bold(x)_i^t - bold(x)_a^t)) dot (1 - bold(U))
$

吞食行为可表示为：

$
bold(x)_i^(t+1) = bold(x)_a^t dot cos(2 pi l) dot mu
$

该阶段在种群过快收敛时可以重新引入差异信息，降低搜索陷入单一路径通道的概率。

/* 在本文实现中，每次位置更新后都需要进行边界修复，再重新计算适应度。若新个体优于旧个体，则接受更新；否则保留原个体。若新个体优于当前全局最优解，则更新 $bold(x)^*$ 并写入外部存档。迭代达到最大次数后，输出全局最优个体对应的航点序列。整体上，MSA 通过追捕、伏击、击打和性食同类等多种搜索机制，在全局探索、局部开发和种群多样性之间保持平衡。 */

MSA 的执行过程可概括为以下几步。首先，根据航点搜索边界随机初始化螳螂种群，并计算每个个体对应路径的适应度；随后，建立外部存档，保存当前搜索过程中出现的优质历史解；然后，在每轮迭代中根据回收控制因子选择追捕者或伏击者行为，完成寻找猎物阶段的全局探索；接着，围绕当前最优解执行攻击猎物阶段，并根据击打成功、击打失败或振荡扰动更新个体；随后，按概率执行性食同类操作，通过吸引、交配和吞食行为增加种群扰动。每次更新后都进行边界修复、适应度评价和最优解记录，直到达到最大迭代次数后输出全局最优航点序列。

=== 人工蜂鸟算法（AHA）设计与实现

人工蜂鸟算法（Artificial Hummingbird Algorithm, AHA）模拟蜂鸟的空间记忆、灵活飞行和觅食行为。算法中每只蜂鸟对应一个候选解，食物源位置对应无人机全局路径的航点编码。设路径包含 $N$ 个中间航点，则第 $i$ 只蜂鸟的位置向量为：

$
bold(x)_i = [x_(i,1), y_(i,1), dots, x_(i,N), y_(i,N)] in bb(R)^(2N)
$

种群初始化时，所有蜂鸟在带状搜索边界内随机生成：

$
bold(x)_i^0 = bold(x)_L + bold(r) dot (bold(x)_U - bold(x)_L)
$

其中 $bold(x)_L$ 和 $bold(x)_U$ 分别为搜索空间下界和上界。每个个体解码为航点序列后，与起点、终点共同构成完整路径，并使用前述适应度函数计算代价 $J(bold(x)_i)$。

*访问表与目标食物源选择。* AHA 的核心结构是访问表 $bold(V T)$。访问表用于记录蜂鸟对不同食物源的访问间隔，初始状态为：

$
(V T)_(i,j)^0 = cases("null" quad i = j, 0  quad i != j)
$

其中主对角线表示蜂鸟自身所在食物源，不参与访问选择。对蜂鸟 $i$ 而言，算法先在第 $i$ 行中寻找访问间隔最大的食物源；若多个食物源访问间隔相同，则选择其中适应度更优的个体作为目标。该规则可写为：

$
cal(C)_i = {j | (V T)_(i,j) = max_(k != i) (V T)_(i,k)}, quad
j^* = arg min_(j in cal(C)_i) J(bold(x)_j)
$

这样既能避免频繁重复访问同一食物源，也能使搜索逐步向高质量路径附近集中。

*飞行模式。* ，AHA 是通过一个方向向量 $bold(D) in {0,1}^(2N)$ 来控制这一次更新要涉及到哪些维度，如果只挑选一个维度来更新，那就对应着轴向飞行，比较适合对单个航点的坐标去做精细的调整；如果是随机选了好几个维度一起更新，那就对应着对角飞行，可以同时对若干个航点进行调整；要是所有的维度都参与了更新，那就对应着全向飞行，能够对整条路径的形态产生比较大的扰动，这三种飞行模式合在一起，就提供了从局部微调一直到全局扰动的不同搜索尺度。

*引导觅食。* 引导觅食模拟蜂鸟根据记忆飞向目标食物源的行为。设目标食物源为 $bold(x)_(j^*)^t$，候选位置更新为：

$
bold(v)_i^(t+1) = bold(x)_(j^*)^t + a dot bold(D) dot (bold(x)_i^t - bold(x)_(j^*)^t), quad a tilde.op cal(N)(0,1)
$

其中 $a$ 为标准正态随机变量，$dot$ 表示逐元素乘法。当 $a$ 较小时，候选解靠近目标食物源；当 $a$ 取较大值或负值时，候选解可能跨越目标区域，从而增强跳出局部最优的能力。

*领地觅食。* 领地觅食模拟蜂鸟在自身食物源附近继续搜索的行为。其更新不依赖外部目标，而是在当前个体附近施加扰动：

$
bold(v)_i^(t+1) = bold(x)_i^t + b dot bold(D) dot bold(x)_i^t, quad b tilde.op cal(N)(0,1)
$

其中 $b$ 同样服从标准正态分布。该策略主要用于局部开发，可对当前路径的部分航点进行修正。

*迁徙觅食。* 为避免种群长期集中在局部区域，AHA 每隔 $2n$ 次迭代触发一次迁徙操作。算法选取当前适应度最差的蜂鸟：

$
i_("worst") = arg max_i J(bold(x)_i^t)
$

并将其重新随机初始化：

$
bold(x)_(i_("worst"))^(t+1) = bold(x)_L + bold(r) dot (bold(x)_U - bold(x)_L)
$

迁徙觅食相当于向种群注入新的候选路径，可在搜索后期补充多样性。

*边界处理、贪婪选择与访问表更新。* 引导觅食或领地觅食生成候选解后，先对越界维度进行裁剪，再计算候选路径的适应度。个体更新采用贪婪准则：

$
bold(x)_i^(t+1) = cases(
  bold(v)_i^(t+1) quad J(bold(v)_i^(t+1)) < J(bold(x)_i^t),
  bold(x)_i^t quad "otherwise"
)
$

每次觅食后都需要更新访问表。若蜂鸟 $i$ 在引导觅食中访问了目标食物源 $j^*$，则将 $(V T)_(i,j^*)$ 置为 0，同一行中其他可访问元素加 1：

$
(V T)_(i,j^*) arrow 0, quad (V T)_(i,k) arrow (V T)_(i,k) + 1, quad k != j^*
$

若蜂鸟位置得到改善，则其他蜂鸟对该新食物源的访问间隔也需要相应刷新，使新产生的优质解更容易在后续迭代中被关注。

AHA 的路径规划流程可以概括为：先把蜂鸟种群和访问表初始化好；在每一轮迭代当中随机地去选择飞行模式，并且在引导觅食和领地觅食之间进行切换；产生候选解之后接着进行边界处理、适应度评价还有贪婪选择；按照规则去更新访问表，并且周期性地去执行迁徙觅食，这种算法借助访问表减少了重复搜索，依靠多种飞行模式来调节更新的尺度，再通过迁徙机制让全局探索的能力被维持住。

=== 基于成功历史参数自适应学习的差分进化算法（AL-SHADE）设计与实现

基于成功历史参数自适应学习的差分进化算法是在 L-SHADE 基础上发展而来的自适应差分进化算法，后文简称 AL-SHADE。该算法保留成功历史参数记忆和线性种群规模缩减机制，并引入 current-to-Amean/1 变异策略，以利用外部档案中的优质历史信息。本文将 AL-SHADE 作为主要全局路径规划框架。

AL-SHADE 的个体编码仍为二维航点向量：

$
bold(x)_i = [x_(i,1), y_(i,1), dots, x_(i,N), y_(i,N)]
$

初始种群按搜索边界随机生成：

$
bold(x)_i^0 = bold(x)_L + bold(r) dot (bold(x)_U - bold(x)_L)
$

算法维护两个历史记忆数组 $bold(M)_F$ 和 $bold(M)_("C R")$，分别存储成功的缩放因子和交叉概率。每个个体在迭代时随机选择一个记忆槽，并由柯西分布和高斯分布生成参数：

$
F_i tilde.op "Cauchy"(M_(F,r_i), 0.1), quad (C R)_i tilde.op cal(N)(M_(C R,r_i), 0.1)
$

AL-SHADE 采用两类变异策略。第一类为 current-to-pbest/1：

$
bold(v)_i = bold(x)_i + F_i (bold(x)_("pbest") - bold(x)_i) + F_i (bold(x)_(r_1) - bold(x)_(r_2))
$

其中 $bold(x)_("pbest")$ 从前 $p$% 优质个体中随机选取，$bold(x)_(r_2)$ 可来自当前种群或外部档案。


第二类变异策略为 current-to-Amean/1：

$
bold(v)_i = bold(x)_i + F_i (bold(x)_("Amean") - bold(x)_i) + F_i (bold(x)_(r_1) - bold(x)_(r_2))
$

其中 $bold(x)_("Amean")$ 为外部档案中优质个体的加权均值：

$
bold(x)_("Amean") = sum_(i=1)^m w_i bold(a)_i
$

权重可按排序位置确定：

$
w_i = frac(ln(m + 0.5) - ln(i), sum_(j=1)^m (ln(m + 0.5) - ln(j)))
$

*自适应策略选择。* AL-SHADE 对两种变异策略采用概率调度。对每个个体生成随机数 $r in [0, 1]$，若 $r < P_s$，则执行 current-to-pbest/1；否则执行 current-to-Amean/1。第 $g$ 代结束后，根据两种策略产生改进解的成功比例更新 $P_s$：

$
P_s^(g+1) = P_s^g + 0.05 dot (1 - P_s^g) dot (P_1 - P_2) dot frac(F_("Es"), F_("max"))
$

其中，$P_1$ 和 $P_2$ 分别表示 current-to-pbest/1 与 current-to-Amean/1 在当前代中的成功比例，$F_("Es")$ 为已消耗的函数评价次数，$F_("max")$ 为最大函数评价次数。若 $P_1 > P_2$，说明精英引导策略在当前阶段更有效，$P_s$ 相应增大；反之，则增加 current-to-Amean/1 的执行机会。该机制使算法可以随搜索进程动态调节开发和探索强度。


变异后，算法使用二项交叉生成试验向量：

$
u_(i,j) = cases(v_(i,j) quad r_j <= (C R)_i " or " j = j_("rand"), x_(i,j) quad "otherwise")
$

其中 $j_("rand")$ 保证至少一个维度来自变异向量。若变异向量越界，则采用父代与边界之间的中点进行修正：

$
v_(i,j) = cases((x_(i,j) + L_j) / 2 quad v_(i,j) < L_j, (x_(i,j) + U_j) / 2 quad v_(i,j) > U_j, v_(i,j) quad "otherwise")
$

选择阶段采用贪婪准则：

$
bold(x)_i^(g+1) = cases(bold(u)_i^g quad J(bold(u)_i^g) <= J(bold(x)_i^g), bold(x)_i^g quad "otherwise")
$

若试验个体优于父代，则父代进入外部档案，同时记录本次成功的 $F_i$、$(C R)_i$ 及适应度改变量。历史记忆通过加权 Lehmer 平均更新：

$
"mean"_("WL")(S) = frac(sum_n w_n s_n^2, sum_n w_n s_n), quad
w_n = frac(|J(bold(x)_n) - J(bold(u)_n)|, sum_l |J(bold(x)_l) - J(bold(u)_l)|)
$

为提高后期开发能力，AL-SHADE 还采用线性种群规模缩减策略。设初始种群规模为 $P_("init")$，最小种群规模为 $P_("min")$，则当前目标种群规模为：

$
P_(g+1) = "round" lr(frac(F_("Es"), F_("max")) dot (P_("min") - P_("init")) + P_("init"))
$

随着函数评估次数增加，种群规模逐步从 $P_("init")$ 降至 $P_("min")$，计算资源也从广域探索转向局部精细搜索。

在无人机路径规划中，AL-SHADE 的实现流程可以概括为：初始化种群、历史记忆和外部档案；为每个个体生成 $F_i$ 与 $(C R)_i$；按自适应概率选择变异策略；交叉得到试验个体；根据适应度执行选择并更新外部档案；最后更新历史记忆和种群规模。与 PSO、ACO、GA、MSA 和 AHA 相比，AL-SHADE 更适合连续航点坐标优化，可以在保持搜索多样性的同时逐步提高局部开发能力。但传统 AL-SHADE 仍未显式考虑路径连续性、障碍威胁感知和外部档案相似解冗余问题，这也是后文进行改进的主要原因。



== 本章小结

本章围绕无人机集群全局路径规划算法来展开，首先建立了一个统一的适应度函数，在这个评价框架里面，路径长度、平滑性、静态障碍风险、机间安全、通信限制、通道宽度还有编队保持等因素都被纳入其中，该函数为不同的优化算法提供了共同的比较标准，同时也让后续的实验可以从任务效率、安全性以及队形稳定性这些方面去进行综合分析。

在算法层面，本章分别介绍了 PSO、ACO、GA、MSA、AHA 和 AL-SHADE 在二维航点优化问题当中的设计方式，PSO 的收敛速度比较快，ACO 和 GA 比较适合用在经典启发式的对比上，MSA 与 AHA 则借助生物启发机制来增强搜索的多样性，而 AL-SHADE 依靠历史参数记忆和线性种群缩减机制，更适合对连续航点坐标去做自适应的优化，通过上面这些内容，可以明确各个算法在全局路径规划里的基本作用、适用特点以及潜在不足，下一章将会在这个基础之上进一步建立无人机编队控制模型和局部避障模型，为全局路径的执行与检验提供支撑。

= 编队模型构建

无人机集群路径规划要解决的不只是从起点到目标点找出一条可行的航迹，它还需要让多架无人机在飞行的过程当中保持稳定的队形，并且能够在有障碍物的环境里协同地进行避障，所以在使用全局规划结果去执行任务之前，需要先建立起编队的运动描述和队形限制模型，把无人机相互之间的相对位置关系、通信拓扑以及一致性控制方式都明确下来，这一节先对无人机编队控制模型进行建模，这样就为前面全局路径规划结果的跟踪、后续的局部避障还有联合仿真打好了基础。

 == 编队控制模型设计与实现
 === 无人机编队模型
在建模的时候，先把无人机看成质点，并且用地面坐标系去描述它的位置和运动状态；在水平面内，原点 O 是可以根据实际需要去选取的，对于无人机编队的二维平面描述，通常有 l–ψ 法和 l–l 法这两种形式，本文所采用的是 l–l 法，无人机相互之间的相对位置关系由矩阵 $R_x$、$R_y$ 来表示。
 $ cases(
  R_x = mat(delim: "[",
    r^x_(1 1), r^x_(1 2), dots.h, r^x_(1 n);
    r^x_(2 1), r^x_(2 2), dots.h, r^x_(2 n);
    dots.v, dots.v, dots.down, dots.v;
    r^x_(n 1), r^x_(n 2), dots.h, r^x_(n n)
  ),
  
  R_y = mat(delim: "[",
    r^y_(1 1), r^y_(1 2), dots.h, r^y_(1 n);
    r^y_(2 1), r^y_(2 2), dots.h, r^y_(2 n);
    dots.v, dots.v, dots.down, dots.v;
    r^y_(n 1), r^y_(n 2), dots.h, r^y_(n n)
  ),

) $
在这里，$r^x_(i j)$、$r^y_(i j)$ 表示的是第 $i$ 架无人机和第 $j$ 架无人机之间在两个坐标方向上的期望相对位置差，当 $i = j$ 的时候有 $r^x_(i j) = r^y_(i j) = 0$。

多架无人机所形成的一个理想稳定队形可以写成下面的形式：
$ cases(
  |x_i - x_j| -> r^x_(i j),
  |y_i - y_j| -> r^y_(i j),
  |v_i - v_j| -> 0,
) $ 

其中 $x_i, y_i$ 是无人机的坐标，$v_i$ 是速度。

=== 无人机动力学模型

在无人机的编队建模当中，常常会采用包含自驾仪的三自由度运动学模型，在基础的運動学方程里面，纵向运动和横向运动之间是存在耦合关系的，对横向航向自驾仪和纵向自驾仪做解耦处理之后，就可以建立起一个横纵分离的运动学模型，其中，无人机 i 的运动模型如公式(3)所示。
$ cases(
  dot(x)_i = v_i cos theta_i,
  dot(y)_i = v_i sin theta_i,
  dot(theta)_i = omega_i,
  dot(v)_i = frac(1, tau_v) (v_i^c - v_i),
  dot(theta)_i = frac(1, tau_theta) (theta_i^c - theta_i),
) $ <eq:kinematics>

在这组式子里，$v_i$ 是无人机在 XOY 平面上的速度；$theta_i$ 是航向；$omega_i$ 是航向角速度；$tau_v$ 是速度通道的时间常数；$tau_theta$ 是航向角通道的时间常数；$v_i^c$ 是无人机自动驾驶仪的速度参考输入；$theta^c_i$ 是无人机自动驾驶仪的航向角参考输入。

在 XOY 平面上的运动存在下面这种耦合关系。
$ cases(
  tan theta_i = frac(v_(y i), v_(x i)),
  v_i = sqrt(v_(x i)^2 + v_(y i)^2),
) $ <eq:coupling>
$v_(y i), v_(x i)$ 分别是无人机在 XOY 平面上的横向速度和纵向速度。

这样一来，自动驾驶的动力学方程就可以被表示成：
$ cases(
  dot(x_i) = v_(x i),
  dot(y_i) = v_(y i),
  dot(v_(x i)) = frac(1, tau_v) (v^c_(x i) - v_(x i)),
  dot(v_(y i)) = frac(1, tau_v) (v^c_(y i) - v_(y i)),
) $ <eq:autopilot>

因为无人机本身存在机动性的问题，所以需要给它添加动力学的限制条件，如公式(6)所示。
$ cases(
  v_i in (v_min, v_max),
  dot(v_i) in (a_min, a_max),
  dot(theta_i) in (omega_min, omega_max),
) $ <constraints>

=== 集群控制模型

这一节以一致性原理作为基础，分别去设计无人机在横向和纵向上的控制策略，并且把物理机动限制纳入到控制律里面，由此得到本文所采用的协同控制算法。

在多无人机系统当中，无人机之间的信息交互关系通常用图论去描述，设集群里面一共有 $n$ 架无人机，它们的通信拓扑可以表示成无向图或者有向图 $G = (V, E)$，为了描述节点相互之间的连接关系，引入邻接矩阵 $A = [a_(i j)] in bb(R)^(n times n)$ 和拉普拉斯矩阵 $L = [l_(i j)] in bb(R)^(n times n)$，邻接矩阵的元素 $a_{i j}$ 被用来表示通信链路：当无人机 $i$ 可以获取到无人机 $j$ 的信息时，$a_{i j}=1$，否则 $a_{i j}=0$。

拉普拉斯矩阵被定义成 $L = D - A$，其中的度矩阵是 $D = "diag"(d_1, d_2, dots.h, d_n)$，节点的入度为 $d_i = sum_{j=1}^n a_{i j}$，已经有文献表明，拉普拉斯矩阵不仅仅反映了系统的图论拓扑特征，它的特征值分布也和一致性算法的收敛性以及集群飞行的稳定性密切相关，只有当通信拓扑图里面包含了生成树的时候，控制系统才能够实现无偏的渐近收敛。

Tao 等@tao2023 把无人机的水平运动建模成了双积分器系统：$dot(bold(p))_i = bold(v)_i$，$dot(bold(v))_i = bold(u)_i$，其中 $bold(u)_i = (u_(x i), u_(y i))^T$ 是待设计的虚拟加速度控制输入，沿着 $X_g$ 和 $Y_g$ 轴构造的基础一致性协议如下：

$
u_(x i)(t) = - sum_(j=1)^n a_(i j) [(x_i(t) - x_j(t) - r_(i j)^x) + gamma (v_(x i)(t) - v_(x j)(t))]
$

$
u_(y i)(t) = - sum_(j=1)^n a_(i j) [(y_i(t) - y_j(t) - r_(i j)^y) + gamma (v_(y i)(t) - v_(y j)(t))]
$

在式子里，$gamma > 0$ 所表示的是位置误差与速度误差之间的耦合增益，$r_(i j)^x = -r_(j i)^x$、$r_(i j)^y = -r_(j i)^y$ 是前面已经定义过的期望相对位移，这个协议让每一架无人机都沿着两个坐标轴朝邻居的期望相对位置去靠拢，同时让它的速度逐渐接近邻居集合的加权平均值。

上面这种基础形式是默认通信没有延迟，而且拓扑结构一直保持不变的，但是在实际飞行当中，机间的数据链通常都会存在传输延迟；受到信道质量、机间距离还有障碍物遮挡这些因素的影响，通信链路也可能会中断或者恢复，针对这些情况，Tao 等@tao2023 提出了一种可以同时处理非对称通信时延和切换拓扑的改进一致性控制律。

把 $tau_(i i)(t)$ 记作无人机 $i$ 自身状态感知与解算的自延迟，$tau_(i j)(t)$ 则是 $j$ 到 $i$ 的机间通信延迟，这两者需要满足有界性的条件：$0 <= tau_m(t) <= h_m$ 并且 $dot(tau)_m(t) <= d_m < 1$，令 $N_i(t)$ 为 $t$ 时刻无人机 $i$ 的有效邻居集，$a_(i j)(t)$ 为对应的时变邻接权重，那么 $X_g$ 与 $Y_g$ 方向上的改进一致性控制输入就可以写成：

$
u_(x i)(t) = sum_(V_j in N_i(t)) a_(i j)(t) thin [
  k_1 (x_j(t - tau_(i j)) - x_i(t - tau_(i i)) - r_(j i)^x) \
  + k_2 (v_(x j)(t - tau_(i j)) - v_(x i)(t - tau_(i i)))
] + dot(v)_x^"*" - k_3 (v_(x i)(t) - v_x^"*")
$ <improved-consensus-x>

$
u_(y i)(t) = sum_(V_j in N_i(t)) a_(i j)(t) thin [
  k_1 (y_j(t - tau_(i j)) - y_i(t - tau_(i i)) - r_(j i)^y) \
  + k_2 (v_(y j)(t - tau_(i j)) - v_(y i)(t - tau_(i i)))
] + dot(v)_y^"*" - k_3 (v_(y i)(t) - v_y^"*")
$ <improved-consensus-y>

式子里各个参数的物理含义是这样的：$k_1 > 0$ 是位置偏差增益，它负责控制编队几何构型的恢复强度；$k_2 > 0$ 是速度一致性增益，用来调节邻机之间的速度同步速率；$k_3 = k_1 k_2$ 是速度跟踪增益，它让集群的整体速度向期望的巡航速度 $(v_x^"*", v_y^"*")$ 去对齐；$dot(v)_x^"*"$、$dot(v)_y^"*"$ 是参考加速度的前馈项，在编队匀速巡航的时候取零；$r_(j i)^x$、$r_(j i)^y$ 则采用反对称的形式，以便匹配控制量的增量方向。

和基础的协议相比起来，改进之后的算法在三个层面上做了增强：(1) 它明确地把非对称通信时延 $tau_(i j)$ 与 $tau_(i i)$ 引了进来，让各架无人机基于实际通信时序当中的延迟状态，而不是理想的同步状态去进行决策；(2) 把时不变的邻接权重换成了时变的形式 $a_(i j)(t)$，这样就让控制律可以自适应通信链路的动态通断，只要切换序列满足联合连通的条件就能够保证收敛@tao2023；(3) 增设了参考速度的前馈通道，赋予编队整体上的机动跟随能力，以此来满足路径规划层所下达的变速巡航需求。

控制输入 $bold(u)_i$ 到自驾仪速度指令的映射，由一阶惯性环节的逆模型来给出：

$
cases(
  v_(x i)^c = v_(x i) + tau_v u_(x i),
  v_(y i)^c = v_(y i) + tau_v u_(y i),
) $ <control-to-autopilot>

这个变换把自驾仪的动态响应滞后补偿掉了，让无人机在稳态下面可以紧密地跟踪一致性控制量的期望加速度。

==== *飞行约束的调整策略*

@eqt:control-to-autopilot 所求出来的指令，还没有把@eqt:constraints 里面所列出的机动能力边界考虑进去，Tao 等@tao2023 设计了一种分两级递进的最小调整映射，在保证指令可行性的前提下，尽量去保持原始控制的方向。

先由 $bold(u)_i$ 算出合加速度的幅值 $alpha_i = sqrt(u_(x i)^2 + u_(y i)^2)$，再以采样周期 $Delta t$ 去预估下一步的速度 $v_i(t+Delta t) = v_i(t) + alpha_i Delta t$，要是预估出来的值越界了，那就收紧加速度的有效区间 $[a_(min,i)^"new", a_(max,i)^"new"]$，当 $alpha_i$ 超出了上界的时候，就按比例去压缩两轴的分量，以此来保持合加速度的方向不发生变化：

$
u'_(x i) = frac(a_(max,i)^"new", alpha_i) u_(x i), quad
u'_(y i) = frac(a_(max,i)^"new", alpha_i) u_(y i)
$

再用缩放之后的加速度去预估下一时刻的航向：
$
theta_i(t+Delta t) = arctan(frac(v_(y i) + u'_(y i) Delta t, v_(x i) + u'_(x i) Delta t))
$

根据最大的航向角速率 $omega_max$、$omega_min$ 来确定航向的可达区间：

$
theta_(min,i) = theta_i(t) + omega_min Delta t, quad
theta_(max,i) = theta_i(t) + omega_max Delta t
$

假如预测出来的航向偏离了这个区间，那就在保持合加速度幅值不变的前提下面，把航向修正到最近的边界值 $theta_("bound")$ 上，再联立求解出加速度的最终可行解 $(u''_(x i), u''_(y i))$，经过这两级修正之后的控制量通过式@eqt:control-to-autopilot 映射为满足全部机动限制的自驾仪指令。

==== *离散时间实现*

控制器是以周期 $Delta t$ 来迭代运行的，在每一个控制步当中，先采集邻居的延迟状态，再根据@eqt:improved-consensus-x 和@eqt:improved-consensus-y 去计算虚拟加速度；在经过了前面所说的两级限制映射之后，控制器输出自驾仪的速度指令，并驱动无人机的状态进行更新，这样一种结构可以在二维平面里面同时兼顾拓扑变化和飞行安全，为全局路径跟踪以及后续的局部避障提供一个稳定的编队控制基础@tao2023。

在前面的编队控制模型中，本文已经给出了多架无人机之间的队形约束、通信拓扑关系以及一致性控制律，这些内容主要用于保证集群在飞行过程中保持相对稳定的几何构型。不过，在复杂多障碍物环境中，仅有全局参考路径和编队控制还不够，因为无人机在执行任务时仍然可能遇到全局规划阶段没有完全覆盖的局部风险，例如障碍物位置估计误差、传感器探测范围受限、通道突然收窄以及单机避障和队形保持之间的局部冲突。因此，需要在编队控制模型的基础上进一步引入局部避障算法，用来在飞行执行阶段对单架无人机的运动方向进行短时修正。

== 局部避障算法设计与实现

传统人工势场法是局部避障里面比较经典的一类方法，它是由 Khatib 在 1986 年提出来的，它的基本思想是在无人机所处的工作空间当中去构造一个虚拟的势场：目标点会产生吸引势，让无人机朝着任务目标或者当前的参考航点去运动；障碍物则会产生排斥势，使无人机在靠近障碍物的时候受到一种远离障碍物的作用，无人机的运动方向是由吸引力和斥力的合力共同来决定的，所以这种方法可以在不重新去做全局规划的情况下，根据当前的位置和与障碍物之间的距离快速地生成避障的修正量。

设无人机当前的位置为 $bold(p)_i$，目标的航点为 $bold(p)_("tgt")$，障碍物的中心为 $bold(c)_O$，障碍物的影响距离为 $d_0$，无人机到障碍物表面的距离则记为 $d_O = norm(bold(p)_i - bold(c)_O) - R_O$，传统的 APF 通常会把目标引力的势函数写成下面这种形式

$
U_("att")(bold(p)_i) = frac(1, 2) k_("att") norm(bold(p)_i - bold(p)_("tgt"))^2
$

和它相对应的目标引力就是

$
bold(f)_("att") = - nabla U_("att") = k_("att") (bold(p)_("tgt") - bold(p)_i)
$

障碍物斥力的势函数一般会被写成分段的形式：

$
U_("rep")(bold(p)_i) = cases(
  frac(1, 2) k_("rep") lr(frac(1, d_O) - frac(1, d_0))^2 quad d_O < d_0,
  0 quad d_O >= d_0
)
$

由这个势函数所得到的斥力就是

$
bold(f)_("rep") = cases(
  k_("rep") lr(frac(1, d_O) - frac(1, d_0)) frac(1, d_O^2) frac(bold(p)_i - bold(c)_O, norm(bold(p)_i - bold(c)_O)) quad d_O < d_0,
  bold(0) quad d_O >= d_0
)
$

这样一来，传统 APF 所给出的局部避障合力就可以被表示成

$
bold(f)_("APF") = bold(f)_("att") + sum_(O in cal(O)) bold(f)_("rep", O)
$

从实现的角度来看，传统 APF 的结构是比较直观的，计算量也比较小，很适合被嵌入到无人机的在线控制回路里面去，当无人机离障碍物比较远的时候，障碍物的斥力会趋近于零，这时候运动就主要是由目标引力和编队控制来决定的；而当无人机进入到障碍物的影响范围里面的时候，斥力就会逐渐地增大，对原来的运动方向做出修正，这样一来碰撞的风险就被降低了，也正是因为这种实时性比较强、实现又比较简单的特点，人工势场法常常被拿来作为局部避障层的基础方法，和编队控制层配合在一起使用。

== 编队综合控制律与局部避障约束

在把传统 APF 接入到编队控制回路里面之后，每一架无人机 $i$ 在一个控制周期 $Delta t$ 内所接收到的控制量，就可以看成是由两部分组成的：一部分是编队一致性控制所给出的加速度指令 $bold(u)_i^("cons")$，它主要是负责去维持多机之间的相对位置关系；另一部分则是由传统人工势场法所给出的局部势场合力 $bold(f)_("APF")$，它同时包含了目标引力和障碍物斥力，被用来引导无人机朝着参考航点去运动并且远离障碍物，所以，在传统 APF 的条件下，综合控制律就可以写成

$
bold(u)_i^("cmd") = bold(u)_i^("cons") + bold(f)_("APF") $ <integrated-control>

在这个式子里，$bold(u)_i^("cons")$ 是由前面改进过的一致性协议所得到的，$bold(f)_("APF")$ 则是由目标引力 $bold(f)_("att")$ 和障碍物斥力 $bold(f)_("rep")$ 叠加起来得到的，经过这样处理之后，编队控制层负责去保持集群的结构，传统 APF 层则负责提供面向目标以及避开障碍物的局部修正，这两者通过加速度指令在同一个控制周期里面叠加在一起。

在把它输出给自动驾驶仪之前，综合之后的加速度指令还需要去满足无人机自身的动力学约束，先来做速度约束的校验，根据当前的速度和控制周期去预估下一个时刻的速度：

$
bold(v)_i(t + Delta t) = bold(v)_i(t) + bold(u)_i^("cmd") Delta t
$

如果 $norm(bold(v)_i(t + Delta t)) > v_("max")$，那就先把预估的速度缩放到最大速度的边界上面，再由受到限制之后的速度去反算出可行的加速度：

$
bold(v)_i^("lim") = frac(v_("max"), norm(bold(v)_i(t + Delta t))) bold(v)_i(t + Delta t), quad
bold(u)_i^("cmd") arrow frac(bold(v)_i^("lim") - bold(v)_i(t), Delta t)
$

接下来再做加速度约束的校验，如果 $norm(bold(u)_i^("cmd")) > a_("max")$，那就把控制指令按照比例缩放到可行的范围里面：

$
bold(u)_i^("cmd") arrow frac(a_("max"), norm(bold(u)_i^("cmd"))) bold(u)_i^("cmd")
$

在经过了速度和加速度这两级的限制之后，控制量再通过@eqt:control-to-autopilot 转换成自动驾驶仪可以去执行的速度指令，这样看来，传统 APF 的局部避障力并不是单独地去作用在无人机上，而是和编队一致性控制一起构成了闭环的控制输入，并且在经过了动力学约束的映射之后才参与到实际的飞行当中。

== 分层耦合任务执行框架

前文已经分别把编队控制模型、全局路径规划算法还有传统 APF 局部避障方法的基本思想建立了起来，在实际执行任务的时候，这三个部分并不是彼此独立地在运行，而是在同一个闭环里面分层地去进行配合，全局规划层会先给出从起点到目标点的参考航路，编队控制层再把这条航路转化成多无人机协同飞行当中的队形保持指令；当飞行过程里遇到了近场的障碍物，局部避障层就会根据目标引力和障碍物斥力对控制指令做出实时的修正，经过这样的处理以后，系统既可以把全局路径的整体质量保留下来，也能够兼顾到编队的稳定性和局部的飞行安全。

在任务开始的时候，系统会先把起点、目标点、障碍物的分布、无人机的数量还有期望的编队构型这一类先验信息输入进去，全局路径规划算法在统一适应度函数的限制下面去搜索航点序列，并且生成一条面向领航者的全局参考路径，在这之后，领航者就沿着参考路径逐步地去跟踪当前的目标航点；而其余的跟随者则根据期望的相对位移、邻接拓扑还有一致性控制律来生成编队保持的控制量，让集群在整体运动的过程当中尽可能地去维持预设的队形。

进入到飞行执行阶段以后，局部避障层会以一个比较高的控制频率嵌入到编队控制回路里面，如果无人机进入了障碍物的影响范围，APF 模块就会根据目标的方向和障碍物的距离去算出安全的修正量；要是周围的环境比较安全，局部避障项就会趋近于零，这个时候系统就主要是由全局路径跟踪和编队保持控制来主导，换句话说，局部避障并不是要去替代全局规划，而是在保持任务方向的基础上面去提供必要的安全偏置；至于怎么样去进一步增强 APF 的绕行能力和动态安全裕度，这将在下一章的算法改进部分去展开。

从任务执行的角度来看，可以把整个流程理解成这样：全局规划先确定一个大致的飞行方向，编队控制负责去维持多机的结构，局部避障则在出现风险的时候进行短时间的修正，全局规划层对路径长度、到达时间还有平滑性产生影响；编队控制层会影响队形误差和机间的安全裕度；局部避障层关系到的则是集群在面对近场障碍物扰动的时候能不能够及时地做出调整，后续算法的改进将会分别围绕全局路径搜索和局部避障修正来展开，并且最终通过同一个分层耦合框架去反映到无人机集群任务的整体表现上面，具体的流程见下图所示：

#capfig(
  image("figures/分层耦合.png", width: 73%),
  caption: [分层耦合示意图],
  label: <test>,
)

== 本章小结

本章主要完成了无人机集群路径规划任务里面的模型构建工作，首先，针对多无人机协同飞行的问题，建立了建立在虚拟领航者基础上的编队控制模型，并且用邻接矩阵和拉普拉斯矩阵去描述无人机相互之间的通信关系，在这个基础之上，给出了把位置误差、速度误差还有通信时延都考虑进去的一致性控制律，与此同时加进了速度、加速度和航向角速率等方面的限制，这样一来就使编队控制的结果能够更加符合无人机实际运动的条件。

然后，本章介绍了传统人工势场法在局部避障当中的基本思想，说明了目标引力和障碍物斥力对无人机编队模型会产生怎样的影响，传统 APF 的优点在于结构比较简单、实时性也比较好，可以作为局部避障层的一个基础模型；但是它在复杂障碍物的附近也容易出现局部极小值和绕行能力不够这些问题，所以还是需要在后续的章节里面去做进一步的改进。

最后，本章说明了全局规划、编队控制还有局部避障相互之间的耦合关系，全局路径为领航者提供参考航路，编队控制把这条航路转化为多机的协同运动，局部避障则在近场风险出现的时候对控制指令进行修正，这三者一起构成了完整的任务执行框架，由此可以看出来，全局规划路径的质量会继续影响队形保持和局部避障的效果，传统 APF 的局限也会影响到局部安全修正的可靠性，所以下一章将会围绕编队路径规划算法和局部避障算法去展开改进，以此来提高在复杂障碍物环境下面的路径连续性、安全性以及编队适应性。


= 编队避障和全局规划算法的改进

上一章已经把全局路径规划、编队控制还有传统 APF 局部避障之间的分层耦合关系建立了起来，但是从编队路径规划的执行效果来看，系统里面仍然有两类问题是需要进一步去改进的：一类是来自局部避障层的问题，传统 APF 的实时性虽然比较好，但是在障碍物比较密集的区域却比较容易出现局部极小值以及绕行能力不够的情况；另一类则是来自全局路径规划层的问题，前文已经对多类全局路径规划算法进行了建模和比较，在这当中 AL-SHADE 具有比较好的连续优化能力和参数自适应能力，所以本文把它选作后续改进的主要对象，传统 AL-SHADE 容易受到档案冗余、搜索步长不具备环境感知能力以及离散航点路径连续性不足这些方面的影响，针对这些问题，本章分别从局部避障机制和全局路径表达这两个层面来对编队路径规划算法做出改进：一方面，在传统人工势场法的基础上面加入切向旋转斥力机制，以此来提高无人机在近场障碍物附近的主动绕行能力；另一方面，围绕 AL-SHADE 来构建 TALG 这个改进框架，通过外部档案相似度去重、由种群威胁度所驱动的 Lévy-Gaussian 双态缩放因子还有 B 样条曲线参数化路径建模，来改善全局搜索过程当中的历史解利用、环境风险感知以及轨迹连续性。

== 改进人工势场法

传统人工势场法也存在比较明显的局限，首先，在“U 型”障碍物、狭窄通道或者障碍物分布比较密集的区域当中，吸引力和斥力的合力有可能接近零，这就会让无人机陷入局部极小值，表现出来的就是停滞、振荡或者反复绕行，再一个，传统 APF 的斥力方向通常是沿着从障碍物到无人机的径向方向的，它缺少一种主动绕行的切向引导能力，而且在多机编队的场景里还容易和队形保持控制产生耦合冲突，因此，这一节主要是从斥力场建模这个方面来对传统 APF 做出改进，以此来增强无人机在复杂障碍物环境下面的避障能力。

=== 障碍物斥力场的切向旋转增强 
在障碍物斥力场的切向旋转增强这一部分，经典 APF 的斥力方向是严格地沿着从障碍物中心指向无人机的径向线的，当无人机、障碍物和目标大致处在同一条直线上的时候，合力的方向也主要落在这条直线上，这样就很难自然地产生绕行的动作，为了解决这个问题，本文在径向斥力之外又加入了一个切向旋转分量，从而形成了一个矢量复合斥力场。

设无人机 $i$ 当前的位置为 $bold(p)_i = (x_i, y_i)^T$，速度矢量为 $bold(v)_i = (v_(x i), v_(y i))^T$，对于当前探测范围内的障碍物，先把距离无人机最近的那一个障碍物 $O^"*"$ 筛选出来，这里用间隙距离 $d = norm(bold(p)_i - bold(c)_("obs")) - R_("obs")$ 来作为度量，其中 $bold(c)_("obs")$ 和 $R_("obs")$ 分别代表障碍物的中心和半径，斥力的作用范围被定义成 $d_("inf") = R_("obs") + d_("safe") + 50$，这里的 $d_("safe")$ 是预先设置好的安全距离，本文把它取为 20 m，当 $d >= d_("inf")$ 的时候斥力为零；而当 $d < d_("inf")$ 的时候，斥力就由径向分量 $bold(f)_("rad")$ 和切向分量 $bold(f)_("tan")$ 合成在一起。

径向斥力沿着从无人机指向障碍物中心的单位向量 $bold(e)_r$ 方向，它的幅值大小由归一化之后的距离强度 $sigma = max(0, (d_("inf") - d) / d_("inf"))$ 来调控：

$
bold(f)_("rad") = k_r sigma bold(e)_r, quad bold(e)_r = frac(bold(p)_i - bold(c)_("obs"), norm(bold(p)_i - bold(c)_("obs"))) $ <radial-repulsion>

在式子里 $k_r = 25$ 是径向斥力的增益，$sigma in [0, 1)$ 会随着无人机靠近障碍物而变大，这就使得斥力在障碍物边缘附近能够达到一个比较大的值。

切向斥力是这一节改进的重点，先取出 $bold(e)_r$ 逆时针方向的正交单位向量 $bold(e)_t = (-e_(r, y), e_(r, x))^T$，为了让切向力的方向尽量顺着当前飞行的方向，引导无人机绕过障碍物而不是反向折返回去，需要对 $bold(e)_t$ 和速度矢量做点积校核：如果 $bold(v)_i dot bold(e)_t < 0$，那就把 $bold(e)_t$ 取反，切向斥力的表达式为：

$
bold(f)_("tan") = k_t sigma bold(e)_t $ <tangential-repulsion>

这里面 $k_t = 15$ 是切向力的增益，加入切向分量以后，合力的方向就不再只是沿着径向线变化了，而是会带有一种顺着障碍物边缘转动的趋势，这样一来，在“U 型”障碍物或者狭窄通道里面，无人机就可以获得主动绕行的运动分量，局部死锁的概率也就随之降低了。

复合斥力就是这两个分量加在一起 $bold(f)_("rep") = bold(f)_("rad") + bold(f)_("tan")$，当无人机位于开阔区域并且离障碍物比较远的时候，$sigma ≈ 0$，斥力自然就退化成了零，这就避免了给集群编队带来不必要的扰动，而在靠近障碍物的时候，径向分量主要负责安全的避让，切向分量则提供绕行的引导，两者一起实现比较平滑的规避动作。

/* === 前视预测减速机制
对于前视预测减速机制，目标不可达问题通常会在目标附近的障碍物斥力把引力抵消掉的时候出现，这时候无人机很容易在终点前面停滞下来，为了缓解这个问题，本文引入了一种建立在运动预测基础上的前视安全评估，具体的做法是沿着无人机当前的速度方向，用固定的时间步长向前预测一小段轨迹，再去计算预测出来的轨迹到各个障碍物的最小间隙，如果这个间隙比减速阈值还要小，那就按照比例去降低引力的参考速度，让无人机用一个比较安全的速度靠近目标区域。

具体来说，取预测的视距 $T_h = 5.0$ s，并把它等分成 $N_s = 25$ 个微段，微段的时间步长就是 $Delta t_s = T_h / N_s$，把当前的位置当作起点，在速度保持恒定的假设下面逐段往前递推，就可以得到 $N_s$ 个预测的位置点，对每一个预测点都计算出它到各个障碍物的间隙，然后把整个预测时域里面的最小值记为 $d_("pred")$：

$
d_("pred") = min_(k=1)^(N_s) min_(O in cal(O)) lr(norm(bold(p)_i + k Delta t_s bold(v)_i - bold(c)_O) - R_O) $ <predicted-clearance>

把减速阈值设成 $d_("slow") = 2.5 d_("safe") = 50$ m，前视减速因子 $lambda$ 被定义为：

$
lambda = cases(
  1 quad d_("pred") >= d_("slow"),
  max(0.2, frac(d_("pred"), d_("slow"))) quad 0 <= d_("pred") < d_("slow"),
  0.2 quad d_("pred") < 0
)
$ <speed-reduction>
当 $d_("pred") >= d_("slow")$，也就是说前方比较安全的时候，$lambda = 1$，引力速度保持原来的大小不变；而当 $d_("pred") -> 0$ 的时候，$lambda arrow 0.2$，引力速度就下降到巡航值的 20%，这样做就可以让无人机在密集的障碍区里以较低的速度穿行，避免因为高速逼近之后机动余量不够而难以及时转向，把 $lambda$ 作用到导航参考速度 $bold(v)_("ref")$ 上，就可以得到经过安全缩放之后的有效参考速度 $bold(v)_("ref")^("eff") = lambda bold(v)_("ref")$。

*目标引力场。* 在目标引力场部分，引力场的作用是驱动无人机沿着全局路径的航点序列去飞行，设当前的目標航点为 $bold(p)_("tgt")$，也就是全局规划器输出并经过 APF 局部修正之后的下一个航点；无人机 $i$ 的位置是 $bold(p)_i$，目标方向、期望巡航速度以及引力加速度指令分别为：

$
"direction" arrow norm( bold(p)_("tgt") - bold(p)_i), quad "distance" arrow norm("direction") $ <target-direction>

$
bold(v)_("ref") = v_("cruise") dot frac("direction", "distance"), quad v_("cruise") = 25 space "m/s" $ <ref-velocity>

$
bold(u)_("nav") = dot(bold(v))_("ref") - k_3 thin (bold(v)_i - bold(v)_("ref")^("eff")) $ <nav-guidance>

当无人机距离目标航点不到 50 m 的时候，巡航速度会按照比例做线性的缩减（$v = max(5, 25 dot d / 50)$），这样一来航点切换的过程就会变得更加平滑；等到距离最终目标不到 1 m 的时候，引力就被置为零，无人机由编队控制来接管并且完成最后的悬停。 */

/* *控制参数分析与标定。* 在控制参数分析与标定这方面，APF 局部避障器主要涉及三个参数：径向斥力增益 $k_r$、切向力增益 $k_t$ 还有安全距离 $d_("safe")$，本文把 $k_r$ 取成 25，这样就可以让无人机在 $d_("safe") = 20$ m 这个缓冲区边缘，也就是 $sigma ≈ 0.5$ 的位置，受到大约 12.5 N/kg 的径向斥力，从而能够在两到三个控制周期里面改变 80 m/s 巡航状态下的航向，切向力增益被取成 $k_t = 15$，这时候切向分量差不多是径向分量的 60%，从仿真的调试情况来看，如果这个比例太小，也就是小于 30% 的话，绕行的效果就不太明显，而如果比例太大，也就是大于 90% 的话，又有可能让无人机过分地偏离目标方向，安全距离 $d_("safe") = 20$ m 是把编队包络半径，大约是 60 m，以及 GPS 和惯导组合定位的误差，大约是 5–10 m，综合起来考虑之后确定的，它在障碍物周围形成了两级缓冲：在 $d_("safe")$ 以内是强斥力的紧急避让区，在 $d_("safe")$ 到 $d_("inf")$ 之间则是线性衰减的预警区。

前视预测参数取的是 $T_h = 5.0$ s、$N_s = 25$，这对应着大约 0.2 s 每段的时间分辨率，在 80 m/s 的速度下面，它的空间分辨率大约是 16 m 每段，这样就可以提前一到两个预测段去发现前方的障碍物，减速阈值 $d_("slow") = 2.5 d_("safe") = 50$ m 和下限 $lambda_("min") = 0.2$ 配合在一起使用，可以让无人机在进入到安全缓冲区之前就开始减速，从而避免高速冲进紧急避让区之后，因为机动能力不足而没办法及时转向的情况。 */


在实际执行的时候，改进之后的人工势场法并不是单独地去替代编队控制器，而是作为局部安全修正的项被嵌入到已有的编队控制回路里面，每一架无人机 $i$ 在每一个控制周期 $Delta t = 0.1$ s 里面，都会接收到来自三个功能通道的加速度指令：编队一致性控制通道、目标引力通道还有障碍物避让通道，这三者采用加性叠加的方式去形成最终的控制输入：

$
bold(u)_i^("cmd") = bold(u)_i^("cons") + bold(f)_("att")  + bold(f)_("rep") 
$ <improved-integrated-control>


这样一种叠加的方式使得改进之后的 APF 可以和分层耦合框架保持一致：当无人机远离障碍物的时候，$bold(f)_("rep")$ 会逐渐地趋近于零，这时候运动就主要是由全局路径跟踪和编队控制来决定的；而当无人机进入到障碍物影响范围里面的时候，径向斥力负责把安全距离拉开，切向斥力负责提供绕行的趋势，得到了 $bold(u)_i^("cmd")$ 之后，仍然是按照第三章所给出的速度约束和加速度约束去进行限幅，再通过@eqt:control-to-autopilot 转换成自动驾驶仪可以去执行的速度指令。

== 外部档案相似度去重

传统基于成功历史参数自适应学习的差分进化算法在对外部档案 $cal(A)$ 进行更新的时候，通常采用的是“当档案满了就随机替换掉其中一项”这种方式来存入被淘汰的父代向量，这个方法实现起来是很简单的，但在路径规划任务里面却比较容易带来冗余，随着种群在迭代后期慢慢靠近某一个较优的解，很多被淘汰掉的父代向量都会和这个解非常接近，也就是说在 $bb(R)^(2N)$ 空间当中的距离很小，而随机替换策略没有办法识别出这类相似的个体，于是档案当中就可能积累起大量几乎相同的解，这样计算出来的加权档案均值 $bold(x)_("mean")^t$ 就会变得更像是单一聚类中心的一个近似估计，很难反映出多个独立优质区域的分布信息。

在基于欧氏距离的相似度判别这一块，TALG 在每一次准备向外部档案 $cal(A)$ 写入被淘汰的父代向量 $bold(x)_("parent")$ 的时候，都会先把它和当前档案里面所有成员之间的欧氏距离计算出来：

$
d_"min"^("archive") = min_(j=1)^(|cal(A)|) norm(bold(x)_("parent") - bold(a)_j)_2 $ <archive-min-dist>

本文把相似度阈值设定为 $tau_("sim") = 25.0$，它的含义可以这样来理解：在 $2N$ 维的航点坐标空间里面，当 $N = 20$、$D = 40$ 的时候，如果两个个体的欧氏距离小于 25，那么平均到每个维度上的差异大约是 $25 / sqrt(40) ≈ 4.0$，这一类个体解码之后得到的候选路径在几何形态上已经很接近了，它们给加权均值计算所提供的信息也基本上是相似的。

在去重决策逻辑方面，比较了 $d_"min"^("archive")$ 与阈值 $tau_("sim")$ 之后，档案的更新会按照下面这几种情况来处理：

$
"ArchiveUpdate"(bold(x)_("parent")) = cases(
  "替换最近邻" bold(a)_("nearest")quad"if " d_"min"^("archive") < tau_("sim") space "and" space J_"parent" < J_"nearest",
  "丢弃父代"quad "if " d_"min"^("archive") < tau_("sim") space "and" space J_"parent" >= J_"nearest",
  "存入档案（追加或随机替换）"quad "if " d_"min"^("archive") >= tau_("sim")
) $ <archive-dedup-logic>

如果父代和档案当中的某个成员在空间上很接近，但是父代的适应度更优，那就用父代把这个相似的成员替换掉，这样做既可以减少重复的解，也能够把该区域内比较好的搜索结果保留下来，如果父代和档案成员相似而且适应度又不占优势，那就说明这个区域已经有更好的代表解了，父代就不再被写进档案，如果父代和档案里所有的成员都保持着足够的距离，那就说明它可能是来自一个还没有被覆盖到的新区域；这个时候要是档案还没有满就直接追加进去，要是档案已经满了就随机替换掉其中一项，以此来保留传统 L-SHADE 档案更新当中的随机性。

在去重效益分析方面，引入了相似度去重之后，外部档案里面保留下来的解会变得更加分散，重复的信息也会减少，在障碍物比较密集的路径规划场景当中，种群往往会集中到少数几条可行的狭窄通道附近，如果不去做去重处理，容量为 $N_A ≈ 1300$（$P = 500$，$r_("arc") = 2.6$）的档案里面，很可能会有相当一部分的槽位都存放着相互距离不到 25 的近重复解，而去重之后，档案就可以覆盖到更广的优质解区域，$bold(x)_("mean")^t$ 的计算也不再过度依赖单一的聚类，这样一来，current-to-Amean/1 变异策略当中的差分方向 $(bold(x)_("mean")^t - bold(x)_i^t)$ 就可以获得区分度更高的搜索引导。

== 缩放因子的环境感知生成
TALG 的第二项改进是把种群威胁度 $T_i in [0, 1]$ 引了进来，用它来描述单条候选路径在面对障碍物时的风险水平，传统的 AL-SHADE 在生成变异因子的时候基本上不去区分个体所处的环境，而这里则是把路径和障碍物之间的空间关系压缩成了一个标量，$T_i$ 越大，就说明这条路径越靠近障碍物，甚至已经出现了碰撞，这个时候算法就应当倾向于采用更大尺度的变异，让个体能够尽快地离开危险的区域。
=== 种群威胁度量化模型
在三区威胁映射这一块，对于候选路径 $bold(x)_i$，先算出各个采样点到所有障碍物表面的净空距离，再从里面取出最小值，全路径最小净空被记作 $d_("min", i) = min_(j=1)^100 min_(O in cal(O)) (norm(bold(p)_j - bold(c)_O) - R_O)$，随后按照下面的式子把它映射成威胁度：

$
T_i = cases(
  1.0 quad d_("min", i) <= 0 space "(碰撞区)",
  exp(lr(-frac(2 d_("min", i), d_("safe") - d_("min", i) + epsilon))) quad 0 < d_("min", i) < d_("safe") space "(过渡区)",
  0.0 quad d_("min", i) >= d_("safe") space "(安全区)"
) $ <threat-mapping>

当 $d_("min") <= 0$ 的时候，路径至少有一个地方进入了障碍物的内部，威胁度就直接取为 1，要是 $d_("min") >= d_("safe") = 20$ m，那就说明整条路径都和障碍物保持着安全间隔，威胁度就取为 0，算法可以更多地采用高斯态去做精细的搜索，处在两者之间的是过渡区，这里并没有使用简单的线性缩放，而是采用了一个指数函数 $exp(-2 d_("min") / (d_("safe") - d_("min")))$，让路径越是接近障碍物的时候风险上升得就越快，这样的处理方式更加符合直观的判断：从 20 m 接近到 10 m 所引起的风险变化，通常要比从 5 m 接近到 1 m 的风险变化来得小。

=== 威胁驱动的 Lévy-高斯双态变异缩放因子

在得到了个体威胁度 $T_i$ 之后，TALG 就把这一风险信息继续反馈到 DE 的变异过程里面，用它来调节缩放因子 $F$ 的生成方式。

在传统方法的局限方面，传统 AL-SHADE 对所有个体都是从柯西分布 $F_i ~ "Cauchy"(M_(F, h_i), 0.1)$ 里面去采样 $F$ 的，柯西分布具有重尾的特性，所以 $F$ 会有一个比较小的概率取到比较大的值（$F ≫ 1$），这在理论上是可以帮助受困的个体跳出局部最优的，但是它并不关心个体所处的环境风险到底怎么样，处在开阔安全区域的个体，还有已经贴近障碍物、接近碰撞的个体，在传统的框架里面获得大 $F$ 的概率是完全一样的，这样就容易带来两个问题：安全的个体有可能被偶然的大步长扰动，破坏掉已经取得的收敛结果，而危险的个体又不一定能够及时地获得足够强的逃逸步长。

在双态采样引擎这一块，TALG 把个体的威胁度 $T_i$ 当作切换概率，建立起一个 Lévy-Gaussian 双态 $F$ 生成器，对于个体 $i$，从历史记忆槽 $h_i$ 里面读出 $F$ 的基准位置参数 $mu_F = M_(F, h_i)$，再以尺度参数 $sigma_F = 0.1$ 来生成缩放因子：

$
F_i = cases(
  mu_F + sigma_F dot "Lévy"(beta) quad "if " "rand"(0, 1) <= T_i space "(Lévy 态)",
  mu_F + sigma_F dot cal(N)(0, 1) quad "otherwise" space "(高斯态)"
) $ <talg-dual-mode>

生成之后的 $F_i$ 还需要再做一下边界处理，如果 $F_i <= 0$，那就重新采样，最多重试 100 次；在比较极端的情况下就把它设成机器精度 $epsilon$，以免搜索停滞下来，如果 $F_i > 1$，就把它截断为 1，因为 $F = 1$ 已经对应了全幅的差分跳跃，可以看作是搜索步长的上限。

高斯态，也就是 $"rand" > T_i$ 的时候，沿用了经典 SHADE 的采样思路，通过均值附近的正态扰动来细调搜索步长，比较适合安全区域当中的平滑收敛，当 $T_i ≈ 0$ 的时候，个体几乎总是会进入高斯态，Lévy 态，也就是 $"rand" <= T_i$ 的时候，则是用来处理受障碍物威胁比较高的那些个体，这个时候算法会以概率 $T_i$ 把正态噪声替换成由标准 Mantegna Lévy 分布（$beta = 1.5$）所产生的随机步长，Lévy 分布的方差是无限的，尾部按照幂律去衰减（$P(|x| > t) ∝ t^(-beta)$），它生成大幅值步长的概率比常规的正态扰动和柯西采样都要高，在 $T_i ≈ 1$ 这种高威胁的场景下面，个体大约会有 30%～40% 的概率获得 $F > 0.8$ 的大幅变异，和纯柯西采样大约 15% 的概率相比起来是有明显提升的。

在 Mantegna Lévy 步长生成这一块，Lévy 随机步长是通过 Mantegna 算法来生成的，对于稳定指数 $beta in (0, 2)$，先构造两个相互独立的正态变量 $u ~ cal(N)(0, sigma_u^2)$ 和 $v ~ cal(N)(0, 1)$，其中：

$
sigma_u = lr(frac(Gamma(1 + beta) sin(pi beta / 2), Gamma((1 + beta) / 2) * beta * 2^((beta-1)/2)))^(1 / beta) $ <mantegna-sigma>

那么 Lévy 步长就可以写成 $s = u / (|v|^(1/beta))$，$beta = 1.5$ 是群智能优化里面比较常用的一个折中取值，$beta$ 越小，分布的尾部就越重，跳跃也就越激进；$beta$ 越接近 2，分布就越接近正态分布，为了保证数值的稳定，分母里面引入了一个小量 $epsilon = 10^(-12)$，用来避免除零的问题。

在威胁自适应的整体效果方面，双态 $F$ 生成器把环境风险引入到了参数采样当中，让搜索步长可以随着空间状态而变化，在开阔的空域里面，$T_i ≈ 0$，大多数个体都以高斯态来运行，$F$ 围绕着历史成功均值 $mu_F$ 做小幅的波动，变异的步长比较稳定，算法就更适合去优化路径长度和平滑性，在障碍物密集的通道当中，$T_i -> 1$，更多的个体会进入 Lévy 态并产生较大幅度的差分跳跃，这类跳跃有机会把个体带到障碍物另一侧的安全区域，也可以通过外部档案 $cal(A)$ 把大尺度探索所产生历史信息保留下来，进而影响到后续代的加权档案均值 $bold(x)_("mean")^t$ 和 current-to-Amean/1 的变异方向，由于高斯态与 Lévy 态的比例是由 $T_i$ 连续调节的，搜索行为就不会在安全区和危险区之间突然切换，而是呈现出一种更加平滑的过渡。


== B 样条曲线参数化路径建模

B 样条曲线是一类由控制点和节点向量一起确定的分段多项式参数曲线，它和直接把离散航点连接起来所得到的折线路径不一样，而是通过控制点间接地去决定曲线的形态，因此它具有局部支撑、连续性比较好还有端点可以被限制这些特点，在把它用于无人机路径规划的时候，优化的阶段仍然可以只维护有限个控制点；到了适应度评估和轨迹执行的阶段，再由这些控制点去生成连续光滑的飞行路径，这样做就可以缓解离散航点编码所带来的急转弯、折线还有曲率突变的问题。

在曲线定义这一块，设控制点序列为 $bold(c)_0, bold(c)_1, dots, bold(c)_m in bb(R)^2$，其中 $m+1$ 是控制点的数量；设 B 样条的次数为 $p$，节点向量为

$
bold(U) = (u_0, u_1, dots, u_(m+p+1))
$ <bspline-knot-vector>

那么 $p$ 次 B 样条曲线就可以被表示成

$
bold(C)(u) =
sum_(i=0)^m N_(i,p)(u) bold(c)_i,
quad u in [u_p, u_(m+1)]
$ <bspline-def>

这里面 $N_(i,p)(u)$ 是第 $i$ 个 $p$ 次 B 样条基函数，基函数是由 Cox-de Boor 递推公式给出来的，零次基函数被定义成

$
N_(i, 0)(u) = cases(
  1 quad"if " u_i <= u < u_(i+1),
  0 quad "otherwise"
) $ <bspline-basis0>

当 $r = 1, 2, dots, p$ 的时候，$r$ 次基函数按照下面的方式递推：

$
N_(i,r)(u) =
frac(u - u_i, u_(i+r) - u_i) N_(i,r-1)(u)
+ frac(u_(i+r+1) - u, u_(i+r+1) - u_(i+1)) N_(i+1,r-1)(u)
$ <bspline-basisr>

如果递推式里面的分母为 0，那么对应的分式项就按 0 来处理，从这个递推关系可以知道，在任意一个参数 $u$ 的地方只有有限个相邻的基函数是非零的，所以单个控制点通常只会影响到曲线的一段局部形状，也就是说，优化器在调整某个控制点的时候，一般不会轻易地破坏掉整条路径的结构，这对提高变异和交叉操作的有效性是有好处的。

在节点向量构造方面，为了让曲线的起点和终点分别通过首尾的控制点，也就是要满足 $bold(C)(u_p) = bold(c)_0$ 和 $bold(C)(u_(m+1)) = bold(c)_m$，本文采用的是 clamped 均匀节点向量，具体的做法是在节点向量的两端各自设置 $p+1$ 个重复节点，使得曲线的端点被首尾控制点所限制；中间节点则均匀分布，用来保持参数区间的划分相对均衡，节点向量可以写成

$
u_i = cases(
  0 quad 0 <= i <= p,
  frac(i - p, m - p + 1) quad p < i <= m,
  1 quad m < i <= m + p + 1
) $ <clamped-knot>

其中 $i = 0, 1, dots, m+p+1$，本文采用的是三次 B 样条，也就是 $p = 3$，三次 B 样条在普通节点的地方具有 $C^2$ 连续性，可以让路径的位置、切向方向和曲率的变化都更加平滑，这也更符合无人机在受到速度、加速度和航向角速度限制时候的实际飞行特点。

在 de Boor 递推求值这一块，实际计算的时候，如果直接按照基函数的定义去求 $bold(C)(u)$，就需要显式地计算多个基函数，过程会比较繁琐，本文采用 de Boor 算法来对 B 样条曲线进行稳定的求值，在给定参数 $u$ 之后，先确定它所在的节点区间 $[u_s, u_(s+1))$，也就是

$
u_s <= u < u_(s+1), quad s in {p, p+1, dots, m}
$ <deboor-span>

随后取出受这个参数影响的 $p+1$ 个控制点，并且初始化

$
bold(d)_j^((0)) = bold(c)_(s - p + j),
quad j = 0, 1, dots, p
$ <deboor-init>

第 $r$ 轮的递推，其中 $r = 1, 2, dots, p$，按照下面的线性插值来进行：

$
bold(d)_j^((r)) =
(1 - alpha_(j,r)) bold(d)_(j-1)^((r-1))
+ alpha_(j,r) bold(d)_j^((r-1))
$ <deboor-recur>

其中

$
alpha_(j,r) =
frac(u - u_(s - p + j), u_(s + 1 + j - r) - u_(s - p + j)),
quad j = r, r+1, dots, p
$ <deboor-alpha>

在完成了 $p$ 轮的递推之后，曲线点就由

$
bold(C)(u) = bold(d)_p^((p))
$ <deboor-result>

来得到，de Boor 算法只对和当前参数相关的局部控制点进行递推，单点求值的复杂度是 $O(p^2)$，本文固定采用三次 B 样条，$p$ 可以看成是一个常数，所以这个求值过程在路径采样当中具有比较好的计算效率和数值稳定性。

在路径重构与适应度评估这一块，在 TALG 框架当中，优化器仍然是以实数向量的形式去维护候选解，设个体 $bold(x)_i in bb(R)^(2N)$ 解码之后得到了 $N$ 个中间控制点，把它和起点 $bold(S)$ 还有终点 $bold(T)$ 拼接起来，就可以得到完整的控制点集合

$
cal(C)_i = {bold(S), bold(c)_1, bold(c)_2, dots, bold(c)_N, bold(T)}
$ <bspline-control-set>

然后在参数区间 $[u_p, u_(m+1)]$ 上面均匀地选取 $N_s$ 个参数值，再利用 de Boor 算法生成采样路径

$
cal(P)_i = {bold(C)(u_1), bold(C)(u_2), dots, bold(C)(u_(N_s))}
$ <bspline-sampled-path>

适应度函数里面的路径长度 $L$、平滑性 $S$、综合风险 $R$、通道宽度惩罚 $W$ 还有编队保持代价 $F$，都是基于采样点集合 $cal(P)_i$ 来计算的，不再直接去使用控制点的折线，这样一来，被评价的对象就是实际执行的时候更接近真实情况的光滑轨迹，而不是控制点连线所形成的那种近似路径。

这样一种参数化的方式可以带来两个直接的好处，其一，B 样条曲线本身就具有连续性，可以比较明显地减少折线路径当中的急转弯和曲率突变，让路径更容易满足无人机的飞行限制，其二，每个控制点只影响到有限的局部曲线段，优化器在调整单个控制点的时候不太容易引起全局路径的剧烈震荡，所以差分变异和交叉操作也就更容易生成可接受的试验向量。

在降维效应方面，B 样条参数化还能够在保留路径表达能力的同时去降低搜索的难度，在经典的航点编码当中，如果设置 $N = 20$ 个中间航点，那么优化的维度就是 $D = 40$，如果改用少量的控制点去描述曲线的主体形态，需要优化的自由控制点就可以减少到大约 $5 ~ 7$ 个，对应的搜索维度也就降到了 $10 ~ 14$，在比较低的维度下面，差分向量 $(bold(x)_(r_1) - bold(x)_(r_2))$ 当中的有效信息会更加集中，交叉操作也更容易把有意义的路径结构保留下来，所以，B 样条不仅仅改善了路径的平滑性，也降低了 AL-SHADE 在高维航点空间当中的搜索压力。

== 本章小结

本章围绕编队路径规划算法的执行需求，分别从局部避障机制和全局路径优化这两个方面去做了改进。首先，针对传统 APF 在复杂障碍物附近比较容易出现局部极小值、目标不可达还有绕行能力不够的问题，本文加入了切向旋转斥力机制，使得无人机在靠近障碍物的时候，既能够获得径向的安全避让，也能够形成一种顺着障碍物边缘绕行的运动趋势，同时通过综合控制律把编队控制、目标引导还有局部避障修正耦合到了同一个加速度指令里面。

在全局路径优化这个方面，本章围绕传统基于成功历史参数自适应学习的差分进化算法在无人机路径规划当中的不足，提出了 TALG 这个改进框架，传统的 AL-SHADE 虽然具有比较好的连续优化能力，但是当它被直接用到航点规划上的时候，仍然存在三个比较明显的问题：外部档案容易积累相似的解，变异参数对障碍物的威胁不够敏感，离散航点编码也容易生成折线式的轨迹，针对这些问题，本章分别从档案维护、搜索步长和路径表达这三个角度去做了改进。

首先，外部档案相似度去重机制是通过欧氏距离去判断待存入的父代和档案个体之间的相似程度的，如果两者过于接近，那就只把适应度更优的那个代表解保留下来；要是差异比较大，那就把它作为新的搜索区域信息写进档案里面去，这样做就可以减少重复的历史解对档案均值的干扰，让 current-to-Amean/1 变异策略获得区分度更高的引导方向。

然后，本章建立了一个基于障碍物净空距离的种群威胁度模型，并且把它引入到了 Lévy-Gaussian 双态缩放因子的生成过程当中，处在开阔区域的个体会更多地采用高斯态小步长去搜索，这样便于去细化路径的长度和平滑性；而靠近障碍物或者处在危险通道当中的个体，则更容易进入到 Lévy 态，通过比较大的步长来跳出局部受限的区域，这样一来，变异的尺度就不再只是依赖于历史的成功经验了，它也能够对环境的风险做出响应。

最后，本章引入了 B 样条曲线来对路径做参数化建模，优化器不再直接去评价控制点的折线，而是先由控制点去生成连续的平滑曲线，然后再基于曲线的采样点去计算适应度，这种处理方式既能削弱离散航点所带来的急转弯，也能够降低搜索的维度，使得 AL-SHADE 的差分变异更容易产出可执行的路径结构。上面所说的全局规划改进与改进之后的 APF 局部避障，共同构成了本文编队路径规划算法改进的核心内容，下一章将会通过仿真实验，从路径形态、收敛曲线还有多项飞行指标这些方面去检验改进算法的实际效果。

= 实验结果分析

== 实验设置与环境配置

=== 仿真环境与障碍物布局

实验采用了一片 $1000 times 1000$ m 的二维仿真空域，无人机集群从 $bold(S) = (0, 0)$ 出发，目标点被设在 $bold(T) = (1000, 1000)$，障碍物的集合由 21 个圆形障碍物组成，它们主要被分成两类：一类是 13 个沿着主对角线 $y = x$ 两侧交替分布的结构化障碍物，偏距为 120 m，半径 $R = 35$ m，它们被用来模拟城市低空走廊里面比较规整的建筑群；另一类是 8 个在 $(100, 900) times (100, 900)$ 区域内均匀随机生成的散落障碍物，半径同样为 $R = 35$ m，它们被用来模拟非结构化环境里小尺度的未知障碍，另外，本文还在 $(500, 500)$ 的位置设置了一个中心障碍物，这样一来，从起点到终点的直连方向上就形成了一个比较难穿越的瓶颈区域，这个位置两侧的安全通道宽度大约是 60 m，对路径规划算法的探索能力和局部开发能力都提出了比较高的要求。

所有的障碍物都被设为静态，也就是 $bold(v)_("obs") = bold(0)$，编队里面包含了 5 架无人机，其中 1 架是虚拟领航者，另外 4 架是僚机，基础队形采用 V 型，僚机的相对偏移量分别是 $(-20, 20)$、$(-20, -20)$、$(-40, 40)$、$(-40, -40)$ m，仿真步长被固定为 $Delta t = 0.1$ s，单次实验的最长运行时间设为 $T_("max") = 100$ s。

=== 对比算法与参数配置

在这次实验里一共比较了七类全局路径规划算法，为了保证结果具有可比性，各个算法统一采用 $N = 20$ 的航点编码，并且使用五分量的加权适应度函数 $J = 0.24 L_"norm" + 0.18 S_"norm" + 0.32 R_"norm" + 0.14 W_"norm" + 0.12 F_"norm"$，局部避障层统一采用改进之后的 APF，参数为 $k_r = 25$、$k_t = 15$、$d_("safe") = 20$ m；编队控制层采用的是第 2 章给出的改进一致性协议，参数为 $k_1 = 0.8$、$k_2 = 1.2$、$k_3 = 0.8$，除了 AHA 因为访问表的开销比较大而取 $n = 50$ 之外，其余算法的初始种群规模和迭代次数都被设为 $P_"init" = 500$、$T = 500$，其他的参数则沿用对应算法文献里面的常用推荐值，具体的配置如下：

#captab(
  caption: [对比算法参数配置一览],
  label: <exp-algo-config>,
)[
  | 算法            | 种群/迭代  | 类别             | 关键参数                                        |
  | PSO             | 500 / 500  | 经典群体智能     | $omega = 0.6$, $c_1 = 1.6$, $c_2 = 1.4$          |
  | ACO            | 500 / 500  | 经典群体智能     | $K = 20$, $q = 0.1$, $xi = 0.85$                 |
  | GA              | 500 / 500  | 经典进化算法     | $p_m = 0.1$, $sigma_0 = 40$, $k = 3$             |
  | MSA             | 500 / 500  | 新型群体智能     | $P = 2$, $rho = 6$, $a = 0.5$, $P_c = 0.2$       |
  | AHA             | 50 / 500   | 新型群体智能     | 无外部控制参数                                  |
  | AL-SHADE (传统) | 500 / 500  | 自适应 DE        | $H = 15$, $p_("best") = 0.15$, $r_("arc") = 2.6$  |
  | AL-SHADE-TALG-QU（改进方法） | 500 / 500 | 改进自适应 DE    | $H = 15$, $p_("best") = 0.15$, $r_("arc") = 2.6$, $tau_("sim") = 25$, $beta = 1.5$ |
]

=== 评价指标体系

实验从四个维度去评价算法的性能：

（1）*任务效率指标*：这里面包括了平均路径长度 $L$（m）和领航者的到达时间 $t_("arr")$（s），用它们来衡量路径的飞行经济性还有任务完成的效率。

（2）*轨迹平滑性指标*：采用的是平均航向偏转角（°）来衡量路径转向是不是平缓，这个指标越小，就说明路径的几何连续性越好，对无人机的稳定跟踪也越有利。

（3）*编队保持指标*：采用的是相对队形误差 RMS（m）来衡量僚机相对于期望队形的偏离程度，这个指标越小，就说明编队越稳定。

（4）*集群安全指标*：采用的是最小无人机间距（m）来衡量多机飞行过程中的内部安全裕度，这个指标越大，就说明无人机之间发生近距离冲突的风险越低。

所有的算法都在相同的障碍物布局、相同的编队控制器以及相同的局部避障参数下面运行，之后再对上面这些指标进行统一的统计和比较。

== 路径规划可视化对比

=== 全局路径形态对比

下面这张图给出的是七种全局规划算法和改进 APF 局部避障器联合运行之后的终态轨迹，图中的黑色虚线表示的是全局规划路径，红色曲线表示的是领航者的实际轨迹，其余的彩色曲线表示的是四架僚机的轨迹，蓝色的圆形区域表示的是障碍物以及它的安全缓冲区，每幅图的左上角都标注了对应算法的最终到达时间、最小无人机间距还有碰撞状态。

#capsubfig(
  (
    (content: image("figures/lujing/final_snapshot_pso_apf.png", height: 5.2cm, fit: "contain"), subcaption: [PSO + APF]),
    (content: image("figures/lujing/final_snapshot_aco_apf.png", height: 5.2cm, fit: "contain"), subcaption: [ACO + APF]),
    (content: image("figures/lujing/final_snapshot_ga_apf.png", height: 5.2cm, fit: "contain"), subcaption: [GA + APF]),
    (content: image("figures/lujing/final_snapshot_msa_apf.png", height: 5.2cm, fit: "contain"), subcaption: [MSA + APF]),
    (content: image("figures/lujing/final_snapshot_aha_apf.png", height: 5.2cm, fit: "contain"), subcaption: [AHA + APF]),
    (content: image("figures/lujing/final_snapshot_alshade_origin_apf.png", height: 5.2cm, fit: "contain"), subcaption: [AL-SHADE + APF]),
    (content: image("figures/lujing/final_snapshot_alshade_talg2_apf.png", height: 5.2cm, fit: "contain"), subcaption: [AL-SHADE-TALG-QU + APF]),
  ),
  columns: 2,
  caption: [七种算法联合 APF 局部避障后的终态轨迹对比],
  label: <exp-path-compare>,
)

从整体的轨迹上来看，七种算法都能在 APF 局部避障器的辅助下到达目标点，图里面标注的碰撞状态也都是 No，这说明局部避障层可以对全局规划路径进行安全方面的修正，不过，全局路径本身的质量还是会直接影响到 APF 的介入程度以及编队轨迹的形态，如果全局路径在障碍物密集的区域过于贴近障碍物，僚机的轨迹往往就会出现比较明显的横向偏移和队形拉伸；要是全局路径比较平滑，并且留出了足够的通行空间，领航者和僚机的轨迹就更容易保持平行和一致。

PSO、GA、MSA 和 AHA 的终态轨迹整体上比较接近，基本上是沿着主对角线的方向去穿越障碍物的分布区域，并且在中段绕开了多个圆形的障碍物，这里面，PSO 的最终时间为 77.70 s，最小无人机间距为 19.11 m；GA 的最终时间为 81.00 s，最小无人机间距为 19.77 m；MSA 和 AHA 的最终时间是 77.00 s和77.50s，最小无人机间距分别为 18.79 m 和 19.83 m，上面这些算法都能够完成任务，不过在中段和靠近终点的地方，僚机的轨迹出现了一定程度的队形展开，这就说明局部的避障修正还是会对编队结构产生影响。

ACO 的轨迹是最曲折的，最终到达时间是 86.00 s，在七种算法当中是最长的，它的全局路径在中部区域出现了比较明显的弯折，这就使得领航者和僚机在多个障碍物之间产生了额外的绕行，虽然最终并没有发生碰撞，但是任务的效率偏低，最小无人机间距也只有 18.12 m，这和后文五项指标当中 ACO 路径长度最长、到达时间最长的结果是吻合的。

传统 AL-SHADE 的终态轨迹是最短的，到达也是最快的，最终时间是 71.30 s，不过从图上可以看到，它的全局路径在若干个障碍物之间选择的是比较直接的穿越方式，僚机的轨迹在局部区域需要依靠 APF 来做横向的修正，最终的最小无人机间距为 18.88 m，AL-SHADE-TALG-QU 的最终时间是 72.60 s，比传统的 AL-SHADE 稍微慢了一点，但是最小无人机间距提升到了 20.38 m，这是图示结果里面最高的安全裕度，同时，AL-SHADE-TALG-QU 的全局路径和领航者轨迹都要更加平滑，僚机之间的相对间隔也更加稳定，这就说明 B 样条参数化和威胁感知机制对轨迹的连续性与编队的安全性是有一定改善的。

总体来看，路径可视化的结果和五项指标的结果基本上是吻合的，传统 AL-SHADE 在任务效率上面更占优势，而 AL-SHADE-TALG-QU 则在路径较短、到达较快的基础上面，提高了最小的机间距和编队轨迹的一致性，它更适合那些对安全裕度要求比较高的无人机集群任务。


== 算法性能定量对比

为了不单单依靠适应度曲线去判断算法的好坏，本文进一步从飞行任务执行的效果出发，选取了五个具有直接物理意义的指标来做横向的比较，它们分别是平均路径长度、领航者到达时间、路径平滑性、相对队形误差还有最小无人机间距，在这当中，平均路径长度和到达时间反映的是任务效率，路径平滑性反映的是轨迹的可执行性，相对队形误差反映的是编队保持的能力，最小无人机间距反映的则是集群内部的安全裕度，除了最小无人机间距之外，其余的四项指标都是数值越小越优；最小无人机间距则是越大，说明机间的安全裕度越充分。

#capsubfig(
  (
    (content: image("figures/zhibiao/path_length1.png", width: 92%), subcaption: [平均路径长度]),
    (content: image("figures/zhibiao/arrival_time1.png", width: 92%), subcaption: [领航者到达时间]),
    (content: image("figures/zhibiao/smoothness1.png", width: 92%), subcaption: [路径平滑性]),
    (content: image("figures/zhibiao/formation1_error.png", width: 92%), subcaption: [相对队形误差]),
    (content: image("figures/zhibiao/min_distance1.png", width: 60%), subcaption: [最小无人机间距]),
  ),
  columns: 2,
  caption: [七种算法在五个飞行性能指标上的对比],
  label: <exp-five-metrics>,
)

五项指标的具体数值可以看表@exp-five-metrics-table，为了读起来方便，表里面用向下的箭头来表示数值越小越优，用向上的箭头来表示数值越大越优。

#captab(
  caption: [七种算法五项性能指标对比],
  label: <exp-five-metrics-table>,
)[
  | 算法 | 路径 (m) ↓ | 时间 (s) ↓ | 偏转 (°) ↓ | 队形 RMS (m) ↓ | 机间距 (m) ↑ |
  | PSO（粒子群） | 1646.43 | 77.70 | 0.937 | 9.977 | 19.48 |
  | ACO（蚁群） | 1805.31 | 86.00 | 1.062 | 9.271 | 18.06 |
  | GA（遗传） | 1684.37 | 80.90 | 0.930 | 9.274 | 19.59 |
  | MSA | 1640.24 | 77.00 | 0.908 | 9.154 | 18.59 |
  | AHA | 1659.34 | 77.50 | 0.952 | 9.395 | 18.74 |
  | AL-SHADE | 1540.46 | 71.30 | 0.835 | 9.214 | 19.12 |
  | AL-SHADE-TALG-QU | 1543.71 | 72.60 | 0.796 | 7.989 | 20.47 |
]

从任务效率来看，AL-SHADE 和 AL-SHADE-TALG-QU 的平均路径长度要明显短于其他的算法，它们分别是 1540.46 m 和 1543.71 m，两者之间只相差了 3.25 m，这就说明改进后的算法在引入了平滑和安全限制之后，并没有明显地牺牲掉路径的长度，在领航者到达时间这个方面，AL-SHADE 是最短的，为 71.30 s；AL-SHADE-TALG-QU 为 72.60 s，只比 AL-SHADE 多了 1.30 s，还是明显要比 PSO、ACO、GA、MSA 和 AHA 更优，从中可以看出来，改进算法在保持了比较高的飞行效率的同时，也给路径平滑性和编队安全性留出了更多的优化空间。

从轨迹平滑性来看，AL-SHADE-TALG-QU 的平均航向偏转是最低的，为 0.796°，优于 AL-SHADE 的 0.835°，也低于 ACO 的 1.062° 和 AHA 的 0.952°，这表明 B 样条曲线的参数化可以削弱离散航点连接所带来的局部折角，让领航者的轨迹变得更加连续，对于无人机集群来说，更小的航向偏转可以减少飞行控制里面的加速度突变，对僚机在跟随过程中维持队形也是有帮助的。

从编队保持的效果来看，AL-SHADE-TALG-QU 的相对队形误差 RMS 是 7.989 m，在七种算法里面是最低的结果，和传统 AL-SHADE 的 9.214 m 相比，误差降低了大约 13.30%；和 PSO 的 9.977 m 相比，误差降低了大约 19.93%，这就说明改进算法所生成的路径对编队控制层更加友好，可以减少局部的急转弯和通道压缩对队形结构的扰动，让僚机在跟随领航者的时候保持更加稳定的相对位置。

从安全性来看，AL-SHADE-TALG-QU 的最小无人机间距达到了 20.47 m，在所有算法里面是最大的，这就说明它在飞行过程当中所保留的机间安全裕度是最充分的，相比之下，ACO 和 MSA 的最小机间距分别是 18.06 m 和 18.59 m，安全裕度相对要弱一些，AL-SHADE 虽然路径比较短、到达时间也最优，但它的最小机间距是 19.12 m，比改进算法要低，综合来看，AL-SHADE-TALG-QU 在路径长度和到达时间上和传统 AL-SHADE 比较接近，同时在平滑性、队形误差还有机间安全距离这三个关键指标上取得了最优的结果，综合的飞行性能更加均衡。

== 五指标综合评价

为了进一步比较各个算法的整体表现，本文对五项指标做了名次的统计，路径长度、到达时间、平均航向偏转还有队形误差按照数值从小到大来排序，最小无人机间距按照数值从大到小来排序，单项排名越靠前，就表示算法在对应的指标上表现得越好；平均排名越小，就表示综合的性能越优。

#captab(
  caption: [七种算法五项指标排名统计],
  label: <exp-rank-table>,
)[
  | 算法 | 路径长度排名 | 到达时间排名 | 平滑性排名 | 队形误差排名 | 机间距排名 | 平均排名 |
  | PSO | 4 | 5 | 5 | 7 | 3 | 4.8 |
  | ACO | 7 | 7 | 7 | 4 | 7 | 6.4 |
  | GA | 6 | 6 | 4 | 5 | 2 | 4.6 |
  | MSA | 3 | 3 | 3 | 2 | 6 | 3.4 |
  | AHA | 5 | 4 | 6 | 6 | 5 | 5.2 |
  | AL-SHADE | 1 | 1 | 2 | 3 | 4 | 2.2 |
  | AL-SHADE-TALG-QU | 2 | 2 | 1 | 1 | 1 | 1.4 |
]

从表@exp-rank-table 里面可以知道，AL-SHADE-TALG-QU 的平均排名是 1.4，在七种算法当中是综合最优的，它的路径长度和到达时间都排在第二，和传统 AL-SHADE 的差距比较小；同时在路径平滑性、队形误差还有最小机间距这三项指标上排在第一，优势主要是体现在轨迹质量、编队稳定性还有安全裕度这些方面，传统 AL-SHADE 的平均排名是 2.2，综合表现是次优的，它的特点是任务效率最高，但是队形保持和机间安全不如 AL-SHADE-TALG-QU，MSA 的平均排名是 3.4，整体的表现比较均衡，GA 和 PSO 分别在最小机间距或者收敛速度方面有着一定的优势，但是综合排名是落后于 MSA、AL-SHADE 还有改进算法的，ACO 在本组指标当中的平均排名是最高的，这说明它在当前的场景下面不太适合作为复杂障碍物环境里面的首选规划算法。

== 综合分析与讨论

=== 算法选型建议

结合上面这些实验的结果，在不同应用场景下面的算法选择可以参考下面这些思路：

（1）*效率优先场景*：要是任务主要关注的是路径长度和到达时间，那就可以优先去考虑传统 AL-SHADE，它的平均路径长度是 1540.46 m，到达时间是 71.30 s，这两项都是七种算法里面最优的结果，比较适合那些对飞行时间比较敏感、对环境中安全裕度要求相对可控的任务。

（2）*安全与编队稳定优先场景*：这个场景推荐使用 AL-SHADE-TALG-QU，这种算法的最小无人机间距达到了 20.47 m，队形误差 RMS 降到了 7.989 m，在本组实验里面都是最优的；同时它的平均航向偏转只有 0.796°，这就说明它生成的路径更加平滑，可以减轻局部避障和编队控制层的调整压力。

（3）*综合均衡场景*：可以考虑 AL-SHADE-TALG-QU 或者 MSA，AL-SHADE-TALG-QU 在五项指标里面的平均排名是最低的，适合作为复杂环境下面综合优选的方案；MSA 在路径长度、到达时间、平滑性和队形误差上都是处在中上的水平，可以作为结构相对简单、实现复杂度适中的备选算法。

（4）*快速收敛或基准对比场景*：PSO 可以被用作基础的对比算法，它的适应度曲线收敛得最快，最小机间距排在第三，但是队形误差和路径平滑性并不占优势，所以，PSO 更适合被用来做算法的基准对照，或者是用在那些对实时性要求比较高、对轨迹品质要求相对比较低的场景里面。

=== 局限性与未来方向

实验的结果表明，AL-SHADE-TALG-QU 在多项指标上面有着比较好的综合表现，不过本文还是存在着一些局限，第一，实验主要是基于一组典型的障碍物环境来展开的，还没有在不同的障碍物密度、不同的通道宽度以及不同的集群规模下面系统地检验算法排名的稳定性，第二，B 样条控制点的数量、采样的密度以及曲线的次数对路径质量所产生的影响，还需要去做进一步的分析，第三，威胁度的阈值 $d_("safe")$、相似度阈值 $tau_("sim")$ 还有 Lévy 稳定指数 $beta$ 主要是依据经验来设定的，参数的自适应能力还有提升的空间，第四，当前的实验环境还是静态的二维障碍物场景，如果要把它扩展到三维动态障碍物还有真实的低空空域里面，那就还需要继续去研究。

= 总结与展望

== 工作总结

本文面对的是复杂多障碍环境下的无人机集群路径规划与协同飞行问题，为此建立了一个由全局路径规划、编队控制和局部避障组成的分层耦合框架，在这个框架里面，全局规划算法负责去生成任务级的参考路径，改进之后的一致性控制方法被用来维持多机的队形，到了执行阶段再通过改进的人工势场法去进行在线的安全修正，这样做就可以同时把路径的全局性、飞行安全、队形稳定还有局部避障的实时性都考虑进去。

在编队控制部分，以虚拟领航者作为核心来建立无人机集群的协同模型，并且利用邻接矩阵和拉普拉斯矩阵去描述通信拓扑的关系，在一致性控制律里面，本文加入了通信延迟、速度跟踪还有横向偏置调节的机制，使僚机在跟随领航者运动的时候能够保持住预设的相对位置，当通道发生收缩或者需要把障碍物绕开的时候，队形也能够进行适度的弹性调整，这就为后面的全局路径规划和局部避障提供了一个比较稳定的集群运动基础。

在全局路径规划部分，先建立了一个统一的适应度函数，把路径长度、路径平滑性、静态障碍风险、集群内部碰撞风险、通信风险、通道宽度限制还有编队保持代价都放到了同一个评价框架里面，在这个基础之上，本文对 PSO、ACO、GA、MSA、AHA 和 AL-SHADE 等算法进行了建模与对比，针对传统 AL-SHADE 在路径连续性、环境风险感知以及外部档案多样性方面所存在的不足，本文提出了 AL-SHADE-TALG-QU 这个改进框架，该框架通过外部档案的相似度去重来减少冗余的历史解，利用由种群威胁度所驱动的 Lévy-Gaussian 双态缩放因子来增强在障碍物密集区域的跳出能力，并且借助 B 样条曲线参数化路径建模来提升轨迹的连续性和平滑性。

在局部避障部分，在经典人工势场法的基础之上加入了切向的旋转势场力，无人机在接近障碍物的时候，不单单会受到径向斥力的作用，还会获得一种沿着障碍物边缘去绕行的切向引导，这样的处理方式缓解了传统人工势场法在障碍物前方比较容易出现局部极小值和震荡的问题，它也和全局规划层所输出的参考路径形成了互补：全局路径负责提供远距离的引导，局部避障则负责短时域里面的安全修正。




== 研究展望

本文的方法在二维静态障碍物环境下取得了比较好的规划效果，不过仍然还有进一步完善的空间，后续的研究可以从下面几个方面去推进。

第一，把它扩展到三维复杂空域和动态障碍物的场景当中，本文主要研究的是二维静态障碍物的环境，而实际低空空域里面常常会遇到高度限制、运动障碍物、临时禁飞区还有风场扰动这些因素，后续可以把 B 样条路径建模扩展到 $bb(R)^3$ 空间里面，并结合动态障碍物的预测模型去建立时空风险场，使得规划算法可以同时去处理空间避障和时间避碰的问题。

第二，继续对 B 样条控制点数量与优化维度之间的关系进行分析，本文采用 B 样条参数化来提升路径的连续性，但是控制点的数量、曲线的次数还有采样的密度会怎样去影响搜索的效率与路径的质量，这仍然需要更加系统的实验来支撑，后续可以通过消融实验来确定在不同的障碍物密度下面比较合适的控制点规模，并且研究自适应增删控制点的机制，让曲线的表达能力随着环境的复杂度去调整。

第三，把威胁度模型和参数选择的自适应能力提升上去，当前威胁度的阈值 $d_("safe")$、相似度阈值 $tau_("sim")$ 还有 Lévy 稳定指数 $beta$ 主要还是依据经验来设定的，后续可以把贝叶斯优化、强化学习或者在线参数估计的方法引进来，让算法能够根据障碍物的密度、通道的宽度还有种群的分布状态自动地去调整关键参数，从而减少对人工调参的依赖。

第四，面向实时重规划和机载部署去开展轻量化的研究，AL-SHADE-TALG-QU 在路径平滑性和安全裕度方面具有优势，但是 B 样条采样、威胁度的计算还有档案去重这些环节还是会带来一定的计算负担，后续可以从并行距离计算、增量式的 B 样条更新、档案压缩存储还有 GPU 加速这些方面去降低开销，并且对全局规划与局部避障之间的事件触发式重规划机制进行研究。

第五，结合真实的无人机平台来进行半实物或者实飞的检验，本文的实验主要是基于仿真环境的，还没有把传感器噪声、定位误差、通信丢包、执行器延迟还有飞控限制这些实际因素充分地考虑进来，后续可以在 ROS/Gazebo、AirSim 或者真实的多无人机平台上面去做进一步的检验，把算法的输出和低层的飞控接口结合起来，去分析它在真实系统当中的稳定性、鲁棒性还有工程的可用性。

总体来看，本文所提出的分层耦合路径规划框架和 AL-SHADE-TALG-QU 改进算法，在复杂障碍物的环境下面可以获得比较好的路径质量、安全性还有编队的协同性，随着三维动态建模、在线自适应优化还有真实平台检验这些方面逐步地完善起来，这种方法可以进一步去面向城市低空巡检、物流配送、灾害搜索还有多无人机协同侦察等任务来开展应用方面的研究。
